# Drift Report: types

**Spec**: `docs/specs/types.md` (last_reviewed: 2026-06-13)
**Date**: 2026-06-13
**Scope**: Deep-validated the **§17 Collation** surface/semantics (the change landed by `docs/plans/20260613-collation-axis.md`); ran spec-wide automated, timeless-oracle, and freshness checks. The other semantic sections (decimal §15, timezone §16, nullability §11, fragment sorts, …) were validated when they landed and their standing gates are green in the workspace run — not re-deep-audited here.

## Automated checks
- `cargo fmt` — ✅ PASS
- `cargo clippy --all-targets` — ✅ PASS (zero warnings)
- `cargo test` (workspace) — ✅ PASS (0 failures)
- `example_diagnostics` — ✅ PASS (104 passed, 1 ignored)

## Surface drift (§17 Collation)
- ✅ **`NonPortableCollation` diagnostic** — present in `DiagnosticCode` (`crates/smelt-db/src/diagnostics_types.rs:722`), in the spec Surface diagnostic-codes list, and in `docs-site/docs/reference/language.md`. Three-way consistent.
- ✅ **`COLLATE` clause** — lexed/parsed (`crates/smelt-parser/src/lexer.rs`, `syntax_kind.rs`), documented in the docs-site "String collation" section.
- ✅ **Binary allow-list** `{C, POSIX, BINARY, UTF8_BINARY}` (case-insensitive) — matches spec §17 (`crates/smelt-db/src/type_inference/collation.rs:20`); docs-site table matches.
- ⚠️ **`DataType` carries a collation / participates in equality·promotion·CAST** (§17, normative present tense) — not implemented (string `DataType` variants unchanged; no collation field). Disclosed in §Known Divergences ("the value-domain collation field … lands with the engine-declaration feature"). Documented spec-ahead-of-code gap, not silent drift.

## Semantics drift (§17)
- ✅ "Binary is the only portable collation" → `binary_string_comparison_oracle`, `binary_string_groupby_distinct_orderby_oracle`, `binary_collate_min_max_string_no_diagnostic` (live-DuckDB-oracle agreement).
- ✅ "Non-binary collation is not in the portable surface" → `non_binary_collation_diagnoses` (one `NonPortableCollation` Error; degrade to `Unknown`).
- ✅ "Binary names pass through unchanged" → `binary_collation_passes_through`.
- ⚠️ "Combining two different collations is a `TypeMismatch`" (§17) — not test-covered, not implemented; no portable trigger (everything is binary) and no `DataType` field. Disclosed as deferred with the engine-declaration feature.

(All tests above live in `crates/smelt-db/tests/collation_tests.rs`.)

## Invariant drift (§Constraints & Invariants)
- ✅ **No collation field on `DataType` / binary-in-portable** — upheld; the "Standing collation gate" (`cargo test -p smelt-db --test collation_tests`) is active and green.
- ✅ **Postgres `COLLATE "C"` pin** — the §Constraints wording was softened (this report's fix) to state the pin lands with the Postgres runtime backend, consistent with §Known Divergences; no longer stated as a live-but-unmet invariant.

## Timeless-oracle drift
- ✅ No phase vocabulary in `types.md` body or `docs-site/docs/reference/language.md`.

## Freshness
- last_reviewed: **2026-06-13**; most recent code change to referenced paths: **2026-06-13** (`08fcff46`, the collation work). Verdict: **fresh**.

## Fixes applied with this report
- Added `docs/plans/20260613-collation-axis.md` to References → Plans (history) (was only cited inline in §Known Divergences).
- Softened the §Constraints collation invariant: the Postgres `COLLATE "C"` emission pin is now stated as deferred to the Postgres backend rather than as a live invariant.

## Summary
- **No hard drift** — no spec rule is contradicted by code. The implemented portable binary-only contract matches §17 with full test coverage; the remaining gaps are exactly the deferrals the plan declared and §Known Divergences discloses (`DataType` collation field, engine-bound native collation + combining-mismatch rule, Postgres emission pin, fingerprint fold).
- **Recommended next step**: none required. The next collation work (the `DataType` field + engine-bound collation) is gated on the engine-declaration feature, not on closing drift.
