# Drift Report: datagen

**Spec**: docs/specs/datagen.md (last_reviewed: 2026-05-18)
**Date**: 2026-05-30
**Probe phase**: C7 (feature-sweep, probed early as a likely-bug-rich target)

### Automated checks
- datagen suite — PASS (4 + 21 tests)

### Surface / Semantics / Invariant drift
- ✅ No drift found. Every concrete "is a configuration error" rule the spec mandates is enforced and tested:
  - Empty `json_object.fields:` → `deserialize_non_empty_json_fields` (config.rs:427); tested `json_object_rejects_empty_fields`.
  - `shapes:` non-empty / `weight > 0` / `emit ≥ 1` / `pool_size ≥ 1` → dedicated deserializers (config.rs:56,96,110,136).
  - `sticky:` ⊆ `fields:` (Semantics ¶4) → config.rs:67.
  - Field-name agreement across shapes (Constraint 7, names) → config.rs:80-86.
  - `linked_choice` forbidden inside `shapes[].fields` (Constraint 8) → `contains_linked_choice` (config.rs:160-176).
  - `linked_choice` pool/field must resolve (Constraint 9) → config.rs:256,264; tested at config.rs:994.
  - Forward / missing `foreign_key` reference (Constraint 1) → empirically errors with a clear message ("'users' has not been listed yet. Move it before 'events'").

### Known Divergences — all documented design choices, not drift
- `json_object` emits `Utf8` not `Struct`, no array field type, float formatting unpinned across versions — intentional (§Design).
- Constraint 7's *type* agreement across shapes is explicitly **not** statically checked (documented, line 329) — so the absence of that check is not drift.
- No `smelt build` integration, implicit output schema — documented workflow gaps.

### Freshness
- last_reviewed: 2026-05-18 — fresh.

### Summary
- Drift items: 0. Datagen's config validation is mature and well-tested.
- No auto-fixable code bug surfaced (third mature spec in the sweep, after architecture and functions).
