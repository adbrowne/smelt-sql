# Validation report: `state` — 2026-09-05

Produced by phase 11 of `docs/outcomes/20260904-state-residency/outcome.md`, following
`.claude/commands/smelt/validate.md`'s process for `docs/specs/state.md`.

## Automated checks

- `grep -nE "Phase [A-Z0-9]+" docs/specs/state.md` — no hits (timeless-oracle clean).
- `rg -n "reconciliation\.json" crates/ docs-site/ docs/specs/` — 12 hits, all either the
  `run_state.md` legacy-migration sentence (the one sanctioned hit) or code/docs correctly
  describing the legacy file as inert dead weight, not a residency claim. No hit asserts
  `.smelt/`-resident correctness state.
- `cargo test -p smelt-cli --test state_docs_freshness` — 4/4 green, including the new
  `spec_references_are_live` test added this phase.

## Surface drift (spec vs. code)

- `StateMode`, `WarehouseTables` — present in `crates/smelt-core/src/config.rs`, matching
  §"`state.mode` and what each posture provides" and §"Opting out of warehouse bookkeeping".
- `MaintenanceStateDowngraded`, `DeclaredContractRequiresState` — both present verbatim in
  `docs/specs/diagnostics.md`'s unified table, matching §Diagnostics.
- `resolve_availability` — present in `crates/smelt-logical/src/maintenance/availability.rs`,
  matching §"The degradation contract" step 2 (pure function, `smelt-logical`-resident).
- `state_structure_unavailable` (the keyed-grain residue outcome's reporter event) — confirmed
  fully retired from `crates/` (zero hits); replaced by the recorded downgrade per criterion 3.
- No drift found.

## Semantics drift (spec vs. tests)

- `statement_parity` / `execute_parity` (37 + 4 tests): green — engine-resident ledger
  statements match the emitted-vs-executed parity gate.
- `maintenance_conformance` (78 tests, includes the state-deletion leg from phase 9): green —
  equivalence oracle holds under interleaved `.smelt/` deletion for every maintained recipe.
- `walk_coverage`: green — availability resolution composes through the shared walk, no ad hoc
  scan introduced.
- No drift found.

## Invariant drift

- "No correctness state outside the engine" (§Constraints & Invariants) — proven by the
  `maintenance_conformance` state-deletion leg above.
- "Transaction coupling" — proven by `statement_parity`'s ledger-statement coverage.
- "Equivalence is posture-independent" — proven by `maintenance_conformance` running under
  every posture including downgrades.
- No drift found.

## Timeless-oracle grep

- `docs/specs/state.md`: clean (see Automated checks).
- Docs-site pages the spec references (`reference/state.md`, `reference/smelt-yml.md`,
  `guide/targets.md`, `guide/incremental-models.md`, `reference/smelt-explain.md`): no
  `Phase [A-Z0-9]` hits.

## Freshness

- One pre-existing (not outcome-introduced) freshness flag was recorded by phase 6 against an
  unrelated 2026-09-04 commit; not this outcome's damage, left as a `docs/TODO.md` bullet per
  the phase-06 summary. No new freshness drift found in this pass.
- §References was stale (Code missing the phase-4 `availability.rs` owner and the
  `parse_warehouse_tables` fn; User docs and Plans (history) both still read `none yet`) —
  fixed this phase (see outcome.md decision log, phase 11).

## Verdict

`/smelt:validate state` is clean. All items above are landed or pre-existing/out-of-scope; no
open drift remains against this outcome's criteria.
