## Drift Report: cli

**Spec**: docs/specs/cli.md (last_reviewed: 2026-05-27)
**Date**: 2026-05-30
**Phase**: A3 (feature-sweep)

### Automated checks
- cargo fmt — PASS
- cargo clippy --all-targets — PASS (zero warnings)
- cargo test — PASS (full workspace green after fix)
- example_diagnostics — PASS (87 passed)
- example_workspaces (LSP) — PASS (27 passed)
- smelt-runtime parity — PASS

### Surface drift
- ✅ All `smelt` subcommands in the Surface table exist (`run`, `build`, `backbuild`, `seed`, `test`, `diff`, `table`, `type`, `status`, `history`, `explain`, `ui`, `docs generate/list/show/path`).
- ✅ `--version` / `--help` succeed in a non-project directory (Constraint #2). `smelt --version` → `smelt 0.3.2` (CARGO_PKG_VERSION), exit 0.
- ✅ `--dry-run` does **not** exist on `smelt build` (Constraint #7) — clap rejects it (exit 2). `smelt run --help` lists `--dry-run`.
- ✅ `smelt build --show-plan` without a positional model file is a hard error (Constraint #8): `Error: --show-plan requires a model file path as a positional argument`, exit 1.
- ✅ `smelt explain --json` schema matches the spec (models BTreeMap, `dependencies`, `materialization`, `incremental{…}`, `tags`, `owner`, `origin`, `execution_order`, optional `physical`). `crates/smelt-cli/src/explain.rs`.
- ✅ `smelt explain` excludes `materialization: test` models — verified on `examples/ephemeral_demo` (5 non-test models surfaced; `test_user_totals` etc. absent).
- ❌→**FIXED** Sub-directory seed not resolvable in CLI run/explain path. `examples/ephemeral_demo` (asserted clean by both LSP gates) failed `smelt build`/`explain`/`run` with `references undefined model/source 'lookup.regions'`. Root cause: `crates/smelt-cli/src/logical_graph.rs:78` keyed the seed validation set by leaf `name` instead of the canonical dot-path (`address_segments.join(".")`). See **BUG-005**. Fixed in-loop (red-green).

### Semantics drift
- ✅ `smelt build` lifecycle (load → validate target → discover → seed → plan → run) — exercised end-to-end by the example build suites.
- ✅ Argument resolution: a bare `--select <nonexistent>` is expanded via the entity resolver and errors `Model '…' not found` (exit 1), per §"Argument resolution algorithm" step 4. Not a no-op.
- ⚠️ No-op rebuild output — spec §"No-op rebuild output" mandates `smelt: no models matched the selector(s)` / `smelt: nothing to rebuild` on **stderr**, but the spec's own implementation note (cli.md:174) records that the current impl logs via `info!()` (only visible with `RUST_LOG=info`). Confirmed still divergent. Already a documented Known Divergence — not re-logged.
- ✅ `smelt diff` offline: exits 1 when `.smelt/schemas/` is absent (all models "new"), exits 0 when no changes — matches §"smelt diff — offline operation" and Exit-codes table.

### Invariant drift
- ✅ Constraint #2 (`--help`/`--version` need no project) — upheld.
- ✅ Constraint #7 (`--dry-run` absent on build) — upheld.
- ✅ Constraint #8 (`--show-plan` requires positional) — upheld.
- ✅ Constraint #11 (argument resolution uses the same resolver as model SQL; dep graph keyed by canonical paths) — **now** upheld for seeds after BUG-005 fix; previously the CLI seed validator diverged from the Salsa/LSP resolver (asymmetric discovery).
- ⚠️ Constraint #6 (exit codes meaningful) — `0`/`1` are consistent; "config/user error" code is `1` for missing-project and `2` for clap arg errors. Matches the existing Known Divergence "Exit code standardization incomplete". Not re-logged.

### Timeless-oracle check
- ✅ No `Phase [A-Z0-9]` leakage in the spec body outside §Known Divergences/§References. (The §Known Divergences `origin`-field paragraph and `default_type_lookup` Phase refs are paired with plan links / in tolerated sections.)
  - Note: `crates/smelt-cli/src/logical_graph.rs` comments still carry historical "Phase 2"/"Phase 6" labels — these are *code* comments, not spec/user-doc body, so out of scope for the timeless-oracle rule. Left as-is.

### Freshness
- last_reviewed: 2026-05-27
- most recent code change to a Reference→Code path: 2026-05-30 (this fix to `logical_graph.rs`; the `line_index` diagnostics boundary landed 2026-05-29/30).
- Verdict: **fresh** — the spec body still accurately describes behavior; the one behavioral gap (BUG-005) was a code bug, not spec drift, and is fixed.

### Summary
- Drift items: 1 surface (BUG-005, fixed in-loop). 0 new semantics/invariant drift. 2 pre-existing Known Divergences re-confirmed (no-op message via `info!`; exit-code standardization) — not re-logged.
- Recommended next step: none for the spec. BUG-005 is fixed with red-green coverage (`logical_graph.rs` unit test + `tests/subdir_seed_resolution.rs` integration test).
