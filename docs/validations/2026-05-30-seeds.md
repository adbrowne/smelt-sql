## Drift Report: seeds

**Spec**: docs/specs/seeds.md (last_reviewed: 2026-05-05)
**Date**: 2026-05-31
**Phase**: C5 (feature sweep)

### Automated checks
- cargo fmt — PASS (after `cargo fmt --all`)
- cargo clippy --all-targets — PASS (zero warnings)
- cargo test — PASS (full suite green; clean pre-flight baseline)
- example_diagnostics — PASS
- seed gates (`seed_inference`, `seed_yaml_validation`, `seed_loading`) — PASS

### Surface drift
- ✅ Type-inference precedence `BOOLEAN → DATE → TIMESTAMP → INTEGER → DECIMAL → DOUBLE → VARCHAR` — `infer.rs:59-108`. Verified end-to-end against DuckDB on an 11-column adversarial CSV: BOOLEAN, DATE, TIMESTAMP, T-separated→VARCHAR, tz-suffix→VARCHAR, BIGINT(integer), DECIMAL(3,2), >18-digit→DOUBLE, scientific→DOUBLE, VARCHAR, all-empty→VARCHAR. **All correct.**
- ✅ DECIMAL(18,4) cap → DOUBLE fall-through (`infer.rs:178-180`); scientific notation → DOUBLE; TIMESTAMP space-separator requirement; tz-suffix → VARCHAR; all-empty column → VARCHAR.
- ✅ Compile-time samples first 100 rows / runtime whole-file, same code path (`mod.rs:183` `Some(100)`, `seed.rs:227` `None`) — Invariant 1 upheld by construction.
- ✅ CSV strictness: comma, double-quote, mandatory header, UTF-8, BOM consumed, empty cell → NULL (uniform incl. VARCHAR), CRLF/LF — `csv.rs:54-70,113-120`.
- ✅ Materialization `table`/`ephemeral`; `view`/`materialized_view` → hard error at load (`validate.rs:73-85`, `seed.rs:152-171`). Verified: `materialization: view` → exit 1.
- ✅ Ephemeral seed → VALUES CTE rewrite at compile time; `smelt seed` skips it. Verified end-to-end: model referencing an ephemeral seed materialised with the seed data, no seed table created, `smelt seed` loaded 0.
- ✅ `name:` forbidden on seed sidecar (Invariant 8) — `sidecar.rs:137-140`, `SidecarError::NameKeyForbidden`. **BUT** see BUG-027: the CLI swallowed this error (now fixed).
- ✅ Column-set agreement when pinned (`validate.rs:92-101`); `nullable: false` load-time check (`validate.rs:120-130`). Verified: both hard-error at `smelt seed`.
- ✅ LSP missing-sidecar warning + "Pin schema" code action (`smelt-db/lib.rs:849`, `smelt-lsp/backend.rs:2324-2363`).
- ❌ `smelt seed --select <source-path>` — spec says hard error "not a seed"; observed silent exit 0. **BUG-028** (needs-review).

### Semantics drift
- ✅ #1 schema-set agreement (test `column_set_exact_match_passes`); #2 hard coercion; #3 `nullable:false` (test `nullable_false_null_fails`); #4 idempotence (re-load replaces); #6 empty=NULL; #7 ephemeral desugar (verified e2e); #8 deterministic discovery order (`seed.rs:135` sort).
- ❌ #5 compile/runtime inference-divergence diagnostic — **unimplemented** and conflicts with the spec's own §Surface "v1 sharp edge" note. **BUG-029** (needs-review, spec-conflict).

### Invariant drift
- ✅ #1 single inference code path (different sample sizes) — verified.
- ✅ #2–#8 — CSV strictness, inferred type set, column-set match, hard coercion, `load_table` ingest, ephemeral never loaded, `name:` forbidden — all upheld in code.
- ⚠️ #1 wording ("cannot diverge by construction") sits in tension with Semantics 5's divergence diagnostic (the two phases run at different times over different row sets). Noted in BUG-029.

### Additional findings (code inspection / fixtures)
- ❌ **BUG-027 (fixed)** — `read_sidecar_from_path` swallowed all sidecar parse errors via `parse_sidecar(...).ok()`; a malformed/forbidden sidecar loaded silently with full inference. Fixed red-green (`seed.rs:56-63,121`).
- ⚠️ **BUG-030 (needs-review)** — unknown `materialization:` value silently coerced to `table` (`sidecar.rs:147-154`).
- 📄 **BUG-031 (deferred docs-gap)** — calendar-invalid-but-shape-valid date (`2025-02-30`) infers DATE then hard-fails at load; spec-consistent, undocumented interaction.

### Timeless-oracle drift
- ✅ No phase-vocabulary leakage in the spec body. `grep -nE "Phase [A-Z0-9]+" docs/specs/seeds.md` matches only §References ("as of Phase 4", "Phase 6", "Phase 7", "Phase 3") and §Known Divergences — all tolerated (References/plan-history context). User docs (`docs-site/docs/guide/seeds.md`) — no phase vocabulary.

### Freshness
- last_reviewed: 2026-05-05
- Seed code (`crates/smelt-core/src/seeds/`, `crates/smelt-cli/src/seed.rs`) modified this phase (BUG-027 fix).
- Verdict: spec is **substantively fresh** — the Surface/Semantics match the implementation. The open items are BUG-028 (a CLI gate the spec mandates but code omits), BUG-029 (a spec-internal conflict to resolve), and BUG-030 (a tightening decision). Recommend `/smelt:spec seeds` only to resolve BUG-029's Semantics-5-vs-sharp-edge tension during the human pass.

### Summary
- Drift items: 5 (2 surface — BUG-028, BUG-027(fixed); 1 semantics — BUG-029; 1 sidecar-validation — BUG-030; 1 docs — BUG-031).
- The seeds feature is mature: type inference, CSV parsing, ephemeral expansion, sidecar validation, and LSP affordances all match the spec. The one clear code bug (BUG-027, swallowed sidecar errors) is fixed; the rest are batched needs-review/deferred.
- Recommended next step: post-sweep human review of BUG-028 (implement the "not a seed" guard), BUG-029 (align Semantics 5 / sharp-edge note), BUG-030 (reject unknown materialization).
