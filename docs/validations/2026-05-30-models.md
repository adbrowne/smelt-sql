## Drift Report: models

**Spec**: docs/specs/models.md (last_reviewed: 2026-05-21)
**Date**: 2026-05-31
**Phase**: C3 (feature-sweep)

### Automated checks
- cargo fmt — PASS (after `cargo fmt --all`)
- cargo clippy — PASS (zero warnings on `smelt-core`, `smelt-cli`)
- cargo test — PASS (`smelt-core` + `smelt-cli` libs green; full suite green at pre-flight)
- example_diagnostics — PASS (87 passed, 1 ignored)

### Surface drift
- ✅ **File format (single/multi/generator)** — `extract_file_metadata` (`metadata.rs:403`) dispatches single / multi / generator correctly; section-delimiter form `--- name: <name> ---` enforced (`parse_section_delimiter:689`); any other `--- X ---` is `MalformedDelimiter`.
- ❌ **Model naming (single-model)** — spec: "file stem is **always** authoritative; `name:` … has no effect on identity". Code (before fix) honoured frontmatter `name:` over the file stem in `smelt-core::parse_sql_file:325` and `smelt-cli::discovery:301`, while smelt-db's `parse_model:51` used the file stem → LSP↔CLI asymmetry. **Fixed this phase** (BUG-020): both discovery copies now key on `file_stem()`.
- ✅ **YAML frontmatter keys** — all spec keys present on `ModelMetadata` (`metadata.rs:116`), `deny_unknown_fields` active. `generates:` (generator files) is an extra key admitted by the parser; it is specced in `meta_*`/expansion specs, not in models.md's key table — acceptable (cross-spec surface).
- ✅ **Materialization modes (6)** — `Materialization` enum has `view`/`table`/`ephemeral`/`materialized_view`/`test`/`cumulative_aggregate` (`config.rs:43`).
- ✅ **Constraint-violations table** — every row enforced: `ephemeral`+`incremental`/`target` (`config.rs:655/661`), `test`+`incremental`/`target` (`:689/695`), `incremental` without `timeseries` (`validate_timeseries:344`), `ephemeral|test`+`timeseries` (`:356/362`), `cumulative_aggregate`+`timeseries`/`incremental` (`:335/325` + `config.rs:707`), `view|materialized_view`+`incremental.enabled` warnings (`config.rs:671/681`), unknown key → `deny_unknown_fields`.
- ⚠️ **`test` mode missing from docs** — `docs-site/docs/guide/materializations.md` documents 5 of 6 modes (no `test`). Matches Known Divergence #1. Logged BUG-022 (docs-gap, deferred).
- ✅ **`columns:` metadata** — `ColumnMetadata` (`metadata.rs:87`) carries `description`/`tests`/`data_latency`/`default`/`backfill`; no `type:` key (matches spec).
- ✅ **Reference syntax / named params parsed-not-executed** — refs extracted via `extract_refs`; named-arg call syntax parsed (Known Divergence #4, pre-execution).

### Semantics drift
- ✅ **Ephemeral inlining / transitive chains** — covered by ephemeral demo + runtime tests (not re-probed this phase; mature).
- ✅ **Tag merging (union, smelt.yml-first, dedup, case-sensitive)** — `Config::get_tags` (`config.rs:504`), unit-tested.
- ✅ **Materialization precedence** — frontmatter > `models.<name>` > `default_materialization` (`config.rs:622`).
- ❌ **Model discovery uniqueness (Constraint 4 / §Semantics)** — "Model names must be unique within a project … the discovery pass must not yield two `ModelFile` entries with the same model_name". Unenforced: within-file duplicate `--- name: dup ---` sections silently collapse (last wins); cross-file bare-name duplicates survive (but are disambiguated by canonical path, so functionally benign). Internal spec tension (§Semantics "undefined / last writer wins" vs §Constraints "must not yield"). Logged BUG-021 (needs-review). Matches Known Divergence (line 209).
- ✅ **Materialization change (drop + recreate)** — runtime behaviour, not re-probed (mature).

### Invariant drift
- ✅ **1. Pure SQL models** — upheld (no Jinja path; time filters injected at execution).
- ✅ **2. Ephemeral → no DDL** / **3. Test → no DB object** — upheld by materialization handling.
- ❌ **4. Model names unique within a project** — violated (see Semantics; BUG-021, needs-review).
- ✅ **5. Unknown keys rejected** — `deny_unknown_fields`.
- ✅ **6. Tags additive** — `get_tags` unions, never removes.

### Timeless-oracle drift
- ✅ No `Phase [A-Z0-9]` vocabulary in `docs/specs/models.md` body or in `docs-site/docs/guide/{sql-models,materializations}.md`.

### Freshness
- last_reviewed: 2026-05-21
- most recent code change to reference paths (`metadata.rs`/`config.rs`/`discovery.rs`/`model_id.rs`): 2026-05-28
- Verdict: **mildly stale** — Known Divergence #3 mischaracterises the live single-model `name:` behaviour as "ignored / silently dropped" when the code actually *honoured* it for identity (BUG-020); and the cross-file duplicate example (Known Divergence line 209) is stale under canonical-path addressing (BUG-021). Recommend `/smelt:spec models` to (a) update Known Divergence #3 now that BUG-020 is fixed, and (b) re-scope Constraint 4 uniqueness to canonical path.

### Summary
- Drift items: 3 (1 surface — fixed BUG-020; 1 semantics/invariant — BUG-021 needs-review; 1 docs — BUG-022 deferred).
- Recommended next step: `/smelt:spec models` for the two stale Known Divergences (post-sweep human pass); BUG-021's run-pipeline-gating half folds into the BUG-006 family decision.
