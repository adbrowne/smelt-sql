## Drift Report: lsp

**Spec**: docs/specs/lsp.md (last_reviewed: 2026-05-16)
**Date**: 2026-06-05

### Automated checks
- cargo fmt — PASS
- cargo clippy — PASS
- cargo test — PASS
- example_diagnostics — PASS (94/95, 1 ignored)
- example_workspaces — PASS (28/28)
- smelt-lsp unit tests — PASS (11 + 12)

### Surface drift

**Capabilities (backend.rs:1157–1179):**
- ✅ Diagnostics (push) — confirmed
- ✅ Go-to-Definition (`definition_provider: Some(OneOf::Left(true))`)
- ✅ Find References (`references_provider: Some(OneOf::Left(true))`)
- ✅ Hover (`hover_provider: Some(HoverProviderCapability::Simple(true))`)
- ✅ Completions — confirmed, trigger chars `'`, `(`, `.`
- ✅ Rename + PrepareRename (`rename_provider: Some(OneOf::Right(RenameOptions { prepare_provider: Some(true) }))`)
- ✅ Code Actions (`code_action_provider: Some(CodeActionProviderCapability::Simple(true))`)
- ✅ TextDocumentSyncKind::FULL — confirmed

**VSCode activation:**
- ❌ **Spec says**: "The VS Code extension auto-activates when a `smelt.yml` is found in the workspace." (`docs/specs/lsp.md`, §"Editor setup")
- **Code does**: `activationEvents: ["workspaceContains:**/models/**/*.sql", "onLanguage:smelt"]` (`editors/vscode/package.json:25–28`)
- **Drift**: spec says `smelt.yml`, code triggers on `models/**/*.sql`. Code behavior is correct (activates when model files exist).

**Watched files:**
- ❌ **Spec says**: "The server watches for changes to `**/*.py` files (Python model files)" (`docs/specs/lsp.md`, §"Watched files")
- **Code does**: watches `**/models/**/*.py` AND `**/functions/**/*.sql` (`backend.rs:1209–1216`)
- **Drift**: spec omits `**/functions/**/*.sql` watcher, and overstates Python glob (spec says `**/*.py`, code restricts to `**/models/**/*.py`).

**Diagnostic codes — undocumented codes:**
- ❌ **`UnknownSmeltFn`** not in spec. Spec lists `UnrecognizedFunction` as "call to undefined smelt function" but that Rust variant is for unrecognized SQL built-in functions (check_types.rs:74). `UnknownSmeltFn` is the code actually used for unknown `smelt.functions.*` calls — it should be listed in the spec's Functions section. Both codes exist in `diagnostics_types.rs:24,55` and are mapped in `backend.rs:238,244`.
- ❌ **`CteShadowsCallerCte`** not in spec, not in Known Divergences. Added in codegen-soundness plan (Phase C3). Mapped in `backend.rs:254` as `"cte-shadows-caller-cte"`. Should be listed under **Functions** or **References** alongside `CteCycle`.
- ❌ **~70 undocumented codes** beyond the 6 listed in Known Divergences. The spec's Known Divergences acknowledges `FragmentColumnMissing`, `AnnotationTooWide`, `FragmentKindMismatch`, `DeclaredCardinalityUnverifiable`, `ExternFragmentParamUnsupported`, `MissingSeedSidecar`, but the code additionally has the entire meta-language diagnostic set (Phase A-F: ~35 codes), record/map/loader codes (~30 codes), timeseries codes (2), types codes (2), python codes (1), cumulative/incremental codes (9). All mapped in `backend.rs:273–384`.

### Semantics drift

- ✅ Diagnostics pushed on every `textDocument/didChange` — `did_change` handler calls `self.publish_diagnostics` on all tracked files in the project
- ✅ Salsa incremental computation — all analysis delegated to `smelt_db::file_diagnostics`
- ✅ Full text sync — `TextDocumentSyncKind::FULL` confirmed
- ✅ Go-to-definition returns `GotoDefinitionResponse::Array` for ambiguous columns (backend.rs:1987–1990)
- ✅ Rename does not rename SQL files (Constraint #2) — rename handler `backend.rs:2688` only emits `WorkspaceEdit` text edits, not file operations
- ✅ Find-references scoped to project (backend.rs:2127 comment: "smelt projects, and references do not cross project boundaries")
- ✅ Cross-file column rename traverses upstream/downstream (backend.rs rename handler) — confirmed by `yaml_edits::find_source_column_yaml_rename` import and source YAML edit path in rename handler
- ✅ Python models as virtual SQL — `python_model_sources` map in Backend; goto-def navigates to `.py` file at decorator line

### Invariant drift

- ✅ **Constraint #1** (Diagnostics current with latest text) — confirmed; `did_change` handler immediately re-analyzes and republishes.
- ✅ **Constraint #2** (Rename doesn't rename files) — rename handler emits only text edits.
- ✅ **Constraint #3** (Ambiguous columns → array response) — confirmed at `backend.rs:1987–1990`.
- ✅ **Constraint #4** (Column completions include types) — `completion.rs` and `hover.rs` use `TypedColumn`/`format_smelt_type_hover`.
- ✅ **Constraint #5** (Code action generates SQL skeleton) — `code_actions::generate_all_code_actions` produces `CreateModel` action.
- ✅ **Constraint #6** (Workspace loading shared with CLI) — `backend.rs:985` calls `smelt_core::load_workspace`. Standing gate: `example_workspaces` — PASS.
- ✅ **Constraint #7** (Multiple smelt projects per workspace folder) — `find_smelt_projects` used at `backend.rs:966`; project-scoped find-references at `backend.rs:2127`.

### Timeless-oracle drift

- ✅ No `Phase [A-Z0-9]` matches in spec body outside Known Divergences.
- ✅ No `Phase [A-Z0-9]` matches in `docs-site/docs/guide/editor-features.md` or `editor-setup.md`.

### Freshness

- `last_reviewed`: 2026-05-16
- Most recent code change: 2026-06-05 (`fix: batch of ledger fixes (BUG-001/004/012/028/029/030/033/037/038)`)
- Verdict: **stale** — many diagnostic codes added since last review (meta-language Phase A–F, E1 record/map/loader, codegen-soundness `CteShadowsCallerCte`); VS Code activation event changed.

### Summary

- Drift items: 5 total — 3 surface (VSCode activation, watched files, `UnknownSmeltFn`/`CteShadowsCallerCte`), 0 semantics, 0 invariants, 0 timeless-oracle
- All drift is docs-gap (code correct, spec outdated)
- One-line spec fixes applied for VSCode activation, watched files, `UnknownSmeltFn`, `CteShadowsCallerCte`
- Large undocumented code set (meta-language + feature codes): deferred to `diagnostics.md` (spec's own Known Divergences tracking item)
- Recommended next step: none for code; update `last_reviewed` in spec after fixes.
