# Phase 5 claim inventory

Source: `docs/specs/incremental_models.md` at HEAD before phase 5, in-scope ranges only
(`## Overview` 16–140, `:156` polemic sentence, `## Design` 1619–1881, `## Constraints &
Invariants` 1882–2019, `## Limitations` 2020–2094, `## Future Extensions` 2435–2481,
`## References` 2482–2626). One row per normative statement, rejected alternative, research
citation, gate/env-var name, or boundary. `status` is filled in by the adversarial-verify pass.

| id | claim | source lines | status |
|---|---|---|---|
| OV-1 | The equivalence invariant: stored table after any incremental runs == full refresh over processed inputs `S` | 20–30 | preserved |
| OV-2 | A shortcut unprovable safe is refused with a diagnostic, never approximated or silently downgraded | 32 | preserved |
| OV-3 | Order doesn't matter — convergence depends only on set `S` | 34 | preserved |
| OV-4 | Freshness is the only degree of freedom smelt chooses | 35 | preserved |
| OV-5 | Modeller declares `refresh: incremental` + at most two shape facts: clock (`timeseries:`) and identity (`unique_key:`) | 39–43 | preserved |
| OV-6 | Everything beyond the two facts (technique, write addressing, scan, bookkeeping) is derived, printed by `smelt explain` | 44 | preserved |
| OV-7 | Machinery validates the declaration and refuses when SQL can't uphold it; never chooses a different shape | 44 | preserved |
| OV-8 | Four corners table: no-identity+clock → `grain: partition`; no-identity+no-clock → no maintainable shape; identity+clock → `grain: key` (partition∉key) or `grain: key_per_partition` (partition∈key); identity+no-clock → `grain: key` lookup | 46–53 | preserved |
| OV-9 | `grain` is a derived label, writable in frontmatter only as a checked assertion | 48 | preserved |
| OV-10 | Three worked SQL sketches, one per inhabited corner (partition, key, composed key+time) | 55–76 | preserved |
| OV-11 | Corners are not chosen modes — they are implied by declared facts and compose | 78 | preserved |
| OV-12 | Declaring `unique_key` on a clocked table unlocks keyed maintenance without disabling partition rewrite on new-data arrival | 78 | preserved |
| OV-13 | Some capabilities exist only in the composed corner (cites §"Key temporal locality") | 78 | preserved |
| OV-14 | Maintenance plan = set of cells, one per (output column group, trigger, changed input) | 82–86 | preserved |
| OV-15 | Trigger kinds: creation, mutation, definition change, backfill | 85 | preserved |
| OV-16 | Each cell records technique, write addressing (region vs key), and scan clamps | 87 | preserved |
| OV-17 | Different cells of one model derive different answers; one model is simultaneously append/merge/recompute-driven — no single per-model strategy label | 88 | preserved |
| OV-18 | `smelt explain <model>` prints the plan; none of it is declared | 88 | preserved |
| OV-19 | Graph layer lifts the plan to the DAG: forward propagation and backward resolution | 90 | preserved |
| OV-20 | Equivalence invariant fixes what the table must equal, not how much work a run does | 94 | preserved |
| OV-21 | Cost decomposes into read/compute/write, correlated but not tracking each other, each with own governing machinery | 94 | preserved |
| OV-22 | Read cost — delta discovery ladder: change feed cheapest, clock window-forward discovery next (scan clamps), no clock forces full snapshot diff (surfaced, guarded via `scan_bounds`) | 96–98 | weakened (low-value: `scan_bounds` name dropped from Overview, survives 6x elsewhere) |
| OV-23 | Read cost — repair-input ladder: fold (delta+state, needs combiner algebra) vs recompute (full region re-read, no algebra); neither dominates, cost-modelled via `smelt bakeoff` | 99 | preserved |
| OV-24 | Compute cost — scanned volume is only a proxy; joins/aggregations/sorts (shuffle on distributed engines) can dominate | 101 | preserved |
| OV-25 | smelt does not hand-compute minimal deltas — engine evaluates full SQL over a widened scan, join optimisation stays with the optimiser | 101 | preserved |
| OV-26 | smelt controls unit of work — region-scoped repair caps working set/shuffle even when a whole-table MERGE would be equivalent, because locality proofs buy bounded compute not just smaller writes | 101 | preserved |
| OV-27 | Write cost — three orthogonal derived properties per cell: row location, replacement granularity, locality | 103–107 | preserved |
| OV-28 | Row location: region predicate (needs clock), identity (needs unique_key), or any registered-pattern-provable predicate (open pattern set); pure append is the degenerate cheapest case | 105 | weakened (low-value: compressed to a section pointer in Overview; verbatim in untouched §Per-cell write addressing) |
| OV-29 | Replacement granularity: wholesale (DELETE+INSERT/swap, simple, contract-agnostic, rewrites unchanged rows) vs surgical (UPDATE/MERGE/column-scoped, needs identity+change-comparability+engine support) | 106 | weakened (high-value), restored |
| OV-30 | Locality: every verb is cheaper when touched rows cluster in few partitions; plan resolves delta to touched partitions first | 107 | preserved |
| OV-31 | Division of labour: declared facts gate which write mechanisms exist; proofs bound reads/writes/unit-of-work; among survivors, cost model / operator pin / `smelt bakeoff` decides — freshness only thing at stake | 109 | preserved |
| OV-32 | Running example: sources orders/order_events/raw_events/customers/customer_changes with their properties | 113–119 | preserved |
| OV-33 | Running example models table: daily_revenue (partition), order_lifecycle (bare key), order_facts (composed, per-cell-addressing example), event_dedupe (composed, locality example) | 121–128 | preserved |
| OV-34 | `customer_history` (SCD2) appears in §Limitations, deliberately not a maintained shape | 130 | preserved |
| OV-35 | Reading guide: Surface/Semantics/Design/Constraints/Limitations/Known Divergences pointers | 132–139 | preserved |
| SF-1 | The two declared facts (clock, identity) are orthogonal and compose; a model with both is first-class, not a corner case | 156 | preserved |
| SF-2 | Both axes are orthogonal to input consumption: a bare keyed model over a clocked source still consumes window-forward; a composed model's output clock is a property of its own stored shape | 156 | preserved |
| SF-3 (deleted) | "Text anywhere in this corpus that treats … is wrong and is corrected against this section" — polemic, deletion is the phase's own spec-delta item 1 | 156 | n/a |
| DS-1 | Strategy is derived per cell; deriving shape too was rejected (reintroduces silent contract swap) | 1624–1630 | preserved |
| DS-2 | Cited: `docs/research/20260705-refresh-as-maintenance-plan/01-framework.md` §10, §13 | 1630 | preserved |
| DS-3 | One invariant; addressing is the real axis and is per-cell, not per-model; a declared model-wide addressing token was rejected | 1632–1641 | preserved |
| DS-4 | Earlier per-partition/end-state split miscast: per-partition equivalence is a strengthening not a peer | 1632–1635 | preserved |
| DS-5 | Composed shape proves addressing is intrinsic to the write (dimension-correction cell keyed, creation cell region-addressed on same model) | 1638–1639 | preserved |
| DS-6 | Cited: `docs/research/20260716-relation-contract-and-per-cell-addressing.md` | 1642 | preserved |
| DS-7 | Decomposed rung-2 state lives in the presented table (reserved `__` suffix columns), not a second `<model>__state` relation+view; rejected because it splits `ref()` resolution and adds a second DDL/swap path for no benefit (presentation map π is a per-row pure function) | 1644–1651 | preserved |
| DS-8 | Two write mechanisms (region-overwrite, keyed-merge) stay binary per cell; concrete pattern drawn from open registry; locality is a refinement not a third addressing pole (rejected: would suggest a different write primitive/identity requirement, misplaces a derived fact as a shape property) | 1653–1660 | preserved |
| DS-9 | Cited: `docs/research/20260705-keyed-time-superset.md` | 1661 | preserved |
| DS-10 | The two axes compose; the composed shape is deliberately first-class (retitled, non-combative form retained; catalogue of past mistakes + reviewer-defect language deleted per spec delta item 2) | 1663–1668 | preserved |
| DS-11 | Scope maps name per-input dispatch so "what runs when this input changes" is explainable and gives future multi-clock driving-source work a stable home | 1670–1675 | preserved |
| DS-12 | Cited: `docs/research/20260705-keyed-time-superset.md` §5 | 1675 | preserved |
| DS-13 | Factoring by mutation-sensitivity not syntactic provenance — an immutable-at-creation read must not inherit the source's mutation-sensitivity, else targeted cells are lost; makes append-only source declaration load-bearing | 1677–1681 | preserved |
| DS-14 | Cited: `docs/research/20260705-refresh-as-maintenance-plan/01-framework.md` §5 | 1681 | preserved |
| DS-15 | Membership sensitivity is derived, never inferred from collapse — a mutable source read only in row-admission position needs its own derived kind so admission uses the actual join-predicate read and forces the membership-capable recompute family; collapse-admitted cells assign repair by accident | 1683–1694 | preserved |
| DS-16 | Empirical grounding: keyed-enriched shape's dimension cell was admitted solely by a collector misparse until membership sensitivity became first-class | 1692–1694 | preserved |
| DS-17 | Per-edge dirt keys trigger cells — a per-model merged dirty set would erase which repair runs where | 1696–1699 | preserved |
| DS-18 | Cited: `docs/research/20260705-refresh-as-maintenance-plan/10-dependency-propagation.md` §3 | 1699 | preserved |
| DS-19 | Widen-never-narrow — every approximation widens; widening costs compute, narrowing costs correctness silently; `scan_bounds` makes widenings visible, refused by default when unbounded | 1701–1705 | preserved |
| DS-20 | Granularity is declared not derived — deriving from `date_trunc` would let a refactor silently change scheduling semantics; declaration is checked against the derived partition grid | 1707–1710 | preserved |
| DS-21 | The clamp runs both directions — forward reflection and backward resolution are one edge object; keeps test-build (backward) consistent with scheduling (forward); adjointness `forward(backward(P)) ⊇ P` | 1712–1716 | preserved |
| DS-22 | Cited: `docs/research/20260705-refresh-as-maintenance-plan/10-dependency-propagation.md` §2 | 1717 | preserved |
| DS-23 | Offline cost measurement is first-class — `smelt bakeoff` measures real candidates via the actual `execute_project` pipeline over project data in a disposable scratch schema; `--pin` only emits YAML for review, applied pin still subject to admission | 1719–1725 | preserved |
| DS-24 | Cited: `docs/research/20260705-refresh-as-maintenance-plan/01-framework.md` §11 | 1726 | preserved |
| DS-25 | Windowed by default; full scan is the surfaced fallback — a clocked model can always be bounded-scanned, only absence of clock forces wider read | 1728–1733 | preserved |
| DS-26 | The horizon is derived not declared — a declared horizon risks under-estimate corrupting the clamp; declaration admitted only as a warning ceiling; consequence (silent exclusion beyond derived reach) accepted and documented; softenable later | 1735–1742 | preserved |
| DS-27 | Validator, never chooser — auto-selecting/downgrading declared shape rejected as reproducing dbt's `strategy:` footgun | 1744–1746 | preserved |
| DS-28 | Placement is definitional not consumer-counted — capability statable without naming a profile lives in a capability spec; profile-only capability lives in that profile's section; every capability gets exactly one home | 1748–1755 | preserved |
| DS-29 | Rejected alternatives list: `strategy:` sub-knob, dedicated `smelt-maintenance` crate, qualifying output clamp to resolved inner alias, third addressing pole for locality, per-edge grain declarations, closed write-pattern enum baked into surface | 1757–1763 | preserved |
| DS-30 | Cited: `docs/research/20260705-refresh-as-maintenance-plan/` parts 01–10, decision records in `09-spec-readiness.md` §1 and `10-dependency-propagation.md` §11 | 1763–1765 | preserved |
| DP-1 | Logical SQL is pure; framework injects the time filter — no `is_incremental()`/conditional branching (rejected: splits one model into two implicit ones that drift); policed by batch-safety analysis | 1769–1774 | preserved |
| DP-2 | DELETE+INSERT over partition columns, not MERGE, is the default — MERGE rejected as default (needs `unique_key`, cross-engine subtleties) but stays in the strategy enum for opt-in backends; DELETE+INSERT is idempotent under fixed input | 1776–1779 | preserved |
| DP-3 | Three-class batch-safety taxonomy — binary flag rejected (too many bounded-safe workloads need auto-chunking); continuous score rejected (decision is qualitative, maps to three backend execution shapes) | 1781–1783 | preserved |
| DP-4 | Lookback derived from SQL, not frontmatter — `lookback_days:` rejected (drift risk); trade-off is a model with implicit time logic refuses eligibility; derived clamp made observable as counterpart | 1785–1789 | preserved |
| DP-5 | Cited: `docs/research/20260521-incremental-as-planner-rule.md` | 1790 | preserved |
| DP-6 | smelt does not own state, scoped to partition grain — owning a watermark store rejected (duplicates engine state, sync-correctness window); key grain's transactional merge ledger is the one deliberate exception (backend-resident, same-transaction write); consequence: partition-grain `Append` strategy unreachable until gated on ledger-verified unwritten windows | 1792–1798 | preserved |
| DP-7 | Cited: `docs/research/20260705-keyed-collapse-application.md` D7 | 1799 | preserved |
| DP-8 | Non-determinism opted in per column (`columns.<c>.contract: plausible`) — the one place derive-don't-declare yields, because acceptable-to-vary is an author value judgement; whole-model `allow_nondeterministic` boolean rejected (drops guardrail); per-column opt-in still proves via taint flow that tolerance didn't leak | 1801–1806 | preserved |
| DP-9 | Cited: `docs/research/20260703-model-updates.md` §9.2 | 1807 | preserved |
| DK-1 | One shape; column family is the pattern — running-aggregate/latest-value/milestone share shape/invariant/transform/key derivation, differ only in per-column combiner algebra; every consequence is derivable from SQL; litmus rule (`models.md` §Design) forbids multiplying the refresh enum; splitting into peer modes also rejected because combiner intent is per-column not per-model | 1811–1819 | preserved |
| DK-2 | Cited: `docs/research/20260705-unified-keyed-refresh.md`; `docs/research/20260705-keyed-collapse-application.md` | 1820–1821 | preserved |
| DK-3 | The SQL is the oracle — body must be the aggregation itself so `full_refresh(model SQL)` is executable; bare-projection + mode-imposed dedup rejected (no executable oracle, mode adds semantics SQL doesn't carry); plain-overwrite (`ANY_VALUE`) family exists to give snapshot posture an honest aggregated spelling | 1823–1828 | preserved |
| DK-4 | Cited: `docs/research/20260705-model-refresh-review.md` §1.1 | 1829 | preserved |
| DK-5 | Derive `unique_key` and combiners from SQL, not frontmatter — GROUP BY names the key, each projection names its aggregator, combiner is a fixed lookup; a config block restating them re-introduces drift | 1831–1833 | preserved |
| DK-6 | Cited: `docs/research/20260521-incremental-as-planner-rule.md` | 1834 | preserved |
| DK-7 | No write-eligibility clamp — a horizon-clamped merge rejected (silently drops scanned inputs, the one silent-data-loss point; not needed since merge work is proportional to delta size); settled-key GC/work bound deferred, must arrive with late-fact accounting; slice pruning under locality is not such a clamp | 1836–1843 | preserved |
| DK-8 | Cited: `docs/research/20260705-keyed-collapse-application.md` D6 | 1843 | preserved |
| DK-9 | Composed (key, time) output is locality-gated, not a new mode — absorbs event-grain dedupe/per-(key,period) aggregates/clock-sink problem; peer mode rejected (shares invariant/oracle/driver/ledger/column families, differs by one derived/declared fact, earning a gate not a peer per litmus rule); gate exists because without locality merge target is whole key space and output clock would promise unrespected partition structure; declared route is runtime-checked to prevent re-importing the silent truncation the no-clamp rule prevents | 1845–1854 | preserved |
| DK-10 | Cited: `docs/research/20260705-keyed-time-superset.md`; `docs/research/20260705-model-refresh-review.md` §3.2 | 1855–1856 | preserved |
| DK-11 | Observer semantics refused, not smuggled — folding state observations into MIN/MAX/once-write columns yields values no full refresh can reproduce; refused cells name the observer contract as the future opt-in path (§Future Extensions) | 1858–1862 | preserved |
| DK-12 | Ties: honest boundary not fake proof — incumbent-wins + mandatory sequential execution makes overwrite columns deterministic-given-history without claiming unprovable order-independence; last-processed combiner (no ordering column) rejected outright; plain-overwrite family serves that need | 1864–1868 | preserved |
| DK-13 | No `safety_overrides:` for key grain — partition grain offers overrides for partial-correctness waivers; every keyed rejection guards the equivalence invariant itself (bypass would produce order-dependent/double-counted state); escape is remodel or `refresh: materialized_view` | 1870–1874 | preserved |
| DK-14 | One windowed executor, shared — window-forward loop is the shared driver parameterised by (classifier, merge-SQL builder); per-pattern copies rejected (four-way drift risk); every consumer inherits driver's granularity support (§Known Divergences) | 1876–1880 | preserved |
| CO-1 | Equivalence invariant holds for every non-`full` model, every ladder rung; refused not approximated when unprovable; order/set-determinacy is its corollary; per-partition equivalence is a strengthening not separate contract | 1886–1889 | preserved |
| CO-2 | Write addressing is per-cell not per-model, derived by `available = declared facts × trigger/changed-input needs × equivalence invariant × backend capability` over the open write-pattern registry; a keyed write on clocked output stays partition-scoped unless provably impossible; locality refines keyed addressing with a derived slice bound without changing the corner | 1890–1895 | preserved |
| CO-3 | Write-pattern set is an open registry not a closed enum; new patterns admitted by declaring required facts + discharging equivalence proof; `write:` pin is open, fail-loud; unexecutable pattern is not a candidate; stable contract is admission function + equivalence gate, never enumeration | 1896–1900 | preserved |
| CO-4 | Windowed by default where clocked; full scan is surfaced fallback never silent baseline; `scan window ⊇ write window` always | 1901–1902 | preserved |
| CO-5 | Horizon is derived; declared `horizon_ceiling` is warning threshold only, never relaxes clamp; late arrivals beyond derived reach silently excluded by default unless frozen-horizon lattice point | 1903–1906 | preserved |
| CO-6 | A contract-lattice point is admissible only as a complete triple (declaration schema, pure oracle transform, probe emitter), single-owned in `smelt-logical`; conformance gate consumes the oracle transform; mirrors statement-emission single-owner rule | 1907–1911 | preserved |
| CO-7 | One home per capability/rule — invariant/ladder/plan/graph layer here; properties in `model_properties.md`, transforms in `model_transforms.md`, declaration law + litmus rule in `models.md` | 1912–1914 | preserved |
| CO-8 | Proofs are fail-closed — undecidable construct rejects; declared escape hatch may only widen eligibility, never substitute for a proof's default reject | 1915–1916 | preserved |
| CO-9 | Declared `refresh:` value + shape facts are the only shape surface; `grain` is derived check-only; write addressing derived per cell (steerable only via validated `write:` pin); input-consumption derived from source; no `strategy:` sub-knob; machinery validates never chooses | 1917–1920 | preserved |
| CO-10 | Plan is pure data, derived by pure functions, in one place (`smelt-logical`); consumers never re-derive it (also an `architecture.md` invariant) | 1921–1922 | preserved |
| CO-11 | Maintenance statements have one author (§"Statement emission (single owner)"); backends execute never author; printed/gate-verified/executed SQL are the same emitters' output by construction | 1923–1925 | preserved |
| CO-12 | Never fold a delta already reflected in state — every fold consults the ledger, every region recompute resets entries it overwrote, no path merges a window twice | 1926–1927 | preserved |
| CO-13 | Write window = output window per cell — DELETE/merge target and output clamp range over the same output-axis column and window, by construction | 1928–1929 | preserved |
| CO-14 | Only proofs prune — a declared bound is admitted only checked; a guardrail (`scan_bounds`, `horizon_ceiling`) may refuse but never modifies a clamp; no unproven bound drops a scanned input | 1930–1932 | preserved |
| CO-15 | Fail-loud, fail-closed — every admission failure/non-local scan/skeleton-position add/unsupported graph node is a named diagnostic; graph layer never silently under-runs (unrepresentable dirt widens to whole-model) | 1933–1935 | preserved |
| CO-16 | Widen-never-narrow is the composition law of every interval operation (clamp ceiling, grain alignment, footprint reflection, backward widening) | 1936–1937 | preserved |
| CO-17 | Out of scope deliberately: content-aware delta pruning, file-level write-amplification minimisation, cross-project propagation | 1938–1940 | preserved |
| CP-1 | Logical model is pure SQL — no `is_incremental()`, no macros, no conditional branches; framework injects the time filter | 1944–1945 | preserved |
| CP-2 | `timeseries:` required for `grain: partition` — hard error at workspace load otherwise | 1946–1947 | preserved |
| CP-3 | Strategy not on the model — backend chooses physical strategy for the recompute corner's execution | 1948–1949 | preserved |
| CP-4 | smelt does not manage computational state (partition-grain doctrine); watermarks/offsets/run history live in backend; key grain's transactional merge ledger is the one deliberate exception; backend may select only a physical strategy preserving the declared shape's invariants; `Append` unreachable until gated on ledger-verified unwritten windows | 1950–1954 | preserved |
| CP-5 | Output-filter injection is per-model; source-filter pushdown is per-reference | 1955 | preserved |
| CP-6 | Per-partition equivalence with full refresh holds on all local deterministic columns; `contract: plausible` column need only be plausible; globally-dependent columns not equivalent | 1956–1958 | preserved |
| CP-7 | Idempotence under fixed input — re-running same run window on unchanged sources converges to same output state | 1959–1960 | preserved |
| CP-8 | Granularity closed under partition arithmetic — run windows align to whole granularity units; declared granularity must be ≥ derived partition grid (`g_run >= g_part`); violations reject never silently widen | 1961–1964 | preserved |
| CP-9 | Safety-check overrides are explicit — a `safety_overrides` entry names the specific check it bypasses; no global disable | 1965–1966 | preserved |
| CP-10 | No silent downgrade to full refresh — rejected/`NotDerivable` model refused at planning time with a diagnostic | 1967–1968 | preserved |
| CP-11 | `event_time_column` must be accessible at outermost SELECT unless every `UNION ALL` branch traces `Traceable`; else `EventTimeColumnNotVisibleAtOuterSelect` | 1969–1971 | preserved |
| CP-12 | Non-determinism stays in the payload — admitted only into `contract: plausible` columns (plus run-nondeterministic direct projections under compile-time pinning); never in `event_time_column`/`partition_column`/`unique_key` column/membership-or-grouping position; declaring an excluded column `plausible` is a configuration error | 1972–1975 | preserved |
| CK-1 | Opt-in is `refresh: incremental` + declared identity (storage implied `table`); `unique_key` required, must restate `GROUP BY`; no config block; `safety_overrides:` is a hard error | 1979–1981 | preserved |
| CK-2 | `timeseries:` block admitted iff key temporal locality established; else refused (`KeyedForbidsTimeseries`) | 1982–1983 | preserved |
| CK-3 | Body is aggregated `GROUP BY` query; every non-key projection classifies into exactly one column family; combiner is fixed lookup, never author-declared | 1984–1985 | preserved |
| CK-4 | Catalogue is closed and classifier fail-closed — unrecognised aggregators, composite expressions, unproven once-write columns, retractable contributions refused never approximated | 1986–1988 | preserved |
| CK-5 | End-state equivalence holds with model's own SQL as oracle, with exactly two named carve-outs: retained departed keys under snapshot-reconcile, ordering-key ties on overwrite columns | 1989–1991 | preserved |
| CK-6 | No write-eligibility clamp — run merges every scanned delta row, no silent drop; slice pruning under locality is no-op elimination or transactionally-checked declared bound, never a write clamp; future clamp/settled-key GC must ship with late-fact accounting | 1992–1995 | preserved |
| CK-7 | Run shape derived from driving source (clocked ⇒ window-forward; unclocked ⇒ snapshot-reconcile), surfaced by `smelt explain`, never declared | 1996–1997 | preserved |
| CK-8 | Admission matrix enforced per column — fold/once-write families require clocked (replayable) driving source; plain-overwrite requires snapshot posture | 1998–2000 | preserved |
| CK-9 | Window-forward models maintain the transactional merge ledger, written atomically per window's merge; additive-fold refuses ledgered window's re-run; re-run-tolerant may re-merge; snapshot-reconcile keeps no ledger | 2001–2003 | preserved |
| CK-10 | Ordering/parallelism follow derived postures — out-of-order/parallel/sliced backfill only for order-independent models; overwrite columns force sequential temporal order | 2004–2006 | preserved |
| CK-11 | Reprocessing changed input is refused for every family when detected; mitigation is `--full-refresh` or manual cascade rebuild | 2007–2008 | preserved |
| CK-12 | Exactly one clocked driving source under window-forward; zero selects snapshot-reconcile; two-or-more refused | 2009–2010 | preserved |
| CK-13 | Without admitted `timeseries:` block output has no `partition_column`, consumed as a lookup; with one, it is a clocked time-partitioned keyed table | 2011–2012 | preserved |
| CK-14 | Windowed step loop is the shared driver, never a per-pattern copy | 2013–2014 | preserved |
| CK-15 | Key temporal locality established only by the three named routes; derived routes prune by proof, declared route prunes only under transactional runtime check (`KeyedRecurrenceBoundViolated`); a violated declaration fails the run, never silently drops | 2015–2018 | preserved |
| LM-1 | No smelt-maintained SCD2 — no frontmatter opts a keyed model into version retention; SCD2 written as plain windowed SQL over a change stream (worked `customer_history` example) | 2027–2046 | preserved |
| LM-2 | Every version of a key is a row; validity interval closes at next change's event time; newest version open; no-op change events need dedupe (`LAG`-comparison filter) before `LEAD` | 2048–2051 | preserved |
| LM-3 | Two sanctioned routes to keep such a model current: `refresh: full` (always correct, full rescan cost) and `refresh: materialized_view` (engine-native IVM, cites `materialized_view.md` §Design "No named pattern") | 2053–2058 | preserved |
| LM-4 | No `refresh: incremental` route for SCD2 — `LEAD` inadmissible in every corner: key grain rejects window functions outright (`KeyedForbidsWindowFunctions`); partition grain would need to rewrite an already-written earlier partition, outside every output clamp; the succession-pattern recognition idea is sketched in §Future Extensions | 2060–2064 | preserved |
| LM-5 | No SCD2 over mutable snapshots — smelt never manufactures a change stream by diffing successive scans; a snapshot diff can only stamp boundaries with scan time, breaking replay-safety; SCD2 needs a source with real change events (update-events/CDC feed) | 2068–2073 | preserved |
| LM-6 | Maintaining current state from a snapshot (snapshot-reconcile, key grain) is in-bounds; retaining history of snapshot states is not; a snapshot-to-change-stream facility, if ever wanted, is a source-layer concern | 2074–2076 | preserved |
| LM-7 | Other deliberate boundaries collected: late arrivals beyond derived horizon excluded silently | 2082–2083 | preserved |
| LM-8 | No continuous freshness — smelt-owned maintenance is pull-based/per-run, correct as of last `smelt build`; continuous freshness is `materialized_view.md`'s different refresh mode | 2084–2086 | preserved |
| LM-9 | Non-replayable observation contracts refused — min-ever-observed/first-observed-value etc. have no executable full-refresh oracle, rejected not approximated; possible opt-in weaker contract is §Future Extensions | 2087–2090 | preserved |
| LM-10 | No delete signal under window-forward consumption — append-only feed without delete events can't express key deletion; departed keys persist (retention); deletion semantics beyond retention is an open question (§Known Divergences "The key grain") | 2091–2093 | preserved |
| FE-1 | Smelt-maintained SCD2 via succession-pattern recognition — walk verdict every window fn is `LEAD(t)` (or expr over it) partitioned by entity key ordered by driving source's event-time; maintenance theorem: new event touches only its own row + immediate predecessor (bounded footprint, late events included, mid-history splice touches exactly predecessor+successor); technique is keyed MERGE + targeted predecessor patch; SQL is its own oracle; generalises to any LEAD/LAG-over-clock-within-key model; open items: classifier grammar, fail-loud diagnostics, walk vocabulary; cites `docs/research/20260723-scd2-succession-pattern.md` | 2441–2454 | preserved |
| FE-2 | Row-local derivation for a changed column — added-column pure-function case already covered by `PureBackfill`; open extension is redefining an existing column's expression (falls to full recompute today); would need its own trigger, diagnostic, and ledger-composition decision | 2455–2464 | preserved |
| FE-3 | Automatic watermark-diffed `--since-upstream` — persists a per-source "last propagated through" watermark in `smelt-state`, diffed against `covered_intervals`; doesn't solve never-modeled raw source freshness; explicit and automatic forms compose, automatic computes the same `--landed` intervals | 2465–2472 | preserved |
| FE-4 | Observer / prefix-consistency contract for non-replayable combinations — admission matrix refuses folding observations because no executable oracle exists; future opt-in could state a weaker prefix-consistency equivalence explicitly; open: formal statement, opt-in surface, what a conformance oracle even is | 2473–2480 | preserved |
| RF-1 | Code paths for contract/plan/graph layer: `smelt-logical/src/maintenance/{mod,derive,emit}.rs`, `.../propagate.rs`, `smelt-runtime/src/propagation.rs` (`build_forward_graph`, `plan_since_upstream`, `resolve_build_plan`), `smelt-logical/src/analysis/`, `smelt-runtime/src/{cumulative,maintenance_driver,dimension_horizon_merge,transformer,backfill}.rs`, `smelt-state/src/intervals.rs`, `smelt-backend/src/lib.rs` | 2486–2495 | preserved |
| RF-2 | Pure derivation/graph-composition test files named: `maintenance_tracer{,_evolution,_propagation}`, `maintenance_propagation_adjoint` (dedicated home for `forward(backward(P)) ⊇ P`) | 2496–2499 | preserved |
| RF-3 | DuckDB-equivalence + real-workspace propagation test files named: `smelt-runtime/tests/{tracer_maintenance,tracer_evolution,tracer_propagation,since_upstream_propagation}.rs` | 2499–2501 | preserved |
| RF-4 | CLI-wired suites named: `smelt-cli/tests/since_upstream.rs` (sufficiency-vs-full-refresh check), `include_upstreams.rs` (resolved-slices-suffice-vs-full-refresh over two-hop chain + unclocked-ancestor-resolves-to-whole-table case) | 2502–2505 | preserved |
| RF-5 | `smelt-maintenance-testkit` (dev-only, not published) — Link-C in-process harness: real-run-pipeline driver, typed `ModelRecipe` generator (`recipe.rs`), schema-generic schedule generator (`schedule_gen.rs`), S-tracked equivalence oracle (`s_tracker.rs`, `oracle_modes.rs`), multiset-equality oracle (`oracle.rs`); wired as dev-dependency of `smelt-cli` | 2506–2509 | weakened (high-value), restored |
| RF-6 | `cargo test -p smelt-cli --test maintenance_conformance` is the standing generative equivalence gate — deterministic-seeded sample of typed `ModelRecipe` values (append-only partition-grain, fact+mutable-dimension, `grain: key`, generated 2–3 node DAGs), staged/classified/driven through `execute_project` against real DuckDB, asserted equal to full-refresh oracle after every run step under adversarial append/lateness/mutation/redelivery/definition-change schedules; `SMELT_CONFORMANCE_CASES` scales sample depth | 2510–2517 | preserved |
| RF-7 | Composed (`grain: key` + `timeseries:`) recipe family exercises all three key-temporal-locality routes (key-embedded via `execute_project`; key-determined and declared-recurrence-bounded-with-in-bound-redeliveries driven directly against real DuckDB, the latter also used by `locality_route3_recurrence_check.rs`); asserts whole-table and per-slice equivalence every step; own admission-rate floor (`SMELT_CONFORMANCE_COMPOSED_CASES`) | 2517–2523 | preserved |
| RF-8 | Generated model-edge enrichment recipe family — one closure-admissible `LEFT JOIN` shape + two closure-failing siblings (bare inner join, membership predicate over enrichment column); drives delta-restricted-vs-widened-scan choice both ways over fixed `S`; its own P1 skeleton-source-closure verdict (derived, not asserted) gates which cases run; end states must be bit-identical; a second case drives a fully-suppressed conditional write through real observed-delta recording, asserting zero rows written / present-and-empty recorded delta / zero regions scheduled downstream / end state equal to full-refresh oracle; own admission-rate floor | 2523–2532 | weakened (high-value), restored |
| RF-9 | Key-determined route's merge mechanics (write-once partition, additive fold) exercised against real DuckDB; slice-pruned target scan is not (driver omits the slice predicate — DuckDB's `MERGE` binder refuses the real predicate shape, the `BindMerge` divergence) | 2532–2536 | preserved |
| RF-10 | `pinned` module reproduces every construct×posture cell + named hazard schedule as deterministic always-reproducible cases (never proptest-drawn alone); `registry` module tracks named divergences with a staleness report (never-fired entries reported not failed, same pattern as `smelt-db/tests/prop_helpers/known_unknowns.rs`) | 2536–2541 | weakened (low-value: `known_unknowns.rs` analogy and "reported not failed" clause dropped) |
| RF-11 | `crates/smelt-cli/tests/property_discovery/` per-cell probe modules cover constructs the typed recipe generator has no vocabulary for yet (self-referential models, `UNION ALL`, `LEFT JOIN`, correlated `EXISTS`, stacked window frames, cross-source column-name collision, mutable source aggregated directly); disposable research probes; cites `.claude/scripts/property-experimental-gate.sh` | 2541–2545 | preserved |
| RF-12 | `crates/smelt-cli/tests/incremental/` drives a backend's incremental strategy directly given a hand-supplied filter, proving execution correctness independent of filter derivation | 2546–2547 | preserved |
| RF-13 | `cargo test -p smelt-logical --test maintenance_plan_conformance :: coverage_matrix_is_inhabited` — standing inventory gate over the research example catalogue's coverage matrix (`docs/research/20260705-refresh-as-maintenance-plan/07-example-catalogue.md` §"Coverage matrix" + one added `INTERSECT`/`EXCEPT` row); encodes matrix as data, every inhabited cell accounted for by exactly one of `CLAIMED`/`KNOWN_GAPS`; additive-only | 2548–2558 | preserved (restored the `INTERSECT`/`EXCEPT` row detail) |
| RF-14 | `CLAIMED` currently lifts 9 catalogue ids (EX-02, EX-08, EX-12, EX-14, EX-18, EX-24, EX-26, EX-27, EX-35, plus added EX-41/EX-42 row); remainder of the ~100 inhabited cells named individually in `KNOWN_GAPS`; both lists per-cell not per-row | 2558–2566 | weakened (high-value), restored |
| RF-15 | User docs list: `docs-site/docs/index.md`, `guide/{incremental-models,sql-models,materializations}.md`, `concepts/how-it-works.md`, `reference/{timeseries,smelt-yml,cumulative-aggregate,cli}.md`; `cli.md` documents `--since-upstream`/`--include-upstreams`/`smelt explain --show-sql`; `smelt-yml.md` documents `maintenance:` block | 2567–2571 | preserved |
| RF-16 | Plans (history): `docs/plans/20260704-model-updates.md`, `-fundamentals.md`, `docs/plans/20260705-property-discovery-loop.md` | 2572–2574 | preserved |
| RF-17 | Research citations: `docs/research/20260715-conditional-maintenance-without-cdf.md`, `docs/research/20260716-relation-contract-and-per-cell-addressing.md` | 2575–2580 | preserved |
| RF-18 | Related specs list (one for whole spec): `model_properties.md`, `model_transforms.md`, `models.md`, `timeseries.md`, `sources.md`, `expansion.md`, `functions.md`, `materialized_view.md`, `multi_backend.md`, `schema_evolution.md`, `run_state.md`, `virtual_environments.md`, `diagnostics.md`, `architecture.md`, `cli.md` | 2581–2594 | preserved |
| RF-19 | Partition-grain code refs: `smelt-core/src/config.rs` (`PartitionGrainConfig`, `Granularity`, `Weekday`), `smelt-core/src/metadata.rs`, `smelt-logical/src/rules/incremental.rs`, `smelt-logical/src/types.rs`, `smelt-runtime/src/transformer.rs` (`inject_time_filter`, `inject_source_filters`, `is_transparent_single_source`), `smelt-backend/src/lib.rs` (`delete_partitions`, `insert_into_from_query`, `delete_and_insert_transactional`), `smelt-backend-duckdb/src/lib.rs`, `smelt-dialect/src/dialect.rs` (`supports_merge`) | 2598–2606 | preserved |
| RF-20 | Partition-grain tests/docs/plans/research citations (unit tests in `incremental.rs`, CLI `incremental_*.rs`, per-partition full-refresh harness; user docs; four plans; four research docs; legacy `DESIGN.md` §"Incremental Table Builds") | 2607–2619 | preserved |
| RF-21 | Key-grain code refs: `smelt-core/src/config.rs` (`RefreshStrategy`), `smelt-logical/src/rules/cumulative.rs`, `smelt-runtime/src/maintenance_driver.rs` (`WindowedKeyedRule`), `smelt-runtime/src/cumulative.rs`, `smelt-backend/src/lib.rs` (`merge_into`) + duckdb/spark impls | 2623 | preserved |
| RF-22 | Key-grain tests/docs/plans citations (cumulative classifier unit tests, keyed end-state-equivalence harness, `merge_into` tests; four user-doc pages incl. the deduplication tutorial contrast; five plans incl. `20260809-keyed-frontier.md`) | 2624–2626 | preserved |
