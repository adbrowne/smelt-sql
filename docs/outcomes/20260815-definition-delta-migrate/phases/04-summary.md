# Phase 4 summary — Rename `smelt backbuild` → `smelt rebuild`

**Shipped:**
- CLI verb renamed end to end: `Commands::Rebuild(RebuildArgs)` in `main.rs`, `commands/backbuild.rs`
  → `commands/rebuild.rs` (`pub async fn rebuild`), `commands/mod.rs` updated.
- Tests renamed and extended: `tests/rebuild_dry_run.rs` (git-mv, plus new
  `backbuild_verb_is_gone` and `help_lists_rebuild`), `tests/e2e/rebuild_cumulative_e2e.rs`
  (git-mv), `explain_show_sql.rs` / `tutorial_freshness.rs` identifiers updated.
- Verb-mention doc comments updated across `smelt-cli` and `smelt-runtime` (explain.rs,
  reporter.rs ×2, execute.rs, types.rs, fn_bodies.rs, surface_audit.rs).
- Spec sweep: `docs/specs/cli.md`, `model_selection.md` renamed verb mentions;
  `definition_deltas.md` Known-Divergences bullet about the stale `smelt backbuild` name
  deleted. `architecture.md` left untouched — its `backbuild/` mentions are module path /
  mechanism name, out of scope by design.
- docs-site sweep: `reference/cli.md`, `guide/incremental-models.md`,
  `developing/architecture.md`, `README.md`, and `guide/backbuild-synthesis.md` (verb
  mentions renamed, stale "two things called backbuild" naming-collision callout deleted;
  page filename/nav entry untouched — it's the mechanism name).
- `examples/web_analytics/generate_tutorial.py` (`is_backbuild`→`is_rebuild` etc.),
  `tutorial_pages/changing-things.md`/`README.md` updated; the generated
  `docs-site/docs/examples/web-analytics/changing-things.md` regenerated by actually running
  the generator (never hand-edited).
- Also caught by the final `rg` sweep and fixed: residual mentions in `docs/DESIGN.md`,
  `docs/ROADMAP.md`, `docs/TODO.md`.

**Decisions:**
- No compatibility alias — `smelt backbuild` is a hard-removed verb (exits 2, unrecognised
  subcommand), matching the plan's explicit "no back-compat constraint" call.
- `smelt-logical/src/backbuild/` crate module and "backbuild synthesis"/"backbuild option
  catalogue" mechanism naming deliberately untouched everywhere, including
  `docs/specs/architecture.md` and the `docs-site` page's filename/nav — only the CLI verb
  renamed, per the plan's scope note.

**For the next planner:**
- Nothing left over from this phase's own scope; the `rg` sweep for
  `\bsmelt backbuild\b|Commands::Backbuild|BackbuildArgs` is clean except intentional residue
  (the `backbuild_verb_is_gone` test itself, and a `docs/TODO.md` historical parenthetical).
- The `docs-site/docs/guide/backbuild-synthesis.md` page's full narrative rewrite around
  `smelt migrate`/`--apply` is explicitly phase 8's job, not done here — only the verb
  mentions and the now-false naming-collision callout were touched.
- Phase 8/9's "no CLI/runtime consumer drives a backbuild script" divergence sentence in
  `architecture.md` (~line 513) is also untouched, as scoped.

**Gates:**
- `bash .claude/scripts/verify-phase.sh` — ALL GREEN (fmt, clippy both feature sets, full
  `cargo test`, example_diagnostics).
- `cargo test -p smelt-cli --test rebuild_dry_run` — 3 passed.
- `cargo test -p smelt-cli --test tutorial_freshness` — 1 passed.
- `cargo test -p smelt-cli --test explain_show_sql` — 7 passed.
- `cargo test -p smelt-cli --test e2e` — 174 passed (includes `rebuild_cumulative_e2e`).
- `cargo test -p smelt-runtime --test surface_audit` — 2 passed.
