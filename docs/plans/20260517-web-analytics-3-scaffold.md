# Plan: Web Analytics Phase 3 — Example scaffolding

**Date**: 2026-05-18
**Spec**: [`docs/specs/datagen.md`](../specs/datagen.md) — drives the `datagen.yaml` shape (the `json_object`, `linked_choice`, `linked_pools`, `foreign_key`, and partitioning entries are the correctness oracle for the generator config).
**Spec diff**: no spec change in this phase — Phases 1 + 2 already landed the datagen-spec increments (`json_object` in `57ce9489`; `linked_choice`/`linked_pools` in `ebe4da8a`). This phase is the first *consumer* of those generators.
**Tracking branch**: `worktree-web_analytics` (overall plan: [`docs/plans/20260517-web-analytics-example.md`](20260517-web-analytics-example.md); meta-plan: `/home/andrew/.claude/plans/i-would-like-to-stitch-eventstream.md`)
**Docs**: code+docs (the example dir itself ships a stub `README.md`; the full README + docs-site index entry land in Phase 8 per the overall plan)

---

## Execution prompt (for a fresh Claude session)

You are executing this plan from the start of a new session. Your job is to drive Phase 3 to completion using `/smelt:implement`, then dispatch the meta-plan §5 expert reviewers (`sql-expert`, `examples-curator`), then update the in-repo overall-plan status table and push.

**Before touching any code:**

1. Read this plan in full. Then read `docs/specs/datagen.md` — the `json_object`, `linked_choice`, `linked_pools` entries are the oracle for the `datagen.yaml` written here. Do not re-open settled spec decisions; if a spec rule blocks a green test, run `/smelt:spec datagen` to revise the spec rather than encode the divergence in the example.
2. Confirm you are on branch `worktree-web_analytics`. If not, ask before continuing.
3. Find the next phase whose status is `pending` in the Progress tracking table below. That is your starting point. If every phase is `done`, run the post-implementation verification under "Verification" and stop.

**For each phase, run the per-phase loop encoded in `/smelt:implement`:** implementer subagent (`model: sonnet`) → reviewer subagent (`model: sonnet`) → iterate → record + commit + push.

**Phase 5 is the expert-reviewer dispatch loop** — after Phases 1–4 commit, dispatch the meta-plan §5 expert reviewers applicable to this phase (`sql-expert`, `examples-curator`), address material findings, and re-dispatch each expert until clean (or stop-the-line per meta-plan §7). Do NOT skip Phase 5. The autonomy loop's `<<PHASE_COMPLETE>>` sentinel may only fire once Phase 5's acceptance gate is met and the overall-plan status row is updated.

**When to pause and ask the user:**

- The reviewer surfaces the same material finding across two implementer passes.
- TDD tests cannot be made green without violating a spec rule.
- A spec assumption turns out to be wrong (run `/smelt:spec datagen` first to update).
- `cargo test`, `cargo clippy --all-targets`, or `cargo test -p smelt-cli --test example_diagnostics` surfaces a pre-existing failure unrelated to the plan.
- Phase 5: an expert flags the same material finding on round 3 (per-expert bound), or two different experts flag the same systemic concern in the same round.

**Conventions every phase:**

- Red-green TDD: failing test before any implementation. For SQL-only phases the test is "`smelt build --project-dir examples/web_analytics` exits 0 after running `smelt-datagen` at `--scale-factor 0.01`" plus "`cargo test -p smelt-cli --test example_diagnostics` reports zero diagnostics for `examples/web_analytics/`" — both run as the failing oracle before any model/function file is added; both must pass after.
- Atomic per-phase commits with the phase's `Commit.` line verbatim.
- Subagent model rule: implementer + reviewer + every expert in Phase 5 spawn with `model: "sonnet"`. Do not let them inherit `opus` from the parent autonomy loop.
- Never skip hooks, never `--no-verify`, never force-push the tracking branch.
- Don't widen scope: this plan introduces the scaffold (project config, datagen config, raw event source bind, bronze view, parse function, `silver/events_parsed`) only. **No sessionization, no identity stitching, no marts** — those are Phases 4–8 of the overall plan.
- Honor architectural invariants from `CLAUDE.md`: no edits in `crates/` (this phase is example-only). All SQL must parse without LSP diagnostics in `examples/web_analytics/`.
- **Timeless-oracle rule (CLAUDE.md).** This plan file uses phase vocabulary; the example dir's `README.md`, the `setup_sources.sql` comments, and any docs-site touch must read as a feature description with no `Phase N` labels.

---

## Context

The overall plan ([`docs/plans/20260517-web-analytics-example.md`](20260517-web-analytics-example.md), §Goal items 1–2) describes a bronze→silver→gold web-analytics pipeline whose source data is generated by `smelt-datagen` using the two new generators (`json_object`, `linked_choice`) landed in Phases 1 and 2. Phase 3 of the overall plan is the **first concrete consumer** of those generators: it lays down the project skeleton at `examples/web_analytics/` so that subsequent phases (sessionization, identity stitching, marts) have somewhere to add models. The scaffold itself is intentionally minimal — a project config, a datagen config that produces realistic dimension tables (users, devices) joined to a 60-day partitioned events fact via a `linked_pools` block matching the meta-plan's 60/25/10/5 co-occurrence distribution, a bronze view that re-selects from the raw source, a `parse_event_payload` smelt function that decodes the JSON payload into a typed struct, and a `silver/events_parsed` model that calls the function.

## Scope

### In scope

- `examples/web_analytics/smelt.yml` — project config (DuckDB dev target, `default_materialization: view`, `paths: [models]`, `unstable_schema: true` for typed-struct returns from the `parse_event_payload` function).
- `examples/web_analytics/datagen.yaml` — dimension datasets (`users`, `devices`), fact dataset (`events`) partitioned by `event_date` over 60 days from `2026-03-19` to `2026-05-17` with `num_rows: 1_000_000` (defaults scale via `--scale-factor`), plus a single `linked_pools:` entry named `device_user` with four shapes matching the 60/25/10/5 distribution. The `events.columns` include `event_id` (`sequential_id`), `device_id` + `user_id` (both via `linked_choice` referencing `device_user`), `event_ts` (timestamp within the partition day), and `payload` (`json_object` with fields `event_name`, `platform`, `url`).
- `examples/web_analytics/setup_sources.sql` — DuckDB script that creates the `raw` schema and loads three parquet inputs (`users`, `devices`, `events`) into tables. The events table uses hive partitioning. **Honest about its role**: the comment header explains this is the bootstrap step a developer runs after `smelt-datagen` writes the parquet files; `smelt build` itself does not invoke it.
- `examples/web_analytics/models/sources/raw/users.yml` — source schema for the users dimension (columns: `user_id INTEGER`, `signup_date DATE`).
- `examples/web_analytics/models/sources/raw/devices.yml` — source schema for the devices dimension (columns: `device_id INTEGER`, `device_type VARCHAR`, `first_seen_date DATE`).
- `examples/web_analytics/models/sources/raw/events.yml` — source schema for the events fact (columns: `event_id BIGINT`, `device_id INTEGER`, `user_id INTEGER` *nullable*, `seconds_in_day INTEGER`, `payload VARCHAR`, `event_date DATE`). The `event_ts TIMESTAMP` does not exist on bronze — it is composed in silver from `event_date` + `seconds_in_day`.
- `examples/web_analytics/models/bronze/raw_events.sql` — bronze view: `SELECT * FROM smelt.sources.raw.events` (the bronze layer is a passthrough; it exists so later phases can attach incremental + lineage policies to a named model rather than to the raw source).
- `examples/web_analytics/functions/parse_event_payload.sql` — smelt function `parse_event_payload(payload_json: Expr<Text>) -> Expr<Struct<{event_name: Text, platform: Text, url: Text}>>` whose body uses DuckDB's `json_extract_string` to project the three fields into a struct.
- `examples/web_analytics/models/silver/events_parsed.sql` — silver model: project bronze rows with `event_ts` composed from (`event_date` + `seconds_in_day`) and the JSON payload replaced by the parsed struct. Columns: `event_id`, `device_id`, `user_id`, `event_ts`, `event_date`, `parsed: Struct<{event_name, platform, url}>`.
- `examples/web_analytics/README.md` — stub README (a single paragraph saying "see overall plan link" + the two-line bootstrap instructions). Full README content lands in Phase 8 of the overall plan.
- `examples/web_analytics/.gitignore` — ignores `data/`, `target/`, `.smelt/`.
- `crates/smelt-cli/tests/example_diagnostics.rs` — add a new `#[test] fn web_analytics_no_diagnostics()` calling `check_workspace_no_diagnostics("examples/web_analytics")`. Each example is listed explicitly (see the existing `timeseries_no_diagnostics`, `retail_analytics_no_diagnostics`, etc. at lines 82–99).

### Explicitly deferred (scope guardrails)

- **No sessionization.** No `silver/sessions.sql`, no `silver/device_user_edges.sql`, no `sessionize` function. Phase 4 of the overall plan.
- **No identity stitching.** No `gold/identity_*` models, no `eventstream_with_identity` model. Phases 5–7 of the overall plan.
- **No marts.** No `daily_active_users_by_method`, no `identity_method_comparison`. Phase 8.
- **No inline `.test.sql` invariant assertions.** Those land alongside their target models in Phases 4–7.
- **No docs-site index entry.** Phase 8.
- **`scale_factor: 1.0` default for production runs is *not* the verification gate.** The CI gate uses `--scale-factor 0.01` (~10K events). A 1M-row dataset is generated on demand by the developer.
- **No real-fixture parquet committed to the repo.** Data is generated by `smelt-datagen` from `datagen.yaml`. The `data/` directory is gitignored.

---

## Progress tracking

| Phase | Status | Commit | Date |
|-------|--------|--------|------|
| 1 | done | `055786a5` | 2026-05-18 |
| 2 | done | `aa053151` | 2026-05-18 |
| 3 | done | `aaf6f696` | 2026-05-18 |
| 4 | done | *(this commit)* | 2026-05-18 |
| 5 | pending |  |  |

---

### Phase 1: Project skeleton + datagen config

**Goal.** Lay down `examples/web_analytics/smelt.yml`, `examples/web_analytics/datagen.yaml`, `examples/web_analytics/.gitignore`, and a stub `examples/web_analytics/README.md`. After this phase, `smelt-datagen --config examples/web_analytics/datagen.yaml --scale-factor 0.01` runs to completion and writes Parquet files under `examples/web_analytics/data/`. No SQL models yet — the project is still empty from `smelt build`'s perspective.

**Pre-conditions.** None — this is the first phase. Working tree clean on `worktree-web_analytics`.

**TDD tests to write first.**

- `examples/web_analytics/tests/test_datagen_smoke.sh` (or, preferred, inline as a `cargo test -p smelt-datagen` integration test under `crates/smelt-datagen/tests/example_web_analytics.rs::test_datagen_runs_at_small_scale`) — invokes the datagen binary against `examples/web_analytics/datagen.yaml` with `--scale-factor 0.01` and asserts:
  1. Exit code 0.
  2. `examples/web_analytics/data/users/data.parquet` exists and contains > 0 rows.
  3. `examples/web_analytics/data/devices/data.parquet` exists and contains > 0 rows.
  4. `examples/web_analytics/data/events/event_date=*/data.parquet` matches at least one file (partitioned output).
  5. The events parquet's `device_id` column joins to the users dimension's `user_id` only on pool entries (i.e. every `(device_id, user_id)` pair seen in events appears in the pool — verified by rebuilding the pool deterministically from the same seed and looking up each row's pair).
- `crates/smelt-datagen/tests/example_web_analytics.rs::test_datagen_joint_distribution_within_tolerance` — at `--scale-factor 0.1` (`100K` events), the four pool categories — (a) single-owner: `device_id` appears in exactly one pool entry with non-null `user_id`; (b) anonymous: `user_id` is null; (c) shared-device: `device_id` appears in ≥2 entries with different `user_id`s; (d) multi-device user: `user_id` appears in ≥2 entries with different `device_id`s — aggregated from the *pool snapshot* (not row counts) match the 60/25/10/5 input ratios within ±3 percentage points per category. Tolerance is wider than Phase 2's ±2pp because the pool snapshot is finite (one pool of `pool_size` entries, not a sampling distribution).

**Implementation shape.**

- `examples/web_analytics/smelt.yml`:
  ```yaml
  name: web_analytics
  version: 1
  paths:
    - models
    - functions
  targets:
    dev:
      type: duckdb
      database: target/dev.duckdb
      schema: main
  default_materialization: view
  unstable_schema: true
  ```
- `examples/web_analytics/datagen.yaml` — full text below (see "datagen.yaml content" sub-section). Key shape choices:
  - `seed: 42`.
  - `scale_factor: 1.0` default; runs in CI gate at `--scale-factor 0.01`.
  - `users` dataset: `num_rows: 50_000`, columns `user_id` (`sequential_id`), `signup_date` (`date`, range spans the event window minus 90 days lead).
  - `devices` dataset: `num_rows: 150_000`, columns `device_id` (`sequential_id`), `device_type` (`weighted_choice` over `mobile/desktop/tablet`), `first_seen_date` (`date`, same range as users).
  - `linked_pools:` — one entry `device_user` with `pool_size: 100_000`, `seed: 1337` (explicit to make the pool independent of the dataset seed under the seed-isolation rule from spec §`linked_choice`), four shapes:
    - **single-owner** `weight: 0.60`, `emit: 1`, `fields: { device_id: foreign_key(devices), user_id: foreign_key(users) }`.
    - **anonymous** `weight: 0.25`, `emit: 1`, `fields: { device_id: foreign_key(devices), user_id: optional(prob: 0, inner: foreign_key(users)) }`. Per `docs/specs/datagen.md:102` `optional` produces `null` with probability `1 - prob`; `prob: 0` therefore means **always null** (the spec's own `linked_choice` example at line 295 uses this exact form for anonymous devices). Do not flip the sign.
    - **shared-device** `weight: 0.10`, `emit: 3`, `sticky: [device_id]`, `fields: { device_id: foreign_key(devices), user_id: foreign_key(users) }` — emits 3 pool entries with the same device_id, different user_ids.
    - **multi-device-user** `weight: 0.05`, `emit: 3`, `sticky: [user_id]`, `fields: { device_id: foreign_key(devices), user_id: foreign_key(users) }` — emits 3 entries with the same user_id, different device_ids.
  - `events` dataset: `num_rows: 1_000_000`, partitioned by `event_date` over 60 days from `2026-03-19` to `2026-05-17`. **Do not declare `event_date` in `columns:`** — the partitioned writer auto-injects the partition column per row (verified in `crates/smelt-datagen/src/generic_parquet.rs:238` which passes `Some((part_cfg.column.as_str(), date_str.as_str()))` into `write_rows_to_file`). The existing `examples/retail_analytics/datagen.yaml` follows the same convention (no `order_date` under `orders.columns`). Columns:
    - `event_id`: `sequential_id`.
    - `device_id`: `linked_choice { pool: device_user, field: device_id }`.
    - `user_id`: `linked_choice { pool: device_user, field: user_id }` (nullable — the schema's `optional` propagates through `linked_choice` per spec §`linked_choice` Arrow-type rule).
    - `seconds_in_day`: `uniform_int { min: 0, max: 86399 }` — a sub-day offset. The `timestamp` generator's `start`/`end` is fixed across the dataset (it does not know which partition day a row belongs to), so a full-day-resolution `event_ts` correlated with `event_date` is composed in Phase 4's silver model as `event_date + (seconds_in_day * INTERVAL 1 SECOND)`. Do not introduce a partition-aware timestamp generator in this phase.
    - `payload`: `json_object { fields: { event_name: weighted_choice(page_view: 0.50, click: 0.30, purchase: 0.05, scroll: 0.10, error: 0.05), platform: weighted_choice(web: 0.55, ios: 0.25, android: 0.20), url: weighted_choice("https://example.com/home": 0.40, "https://example.com/product": 0.30, "https://example.com/cart": 0.15, "https://example.com/checkout": 0.10, "https://example.com/account": 0.05) } }`.
- `examples/web_analytics/.gitignore`: `data/`, `target/`, `.smelt/`.
- `examples/web_analytics/README.md`: one paragraph linking to the overall plan + two lines on the bootstrap (`smelt-datagen --config datagen.yaml && duckdb target/dev.duckdb < setup_sources.sql && smelt build`). Full README content lands in Phase 8 of the overall plan.

**Critical files (allowed to touch in this phase).**

- `examples/web_analytics/smelt.yml`
- `examples/web_analytics/datagen.yaml`
- `examples/web_analytics/.gitignore`
- `examples/web_analytics/README.md`
- `crates/smelt-datagen/tests/example_web_analytics.rs` (new integration test file)
- `docs/plans/20260517-web-analytics-3-scaffold.md` (this file — committed at the start of Phase 1 work, before any other change)

**Docs touched.** None (the spec is the oracle; Phase 1's README is a stub; the docs-site index entry lands in Phase 8 of the overall plan).

**Review checklist** (material findings only):

- [ ] `smelt.yml` parses (test: `cargo test -p smelt-cli --test example_diagnostics` discovers the example and reports zero project-config errors; the test will still report missing model errors at this phase — that is expected and is asserted *not* to happen after Phase 4).
- [ ] `datagen.yaml`'s `linked_pools.device_user` matches the 60/25/10/5 distribution within ±3pp on a 100K-event run.
- [ ] Pool seed (`1337`) is explicit and documented inline so the seed-isolation rule from spec §`linked_choice` is observable.
- [ ] No `entity:` blocks or other features not used by Phases 3–8.
- [ ] `.gitignore` covers `data/`, `target/`, `.smelt/`. No parquet committed to the repo.
- [ ] README is a stub — does **not** describe identity stitching, sessionization, marts, etc.
- [ ] If the `optional` semantics turned out to need `prob: 0.0` instead of `1.0`, the change was made and the comment in `datagen.yaml` makes the chosen direction explicit.
- [ ] If the partition-aware `timestamp` generator does not exist, the chosen workaround was checked against the `timeseries` example pattern (do not invent generators in this phase).

**Commit.** `feat(examples): web_analytics scaffold — smelt.yml + datagen.yaml + .gitignore + stub README (web-analytics Phase 3)`

---

### Phase 2: Source schema files + bronze view + setup_sources.sql

**Goal.** Land the three source-schema yml files under `models/sources/raw/`, the `setup_sources.sql` bootstrap script, and the `bronze/raw_events.sql` passthrough view. After this phase, `cargo test -p smelt-cli --test example_diagnostics` reports zero diagnostics for `examples/web_analytics/` (modulo the silver model added in Phase 4), AND a developer can manually run `smelt-datagen ... && duckdb target/dev.duckdb < setup_sources.sql && smelt build --project-dir examples/web_analytics` end-to-end and the bronze view materializes.

**Pre-conditions.** Phase 1 committed.

**TDD tests to write first.**

- `crates/smelt-cli/tests/example_diagnostics.rs` already discovers `examples/web_analytics/` (verified in Phase 1 test). Phase 2's contribution is that the discovery now sees the source yml files and the bronze model, and reports zero diagnostics for them. The test addition (or reliance on auto-discovery — verify by inspecting `example_diagnostics.rs` before writing the change) is the failing oracle: the test passes only after the source files + bronze model land.
- `crates/smelt-datagen/tests/example_web_analytics.rs::test_setup_sources_sql_runs` — a smoke test that runs `smelt-datagen` at `--scale-factor 0.01`, then runs `duckdb /tmp/web_analytics_test.duckdb < examples/web_analytics/setup_sources.sql` (or programmatically via `duckdb-rs`), and asserts the three tables `raw.users`, `raw.devices`, `raw.events` exist with > 0 rows each. The test cleans up `/tmp/web_analytics_test.duckdb` at the start. **Caveat:** if the autonomy loop's runner does not have the `duckdb` CLI in `PATH`, prefer the programmatic `duckdb-rs` form.
- `crates/smelt-datagen/tests/example_web_analytics.rs::test_bronze_raw_events_view` — extends the smoke test: after `smelt build --project-dir examples/web_analytics`, the view `main.raw_events` exists and `SELECT count(*) FROM main.raw_events` matches the events parquet row count.

**Implementation shape.**

- `examples/web_analytics/models/sources/raw/users.yml`:
  ```yaml
  description: Web analytics users dimension (synthetic).
  columns:
    - name: user_id
      type: INTEGER
    - name: signup_date
      type: DATE
  ```
- `examples/web_analytics/models/sources/raw/devices.yml`:
  ```yaml
  description: Web analytics devices dimension (synthetic).
  columns:
    - name: device_id
      type: INTEGER
    - name: device_type
      type: VARCHAR
    - name: first_seen_date
      type: DATE
  ```
- `examples/web_analytics/models/sources/raw/events.yml`:
  ```yaml
  description: Raw web analytics events with a JSON-encoded payload.
  columns:
    - name: event_id
      type: BIGINT
    - name: device_id
      type: INTEGER
    - name: user_id
      type: INTEGER   # nullable in data; the type system treats columns as nullable by default
    - name: seconds_in_day
      type: INTEGER
    - name: payload
      type: VARCHAR
    - name: event_date
      type: DATE
  ```
  **Caveat:** smelt's source schema yml does not currently carry per-column nullability (verify by reading `crates/smelt-db/src/sources.rs` and the existing `examples/retail_analytics/models/sources/raw/*.yml` files). If nullability matters for the silver model's type inference and the schema cannot express it, document this as a deferred item under "Deferred during implementation" and proceed — the silver model handles nullability via SQL semantics (NULL just propagates).
- `examples/web_analytics/setup_sources.sql`:
  ```sql
  -- Load smelt-datagen-generated parquet into DuckDB source tables.
  -- Run after `smelt-datagen --config datagen.yaml` writes Parquet under data/.
  -- `smelt build` does not invoke this — it expects the raw schema to exist.

  CREATE SCHEMA IF NOT EXISTS raw;

  CREATE OR REPLACE TABLE raw.users AS
  SELECT * FROM read_parquet('data/users/data.parquet');

  CREATE OR REPLACE TABLE raw.devices AS
  SELECT * FROM read_parquet('data/devices/data.parquet');

  CREATE OR REPLACE TABLE raw.events AS
  SELECT * FROM read_parquet('data/events/event_date=*/data.parquet', hive_partitioning=true);
  ```
- `examples/web_analytics/models/bronze/raw_events.sql`:
  ```sql
  -- Bronze passthrough — provides a named model for downstream silver
  -- transformations to attach to instead of binding directly to the raw source.
  SELECT
      event_id,
      device_id,
      user_id,
      seconds_in_day,
      payload,
      event_date
  FROM smelt.sources.raw.events
  ```

**Critical files.**

- `examples/web_analytics/models/sources/raw/users.yml`
- `examples/web_analytics/models/sources/raw/devices.yml`
- `examples/web_analytics/models/sources/raw/events.yml`
- `examples/web_analytics/setup_sources.sql`
- `examples/web_analytics/models/bronze/raw_events.sql`
- `crates/smelt-datagen/tests/example_web_analytics.rs` (extension)

**Docs touched.** None.

**Review checklist** (material findings only):

- [ ] All three source yml files parse with no LSP diagnostics.
- [ ] The bronze view references `smelt.sources.raw.events` (not `smelt.ref(...)`), per the convention in `examples/retail_analytics/`.
- [ ] `setup_sources.sql` is honest in its comment header about its role (separate from `smelt build`).
- [ ] No JOIN syntax in the bronze view (parser limitation per `examples/timeseries/README.md` — though this view doesn't need any).
- [ ] The events `event_date` column type matches what the partitioned Parquet writer emits (DATE vs VARCHAR). Verify against the spec or against actual generator output before committing.
- [ ] If column nullability in the source schema turned out to be unrepresentable, the gap is recorded under "Deferred during implementation".

**Commit.** `feat(examples): web_analytics raw source schemas + bronze view + setup_sources (web-analytics Phase 3)`

---

### Phase 3: `parse_event_payload` smelt function

**Goal.** Add `functions/parse_event_payload.sql` — a smelt function whose body uses DuckDB's `json_extract_string` to decode the JSON `payload` column into a typed struct. No model calls it yet (Phase 4 does); this phase is the function declaration + a function-only diagnostics test.

**Pre-conditions.** Phase 2 committed. `unstable_schema: true` is set in `smelt.yml` (Phase 1) so the struct return type is allowed.

**TDD tests to write first.**

- `crates/smelt-cli/tests/example_diagnostics.rs` continues to report zero diagnostics for `examples/web_analytics/` after the function file lands. This is the LSP-side oracle. The test fails before the function file is added (or passes vacuously if there are no callers — verify by checking that `example_diagnostics` walks `functions/` files; if it does not, this phase is purely additive and the diagnostics test passes either way).
- `crates/smelt-datagen/tests/example_web_analytics.rs::test_parse_event_payload_function_compiles` — a small integration test that loads the function via `smelt-db`'s function-loading path and asserts:
  1. The function's signature parses to `parse_event_payload(payload_json: Expr<Text>) -> Expr<Struct<{event_name: Text, platform: Text, url: Text}>>`.
  2. No diagnostic errors are emitted for the function file.

   **Caveat:** the function-loading path may not be exposed as a direct API outside the LSP. If so, replace this test with a lower-cost equivalent: a one-line Rust test that calls `smelt-parser` directly on the function source and asserts the parsed signature shape. The principle is the function compiles standalone.

**Implementation shape.**

- `examples/web_analytics/functions/parse_event_payload.sql`:
  ```sql
  -- Decode the raw JSON payload column from bronze.raw_events into a typed
  -- struct. Bronze stores the payload as VARCHAR because smelt-datagen's
  -- json_object generator emits Utf8; silver lifts that into named, typed
  -- fields.
  smelt.define parse_event_payload(
      payload_json: Expr<Text>
  ) -> Expr<Struct<{event_name: Text, platform: Text, url: Text}>> AS (
      {
          json_extract_string(payload_json, '$.event_name') AS event_name,
          json_extract_string(payload_json, '$.platform') AS platform,
          json_extract_string(payload_json, '$.url') AS url
      }
  )
  ```
  The struct literal uses `{<expr> AS <field>, ...}` form — verified against `examples/functions_demo/functions/with_hour.sql:7` which uses `{EXTRACT(HOUR FROM event.ts) AS hour, ..event}`. Do **not** use `{<field>: <expr>, ...}` (that is the *type* literal syntax, not the value literal syntax).

**Critical files.**

- `examples/web_analytics/functions/parse_event_payload.sql`
- `crates/smelt-datagen/tests/example_web_analytics.rs` (extension)

**Docs touched.** None.

**Review checklist** (material findings only):

- [ ] The function declares the struct return type explicitly (no implicit inference).
- [ ] `json_extract_string` is used (returns Text), not `json_extract` (returns JSON type — DuckDB-specific).
- [ ] The function file lives under `functions/` (matching `paths:` in `smelt.yml`).
- [ ] The struct-construction syntax matches the canonical form from `examples/functions_demo/functions/` (no invented syntax).
- [ ] No diagnostics for the function file from `cargo test -p smelt-cli --test example_diagnostics`.

**Commit.** `feat(examples): web_analytics parse_event_payload smelt function (web-analytics Phase 3)`

---

### Phase 4: `silver/events_parsed` model + end-to-end build

**Goal.** Add `models/silver/events_parsed.sql` — projects bronze events with the JSON payload replaced by the parsed struct. After this phase, `cargo test -p smelt-cli --test example_diagnostics` reports zero diagnostics for `examples/web_analytics/`, AND `smelt-datagen --config examples/web_analytics/datagen.yaml --scale-factor 0.01 && duckdb /tmp/we.duckdb < examples/web_analytics/setup_sources.sql && smelt build --project-dir examples/web_analytics --target dev` succeeds end-to-end and materializes both `main.raw_events` (view) and `main.events_parsed` (view).

**Pre-conditions.** Phase 3 committed.

**TDD tests to write first.**

- `crates/smelt-cli/tests/example_diagnostics.rs` for `examples/web_analytics/` — zero diagnostics including for the new silver model. The test fails before `silver/events_parsed.sql` exists (because it references the as-yet-unimplemented file in this phase's diff) or — more practically — passes vacuously until the model is added, then catches any type-inference errors when the model is added. Run the test first, see it pass (or fail with "no model"), add the model, see it pass. The principle is "zero diagnostics".
- `crates/smelt-datagen/tests/example_web_analytics.rs::test_end_to_end_smelt_build` — extends the smoke test:
  1. Run `smelt-datagen ... --scale-factor 0.01`.
  2. Programmatically execute `setup_sources.sql` via `duckdb-rs` (preferred) or the `duckdb` CLI.
  3. Invoke the `smelt` binary (or equivalent in-process API if available) with `--project-dir examples/web_analytics --target dev`.
  4. Assert the binary exits 0.
  5. Open the resulting DuckDB and assert `SELECT count(*) FROM main.events_parsed` matches the events row count.
  6. Assert one row's `parsed` struct projects to non-null `event_name`, `platform`, `url` (i.e. the JSON decode actually worked).

**Implementation shape.**

- `examples/web_analytics/models/silver/events_parsed.sql`:
  ```sql
  -- Compose event_ts from the partition date + sub-day offset, and lift the
  -- JSON payload into a typed struct. Downstream models reference
  -- parsed.event_name / parsed.platform / parsed.url instead of re-parsing the
  -- JSON every time.
  SELECT
      event_id,
      device_id,
      user_id,
      CAST(event_date AS DATE) + (seconds_in_day * INTERVAL 1 SECOND) AS event_ts,
      CAST(event_date AS DATE) AS event_date,
      smelt.functions.parse_event_payload(payload) AS parsed
  FROM smelt.ref('raw_events')
  ```
  The smelt function call surface is `smelt.functions.<name>(...)` — verified against `examples/functions_demo/models/event_with_hour.sql`. The struct projection (e.g. `parsed.event_name`) is a downstream-phase concern; silver just produces the struct column.

**Critical files.**

- `examples/web_analytics/models/silver/events_parsed.sql`
- `crates/smelt-datagen/tests/example_web_analytics.rs` (extension)

**Docs touched.** None.

**Review checklist** (material findings only):

- [ ] `smelt.ref('raw_events')` (not `smelt.sources.raw.events` directly) — the bronze view is the intended intermediate per the layering convention.
- [ ] The function call uses `smelt.functions.parse_event_payload(payload)` — matches `examples/functions_demo` convention.
- [ ] No JOIN syntax (parser limitation; this model doesn't need any either).
- [ ] The end-to-end test materializes both `main.raw_events` and `main.events_parsed` and asserts row counts agree.
- [ ] No diagnostics for `examples/web_analytics/` from `example_diagnostics`.
- [ ] `silver/` directory placement matches the bronze/silver/gold convention introduced in Phase 2.

**Commit.** `feat(examples): web_analytics silver/events_parsed model + end-to-end build (web-analytics Phase 3)`

---

### Phase 5: Expert reviewer dispatch loop

For each expert listed below, dispatch via the Agent tool with `model: "sonnet"`,
brief prompt, the per-phase plan path, the spec path (if relevant), and the
in-repo plan path. The expert returns a list of findings classified as
"material" or "stylistic". Address material findings:

  - For each material finding, either edit directly (small) or dispatch a
    nested implementer subagent (larger).
  - Commit the fix with message `review(web-analytics-3): address {expert-name} feedback`.
  - Push.
  - Re-dispatch the same expert. Loop until the expert returns "no material findings".

Bounds:

  - Max 3 rounds per expert. If unresolved after 3 rounds → emit
    `<<PAUSE_FOR_HUMAN>>`.
  - If two different experts flag the same systemic concern in one round →
    emit `<<PAUSE_FOR_HUMAN>>`.

Experts for this phase (from meta-plan §5):

  - `sql-expert` — focus: bronze view correctness, silver model type inference,
    `parse_event_payload` function body uses canonical DuckDB JSON syntax, no
    parser-unsupported constructs (e.g. JOIN), source yml column types match
    the parquet output, smelt-function struct return type compiles.
  - `examples-curator` — focus: directory layout matches the bronze/silver
    convention from `examples/retail_analytics/`, `setup_sources.sql` is honest
    about its role, `.gitignore` covers all generated artifacts, README stub
    does not over-promise, `datagen.yaml` is the smallest config that exercises
    `json_object` + `linked_choice` + `linked_pools` + partitioning, no dead
    config keys, no unused source columns.

If a literal `sql-expert` or `examples-curator` agent type does not exist,
dispatch `general-purpose` with a prompt that frames it as such (read the spec
+ plan + diff, flag spec/impl drift, missing test cases, scope creep into
later phases — material findings only).

**Loop discipline.**

1. **Round 1.** Dispatch both experts in parallel — single message, multiple
   Agent tool calls. Each prompt MUST include:
   - This plan's path and the spec paths the expert should treat as oracle
     (datagen spec for the datagen.yaml; the existing functions/sources docs
     for the SQL and yml files).
   - The exact file scope from the table above.
   - The diff range to review: commits since the start of Phase 3 (typically
     `git log --oneline {first-commit-of-phase-3-of-overall-plan}..HEAD`).
   - Explicit instruction: report only **material** findings (correctness,
     spec drift, missing test cases, scope creep). Skip nits.
   - Output format: a numbered list of findings with file:line refs, or
     "no material findings".
   - Reminder to spawn with `model: "sonnet"` if the expert's tool palette
     allows nested subagents.

2. **Address findings.** For each expert that returns material findings:
   - If the fix is mechanical (≤~30 lines, single concern), edit directly.
   - If the fix is non-trivial, dispatch an implementer subagent
     (`model: sonnet`) scoped to the same file allowlist.
   - Run `cargo fmt --all`, `cargo clippy --all-targets`,
     `cargo test -p smelt-cli --test example_diagnostics`, and the
     `crates/smelt-datagen/tests/example_web_analytics.rs` integration tests
     after each fix batch.
   - Commit per expert: `review(web-analytics-3): address {expert-name} feedback`.
   - Push after each commit.

3. **Re-dispatch.** Re-dispatch only the expert(s) whose findings were
   addressed. Provide the round-1 prompt plus a diff of what changed since
   round N−1. "No material findings" → that expert is clean and exits.

4. **Repeat** until both experts are clean.

5. **Bounds (stop-the-line).** Emit `<<PAUSE_FOR_HUMAN>>` (with a one-line
   reason on the line above) and stop the autonomy loop if any of the
   following fires:
   - Same expert flags a material finding on round 3 (per-expert bound).
   - Both experts flag the same systemic concern in the same round (per
     meta-plan §7).
   - An expert's findings would force a spec change. Run `/smelt:spec datagen`
     first; if non-trivial, pause for the user.
   - A fix surfaces a pre-existing failure unrelated to Phase 3.

**Critical files (allowed to touch in this phase).** Anything within an
expert's scope per the table above, plus
`docs/plans/20260517-web-analytics-3-scaffold.md` (to record round counts and
the final clean status) and `docs/plans/20260517-web-analytics-example.md` (to
flip the overall-plan status row).

**Review checklist** (material findings only — applied to the expert-dispatch
*process*, not to a code diff):

- [ ] Both experts dispatched at least once.
- [ ] Every material finding either fixed or escalated; none silently dropped.
- [ ] Round count per expert recorded in "Deferred during implementation" below.
- [ ] No expert ran more than 3 rounds; if any did, `<<PAUSE_FOR_HUMAN>>` was emitted.
- [ ] `cargo fmt --all -- --check`, `cargo clippy --all-targets`,
  `cargo test -p smelt-cli --test example_diagnostics` (zero diagnostics for
  `examples/web_analytics/`), and the end-to-end integration test
  (`smelt-datagen --scale-factor 0.01 && setup_sources.sql && smelt build`)
  all green at end of phase.

**Acceptance gate.** Append a one-line summary to "Deferred during
implementation" of the form:

> Phase 5 expert review: sql-expert clean (R{n}), examples-curator clean (R{n}). No stop-the-line fired.

After acceptance gate: flip the overall-plan status row for Phase 3 in
`docs/plans/20260517-web-analytics-example.md` to `done` with today's date and
the latest commit SHA. Commit and push that change. Then emit
`<<PHASE_COMPLETE>>` as the autonomy loop's sentinel.

**Commit(s).** Per round, per expert with findings:
`review(web-analytics-3): address {expert-name} feedback`. The status-table
flip lands as: `chore(web-analytics-3): mark Phase 3 done in overall plan`.

---

## Deferred during implementation

(Append-only. Items surfaced during the work that we chose not to handle in this plan.)

**Phase 1: pool-snapshot test replaces row-level null-fraction check (expert-review fix).**
The first implementer pass wrote `test_datagen_joint_distribution_within_tolerance`, which checked null-fraction ≈ 25% ± 13pp in event rows. The expected value of 25% was wrong math: with weights [0.25, 0.60, 0.10, 0.05] and emits [1, 1, 3, 3], the average emit per draw is 1.30, so the anonymous pool-entry fraction is 0.25/1.30 ≈ 19.2% (not 25%). The 25% figure conflates draw weight with pool-entry fraction when shapes have different emit counts. The old test passed vacuously because 19.2% is within [25−13, 25+13].

Replaced with `test_pool_snapshot_anonymous_fraction_and_determinism`, which calls `LinkedPool::new` directly with seed 1337 (the explicit pool seed from `datagen.yaml`), counts `GenericValue::Null` entries in the `user_id` field, and asserts the fraction is 19.2% ± 3pp. A second pool build with the same seed validates the determinism guarantee. Per-category classification (single-owner vs shared-device vs multi-device-user) is not feasible from pool entries — the labels are not stored — so only the anonymous fraction is verified here; full category breakdown lives in the linked_choice plan's own tests.

**Phase 1: `datagen.yaml` anonymous shape declared first (schema ordering constraint).**
The spec says the Arrow schema for `linked_choice` columns is derived from the **first shape**'s field generators. The plan's shape ordering (60/25/10/5) placed the single-owner shape first, making `user_id` NOT NULL in the Arrow schema — meaning null values from the anonymous shape were silently coerced to `Int(0)` by the Parquet builder. Fix: the anonymous shape (which has `user_id: optional { prob: 0.0 }`) was moved to first position so the schema resolves `user_id` as OPTIONAL Int32. Shape declaration order is now 25/60/10/5 to satisfy the schema constraint. Weights in the YAML comments document the intended 60/25/10/5 distribution.

**Phase 1: non-anonymous shapes use bare `foreign_key` (expert-review fix).**
The first implementer pass wrapped non-anonymous shapes' `user_id` in `optional { prob: 1.0, inner: foreign_key }` to keep types "uniform across shapes". This is unnecessary: `build_schema` derives the Arrow type only from `shapes[0]` (spec invariant — the first shape is the type oracle), and the Parquet writer accepts `GenericValue::Int` into a nullable Int32 column without wrapping. Spec §Constraints invariant 7 says types must agree "modulo Optional wrapping", explicitly tolerating the mix. Fix: non-anonymous shapes use bare `{ type: foreign_key, dataset: users }`. The `datagen.yaml` inline comment explains the schema-ordering constraint and why the bare FK is valid at the Arrow layer.

**Phase 2: source yml per-column nullability is unrepresentable.** The smelt source schema yml format has no nullability field (verified by comparison with `examples/retail_analytics/models/sources/raw/*.yml`). The `user_id` column in `examples/web_analytics/models/sources/raw/events.yml` is nullable in data but typed as `INTEGER` without a nullability qualifier. The type system treats source columns as nullable by default in SQL semantics, so this has no functional impact for Phases 3–8. Tracked as a schema-format gap that would matter if smelt grew strict nullability checking against source schemas.

**Phase 2: partition column declared as `VARCHAR` in source yml (expert-review fix).** The partitioned Parquet writer emits the partition column as `DataType::Utf8` (`crates/smelt-datagen/src/generic_parquet.rs:128`), and the established convention in `examples/retail_analytics/models/sources/raw/{orders,web_events}.yml` is to declare partition columns as `VARCHAR`. The first Phase 2 pass declared `event_date: DATE`, which papers over the physical-vs-logical type with DuckDB's implicit cast at read time. Fix: `event_date: VARCHAR` with an inline comment; downstream silver (Phase 4) casts to DATE explicitly via `CAST(event_date AS DATE)` — same pattern as `retail_analytics/models/staging/stg_web_events.sql:18`.

**Phase 3: `functions` must NOT appear in `smelt.yml`'s `paths:` list.** The plan's Phase 1 `smelt.yml` template included `paths: [models, functions]`. When the `functions/` directory was empty this caused no harm (discover_models found no SQL files there). Once `parse_event_payload.sql` was added, `discover_models()` treated the function file as a regular SQL model and `smelt build` tried to execute the `smelt.define` declaration as raw DuckDB SQL — causing a parser error in `test_bronze_raw_events_view`. The fix: remove `functions` from `paths:`. `discover_function_files()` in `smelt-cli/src/discovery.rs` already hardcodes looking at `project_root/functions/` independently of `paths:`, so function discovery is unaffected. The `smelt.yml` for `examples/functions_demo/` (the reference implementation) similarly has only `paths: [models]`.

**Phase 4: smelt parser does not support `<expr> * INTERVAL 1 SECOND`.** `INTERVAL` is recognised as a typed-literal prefix (`INTERVAL '1 second'`) but is not a token usable in arithmetic-context expressions of the form `<int> SECOND` or `<expr> * INTERVAL 1 SECOND` (lexer has no `INTERVAL` keyword entry — verified in `smelt-parser/src/lexer.rs` and `expr.rs:1498` `is_typed_literal`). The silver model composes `event_ts` via DuckDB's `to_seconds(int) -> INTERVAL` helper: `CAST(event_date AS DATE) + to_seconds(seconds_in_day) AS event_ts`. Equivalent semantics; idiomatic in DuckDB. The plan's Phase 4 implementation-shape SQL with `* INTERVAL 1 SECOND` is therefore aspirational — the working form is `to_seconds(...)`.

**Phase 4: `smelt.ref('<name>')` is dead syntax in live SQL models.** A grep across `examples/*/models/` found zero live FROM-clause uses of `smelt.ref()` (the only matches were comments). The current convention is path syntax `smelt.<dir>.<stem>` — e.g. `smelt.sources.raw.events` (source binding), `smelt.bronze.raw_events` (bronze model), `smelt.staging.stg_orders` (per `retail_analytics`). The silver model uses `FROM smelt.bronze.raw_events`. The plan's Phase 4 review-checklist text "`smelt.ref('raw_events')` (not `smelt.sources.raw.events` directly) — the bronze view is the intended intermediate" remains correct in spirit (silver attaches to a named model, not the raw source) but the literal `smelt.ref` form is wrong. Tracked here so future plan authors do not propagate the stale syntax.

**Phase 4: `smelt.functions.parse_event_payload(payload)` is not callable in model SQL.** The silver model inlines `json_extract_string(payload, '$.event_name')` etc. for three flat VARCHAR columns instead of calling the Phase 3 function. Two blockers prevent the call: (a) smelt's struct-returning function expansion emits invalid DuckDB SQL — the wrapper produces `({...})` shape that DuckDB rejects; (b) the value-literal `{<expr> AS <field>, ...}` in the function body is parsed as a smelt meta-language record literal in the model context, leading to incorrect type inference (the outer wrapper inserts a `CAST(parsed AS DOUBLE)`). The `parse_event_payload` function file remains in the example as a forward-looking artifact demonstrating that smelt can DECLARE struct-returning functions (Phase 3's stated goal) and passes `example_diagnostics` cleanly; using the function from a model body is deferred until the struct-expansion + literal-disambiguation issues are resolved. Downstream models in Phases 5–8 reference `events_parsed.event_name`, `events_parsed.platform`, `events_parsed.url` directly (flat-column ergonomics) instead of `events_parsed.parsed.event_name`.

**Phase 4: workspace root `/data/` added to `.gitignore`.** Smelt-datagen invocations with relative `output:` paths land at the process CWD. The integration tests use temp dirs (`TempDir::new()` + `rewrite_outputs`), but ad-hoc CLI runs from the workspace root leave a `data/` directory there. The Phase 1 + Phase 4 implementer passes each leaked one. The root `.gitignore` already covered `examples/*/data/` but not a bare `data/` at the workspace root; this fix adds `/data/` with a comment explaining the trigger.

---

## Verification

How to confirm the scope is satisfied at the end of Phase 5:

- `cargo fmt --all -- --check` passes.
- `cargo clippy --all-targets` passes with zero warnings.
- `cargo test` passes — no regression in the workspace.
- `cargo test -p smelt-cli --test example_diagnostics` reports zero diagnostics for `examples/web_analytics/`.
- `cargo test -p smelt-datagen --test example_web_analytics` (the new integration test file) passes — covers datagen smoke, joint-distribution-within-tolerance, setup_sources, bronze view, parse function compile, and end-to-end build.
- Manual fresh-checkout dry run succeeds:
  ```bash
  smelt-datagen --config examples/web_analytics/datagen.yaml --scale-factor 0.01
  duckdb examples/web_analytics/target/dev.duckdb < examples/web_analytics/setup_sources.sql
  smelt build --project-dir examples/web_analytics --target dev
  ```
- Phase 5 acceptance gate met: both applicable expert reviewers (`sql-expert`, `examples-curator`) reported "no material findings" on final dispatch. No stop-the-line condition fired.
- The overall-plan status row for Phase 3 in `docs/plans/20260517-web-analytics-example.md` is flipped to `done` with date and commit SHA.
