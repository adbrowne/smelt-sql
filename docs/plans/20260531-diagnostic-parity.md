# Plan: Diagnostic Parity (analysis ↔ build) + function/meta codegen correctness

**Parent (master plan)**: `docs/plans/20260530-feature-sweep.md` — this is a **sub-plan** spawned from the feature sweep to remediate one cluster of its ledger findings: the "LSP-clean but unbuildable" bug class (BUG-006/011/013/015/018/019/024/032). The autonomy loop works this sub-plan phase by phase and rolls up to the master only when it is exhausted.

**Date**: 2026-05-31
**Spec**: `docs/specs/architecture.md` §"Diagnostic parity rule (analysis ↔ build)" (+ §"Run pipeline parity rule (CLI ↔ UI)"); `docs/specs/meta_language.md` (Surface: build-path execution, added in Wave C phases)
**Spec diff**: added §"Diagnostic parity rule (analysis ↔ build)"; sharpened the pre-execution gate reference in §"Run pipeline parity rule"; Known Divergences entry recording the current drift this plan closes.
**Tracking branch**: `worktree-test_features`
**Docs**: code+docs (each Wave C phase lands the `meta_language.md` Surface increment for the construct it executes).

## Execution prompt (for a fresh session / autonomy iteration)

Read this file. Run the next `pending` phase in the Progress-tracking table (skip `done` and `blocked` rows) using the per-phase routine below (pre-flight → spec increment if listed → red-green `/smelt:implement` on the phase's tests, spec as oracle, implementer + reviewer → verification gates → update the table row → commit + push with the phase's commit message). Emit exactly one sentinel: `<<PHASE_COMPLETE>>` (phase done), `<<PHASE_BLOCKED>>` (decision/off-target-red recorded; see §"Block conditions"), `<<SUBPLAN_ADVANCED>>` / `<<MASTER_EXHAUSTED>>` (sub-plan exhausted; see the loop's roll-up rule), or `<<ALL_DONE>>`. There is no hard-stop: a block is recorded and the loop continues to the next pending phase.

## Goal

Make `smelt build`/`run` enforce exactly what the analysis layer (the LSP/`file_diagnostics`) sees, and make every analyzer-accepted construct compile to valid engine SQL. This closes the "LSP-clean but unbuildable" bug class surfaced by the feature sweep: BUG-011, 013, 015, 018, 019, 024, 032 (bounded) and BUG-006 (the meta-language evaluator, multi-phase).

Two guarantees, per the spec rule:
1. **Parity gate** — before any model is compiled/executed, the build runs `file_diagnostics` over the selected models + in-DAG deps and fails fast on any `Error`-severity diagnostic, through one shared gate for both the CLI and `execute_project`. The blocking set is exactly `severity == Error` (not a code allow-list).
2. **Codegen honors the analyzer** — constructs the analyzer accepts (nested `smelt.define`, block `PASSING` fragments, in-model meta) compile to valid SQL rather than being emitted verbatim/dropped.

## Non-goals (deferred, tracked separately)
- Full CLI→`execute_project` migration (BUG-001). This plan reaches parity via a **shared gate helper** both run paths call; the single-entry-point migration is a later effort. The `execute_parity` CI gate named in the spec remains future work beyond the shared gate.
- Backend coverage beyond DuckDB.
- A post-expansion `analyze_sql_string` diagnostic query (a backstop). Source-level gating + correct codegen cover this plan's scope; revisit only if a gap remains.
- Demoting/retuning individual diagnostic *codes* beyond what the gate requires (only bump Warning↔Error where a phase's red-green test proves the verdict is wrong).
- **User-authored planner-rule *registration*** (the extensibility feature: a project shipping its own planner rule). The diagnostic surface, including planner rules, is *in scope* — every built-in rule is surfaced into `file_diagnostics` via the uniform rule → diagnostics interface (P2b) and gated like any other `Error` (§"Diagnostic parity rule (analysis ↔ build)" Scope, §"Planner scope"). What is out of scope is the mechanism for registering *new, non-built-in* rules; when that lands it reuses the same interface and inherits parity for free. BUG-011 is therefore in scope (surface the cumulative classifier into `file_diagnostics`), not deferred.

## Per-phase routine
1. **Pre-flight.** `cargo test --quiet 2>&1 | tail -40`. If red, check **what** is red: if the failure is this phase's own acceptance target (the example/test this phase exists to make green — e.g. P7d and the `meta_config` build), that is expected — **proceed**. If the red is unrelated breakage, treat it as a block (record + continue, per §"Block conditions"); do not build on a broken baseline.
2. **Spec increment** (only the phases that list one): edit the named spec section first; keep it timeless (no phase vocabulary in the spec body).
3. **Red-green `/smelt:implement`.** Write the phase's failing test(s) first, then the implementation, with the spec as oracle. Implementer pass, then reviewer pass (material findings only).
4. **Verify.** `cargo fmt --all`; `cargo clippy --all-targets` (zero warnings); `cargo test` green; plus the phase's specific gates: `example_diagnostics`, `example_workspaces`, `smelt-runtime` parity tests. For `example_builds`: run it **scoped to only the workspace(s) this phase touches** — `SMELT_EXAMPLE_BUILDS_ONLY="<ws1>,<ws2>" cargo test -p smelt-cli --test example_builds` — because a clean-copy build + DuckDB execution of the whole example set is the gate's dominant cost. The **full** sweep (var unset) runs only in P8 / CI; do not run it unscoped in P2–P7.
5. **Record + commit.** Update the status-table row to `done` + date; commit and push tests + impl + spec + table together with the phase's commit message. Emit `<<PHASE_COMPLETE>>` (or `<<ALL_DONE>>` on the last green phase).

## Block conditions (`<<PHASE_BLOCKED>>` — record and continue, no hard-stop)
When a phase hits one of the conditions below, **do not halt the loop**. Instead: (1) set the phase's status-table row to `blocked` with a one-line reason; (2) append a dated entry to §"Blocked phases" (phase id, the decision/reason, and the candidate options for the human); (3) restore the tree to a clean committed state; (4) commit + push the table + blocked-log update; (5) emit `<<PHASE_BLOCKED>>`. The next iteration skips the `blocked` row and picks the next `pending` phase; you review the blocked entries later and we resume them.

Conditions:
- The phase needs a design decision not answered by this plan or the spec (do not guess on architecture-invariant-touching choices).
- Pre-flight is red on **unrelated** breakage (not this phase's own acceptance target — see routine step 1).
- The tree can't be returned to green after the phase (the implementation is incomplete/unsound and would leave a broken baseline).

## Progress tracking

| Phase | Title | Status | Closes | Commit | Date |
|-------|-------|--------|--------|--------|------|
| P1 | `example_builds` harness (build+execute every example; discover & allow-list the currently-unbuildable, each with a logged reason) | done | — (structural) | | 2026-05-31 |
| P2 | Shared `Error`-severity diagnostic gate, wired into both the CLI run path and `execute_project` | done | BUG-015, 019, 024 | feat(runtime): gate build on all Error-severity diagnostics via one shared CLI/UI helper | 2026-06-01 |
| P2b | Uniform planner rule → diagnostics interface; surface built-in rules (cumulative classifier; incremental batch-safety/bounds) into `file_diagnostics` | done | BUG-011 | feat(planner,db): surface built-in planner-rule diagnostics via a uniform rule→diagnostics interface in file_diagnostics (closes BUG-011) | 2026-06-01 |
| P2c | Source-diagnostics producer: surface malformed per-entity source YAML (`MalformedSource`) into the analyzer surface, gated like any `Error` and published by the LSP | done | BUG-032 | feat(db,runtime,lsp): surface malformed per-entity source YAML as MalformedSource diagnostics (closes BUG-032) | 2026-06-01 |
| P3 | BUG-013: expand nested `smelt.define` calls to a fixpoint | done | BUG-013 | fix(dialect): expand nested smelt.define calls to a fixpoint (closes BUG-013) | 2026-06-01 |
| P4 | BUG-018: thread block `PASSING` fragment bindings through substitution | done | BUG-018 | fix(dialect): bind block PASSING fragment arguments in substitution (closes BUG-018) | 2026-06-02 |
| P5 | BUG-006a: in-model list spread executes at build (meta_lists) | done | BUG-006 (lists) | feat(runtime): evaluate in-model list spread at compile time (closes BUG-006 lists) | 2026-06-02 |
| P6 | BUG-006b: HOFs/pipe/lambda + ternary + `config.var` execute at build (meta_hofs, meta_polish) | done | BUG-006 (hofs) | feat(db,runtime): forbid bare List<T> in scalar position + evaluate HOFs/pipe/lambda/ternary/config.var at compile time (closes BUG-006 hofs) | 2026-06-02 |
| P7a | BUG-006c: `smelt.columns_of` reflection executes at build (meta_columns) | done | BUG-006 (columns_of) | feat(runtime): evaluate smelt.columns_of reflection at compile time (closes BUG-006 columns_of) | 2026-06-02 |
| P7b | BUG-006c: wide reflection (`smelt.models.with_tag` / `smelt.sources.with_tag`, `.all`) executes at build (meta_workspace) | done | BUG-006 (wide reflection) | feat(runtime): evaluate wide reflection (smelt.models/sources) at compile time (closes BUG-006 wide reflection) | 2026-06-02 |
| P7c | BUG-006c: loader detector (bare `List`/`Map` loader in scalar position → `MetaListInScalarPosition`) + List-loader build-path lowering (meta_config List form) | done | BUG-006 (List loader) | feat(runtime): forbid bare List/Map config loaders in scalar position (closes BUG-006 loader consumption) | 2026-06-02 |
| P7d | BUG-006c: Map-loader consumption — parse a Map method call on a loader-call receiver, lower the loader + Map method call at build, wire `MAP_METHOD_CALL` into `file_diagnostics`, rewrite the `meta_config` example models to consuming forms, remove `meta_config` from `KNOWN_UNBUILDABLE` | done | BUG-006 (Map loader) | feat(parser,runtime,dialect): lift MAP_METHOD_CALL to postfix position + lower Map<> loader at build + suppress SMELT_RECORD_DECL in printer (closes meta_config) | 2026-06-03 |
| P8 | Close-out: `example_builds` allow-list empty (only intentional `*_broken_*` remain); remove the resolved Known Divergence from architecture.md; update ROADMAP + bug ledger statuses | done | — | docs(diag-parity): close out — bug ledger + spec + roadmap updated (P8) | 2026-06-03 |

**P7 split (2026-06-02).** P7 originally bundled three independent reflection/loader constructs (`columns_of`, wide reflection, loader) into one phase. During execution each proved comparable in size to a full P5/P6, and `columns_of` has a pass-ordering wrinkle the single-phase framing did not anticipate (the build-path meta evaluator runs *before* the printer's `smelt.define` body expansion, so a `columns_of` inside a function body — the `meta_columns` `coalesce_numeric` case — is only visible after function substitution). Mirroring the earlier P2 → P2/P2b/P2c split, P7 is split into **P7a** (`columns_of`), **P7b** (wide reflection), **P7c** (loader). Additionally, the original P7 acceptance ("remove `meta_columns`/`meta_workspace`/`meta_config` from `KNOWN_UNBUILDABLE`") is partly infeasible: `meta_columns` and `meta_workspace` read **unseeded sources** (`raw.orders` / `raw.events`) and fail on their first model regardless of reflection lowering (same category as `meta_lists` in P5), so they stay on the allow-list with a reclassified reason and a **hermetic e2e** as the regression target — exactly the P5/P6 disposition.

**P7c split (2026-06-03).** P7c's original step (4) — "rewrite the `meta_config` example models to a consuming form" — assumed both the `List` and `Map` loaders had an in-model consumer. The `List` loader does (`reduce(map(load_yaml(...), …), …)`), and that path plus the bare-loader detector landed in `58c2fcd4` (proven by the `List` case in `crates/smelt-cli/tests/meta_config_e2e.rs`). The `Map` loader did **not**: a `Map<K, V>` value's consumer is the closed Map API (`m.entries()` / `m.keys()` / …) already specified in `meta_language.md` §"Maps" and partly implemented (the `MAP_METHOD_CALL` parser node, `type_inference/record.rs`), but (i) the parser emits `MAP_METHOD_CALL` only for a bare-identifier receiver, so `load_yaml(...).entries()` does not parse, and (ii) the loader family is not lowered at build. The surface is therefore **already designed — the work is implementation, not a new free-function surface** (an early free-function design was discarded; see `docs/research/2026-06-03-map-consumption-surface.md`). So P7c is marked `done` for the detector + List-loader build path, and the Map-loader half — method-receiver parsing, build-path lowering, `file_diagnostics` wiring, and the example greening that makes pre-flight green — is split into **P7d**. P7c and P7d together close BUG-006 (loader); `meta_config` comes off `KNOWN_UNBUILDABLE` at the end of P7d.

**Status values**: `pending`, `done`, `blocked`. A phase is `done` only when its tests are red-green confirmed and all gates (incl. `example_builds`) are green. A phase is `blocked` when it hit a §"Block conditions" trigger and was recorded for human review (the loop skips `blocked` rows and continues); a blocked phase has a dated entry in §"Blocked phases" and returns to `pending` once the human resolves the decision.

## Blocked phases

Append-only log of phases the autonomy loop recorded as `blocked` (a design decision, unrelated red baseline, or an implementation it could not land green) and continued past. Each entry: date, phase id, the reason/decision, and the candidate options for the human. Resolve an entry by making the decision, setting the phase row back to `pending`, and striking the entry. *(None yet.)*

## Phase detail

### P1 — `example_builds` harness
- **Spec**: none (the spec's CI-gate sentence already names `example_builds`).
- **Tests (red-green)**: new `crates/smelt-cli/tests/example_builds.rs`. For every `examples/*` workspace, run `smelt build` (compile **and** execute on DuckDB). A workspace that builds clean must pass; a workspace on the `KNOWN_UNBUILDABLE` allow-list is skipped **with its failure reason logged** (no silent skip); a `*_broken_*` workspace asserts its expected `Error` diagnostic at the gate rather than executing. Land green: discover the currently-unbuildable set empirically (expect `functions_demo`, `meta_lists`, `meta_hofs`, `meta_columns`, `meta_config`, `meta_polish`, `meta_workspace`) and allow-list them with the observed error. Later phases remove entries.
- **Commit**: `test(diag-parity): add example_builds gate (build+execute every example) with logged known-unbuildable allow-list`

### P2 — Shared `Error`-severity diagnostic gate
- **Spec**: `architecture.md` §"Diagnostic parity rule" (already added) is the oracle.
- **Tests (red-green)**: pick `*_broken_*` fixtures whose error is **not** `UnknownSmeltFn` — at least one each for the codes behind BUG-019 (`CteCycle`), BUG-024 (`MalformedTimeseries`), BUG-015 (a loader content-validation code), BUG-032 (a malformed per-entity source). Each test asserts `smelt build` exits non-zero **and** names the expected `DiagnosticCode`, where today it exits 0 / mis-builds. Add a positive test: a clean fixture still builds. Confirm every existing example remains buildable (they are analysis-clean). (The cumulative classifier codes behind BUG-011 are added to `file_diagnostics` in P2b, then flow through this same gate.)
- **Implementation**: a shared helper `smelt_runtime::gate_diagnostics(db, workspace, &files) -> Result<(), Vec<GateDiagnostic>>` (in `crates/smelt-runtime/src/gate.rs`) that aggregates the **full LSP surface** — `file_diagnostics` **plus** `check_type_diagnostics::accumulated` (matching `smelt-lsp`'s `diagnostics_for`) — over each file, keeps `severity == Error`, and resolves each range to 1-based `(line, col)` once via `line_index::LineIndex` (the build gate's single boundary; Diagnostic range encoding rule). `GateDiagnostic`/`format_gate_errors` emit `path:line:col: error[Code]: message`. Wired into **both** run paths: the CLI runs it *before* dependency validation (so a loader/timeseries/scoping defect is rejected by its real code, not a downstream "undefined ref" or DuckDB binder error) over a gate DB built from the **discovered** files — raw SQL models incl. `*.gen.sql` generator files (so generator-emitted refs resolve through `emitted_models`) plus function-definition files (so `CteCycle`-style function-body diagnostics are covered) — **replacing** the `UnknownSmeltFn`-only block at `run.rs`; `execute_project` calls the same helper at the top over its selected models. Side effect of parity: `per_cohort_union_broken_emission_body_collision_suppression` (a `ModelDefDuplicateName` `Error`) no longer builds clean — it moves out of `example_builds`' `BROKEN_BUILDS_CLEAN` into the `broken`-analyzer-Error category.
- **BUG-032 split out → P2c.** The plan assumed `MalformedSource` already surfaced in `file_diagnostics` (gating-only). It does not — `MalformedSource` has **zero producers**; per-entity source `.yml` files are not registered as `SourceFile`s, and `discover_source_infos` swallows parse errors (`crates/smelt-core/src/sources.rs:272` `Err(_) => continue`). Closing BUG-032 needs a *new* source-diagnostics producer, not just the gate, so it is its own phase (P2c) — mirroring the earlier P2/P2b split for BUG-011. The gate built here catches BUG-032 for free once P2c's producer emits the `Error`.
- **Commit**: `feat(runtime): gate build on all Error-severity diagnostics via one shared CLI/UI helper (closes BUG-015/019/024)`

### P2c — Source-diagnostics producer (BUG-032)
- **Spec**: `architecture.md` §"Diagnostic parity rule" (analysis must see what the build's source discovery sees) + §"Unknown-key doctrine" (per-entity source YAML is strict). Per-entity source discovery is the lazy Salsa `project_sources` path (§"Workspace loading parity rule").
- **Context**: `MalformedSource` (`diagnostics_types.rs:21`) has no producer; `parse_source_yaml` (`sources.rs:157`) can fail with `Io / YamlParse / MissingColumns / MaterializationForbidden / UnknownType / InvalidNameOverride`, all discarded by `discover_source_infos` at `sources.rs:272`. A malformed per-entity source with no consumer builds at exit 0 today (LSP-clean); with a consumer it fails downstream with a misleading "schema does not exist".
- **Tests (red-green)**: a `*_broken_*` per-entity-source fixture (bad/missing type, missing `columns:`, `materialization:` present) produces a `MalformedSource`-mapped `Error` via `file_diagnostics`/the diagnostics API where today it is diagnostic-clean; the same fixture then fails `smelt build` at the P2 gate naming the code (parity end-to-end); a valid per-entity source stays clean in both surfaces.
- **Implementation**: a new analysis-pure source-diagnostics producer in `smelt-db` (a project-scoped query over the per-entity source YAML the workspace already discovers, mapping each `SourceError` variant to a `MalformedSource` diagnostic with a range anchored in the offending file) merged into the analyzer surface the gate and LSP consume. Keep the parse/validation logic in `smelt-core::sources`; the query only aggregates (Salsa purity rule).
- **As-built notes**:
  - **smelt-core**: factored the source/sidecar disambiguation walk into a private `candidate_source_yaml_files` shared by `discover_source_infos` (parses → `SourceInfo`) and the new `discover_source_errors` (collects `(PathBuf, SourceError)` for files that fail `parse_source_yaml`). The walk, sibling-`.csv` skip, address recomputation, and sort order are byte-for-byte the prior behaviour.
  - **smelt-db**: new `#[salsa::tracked] project_source_diagnostics(project) -> Arc<Vec<SourceDiagnostic>>` (keyed on `ProjectInput`, restart-scoped like `project_sources`). It only aggregates — validation stays in `smelt-core`. **Code mapping honours the spec, not the plan's "all → MalformedSource":** `sources.md` §"Diagnostic codes" splits an unrecognised column type out as `SourceTypeError`, so `SourceError::UnknownType → SourceTypeError`; every other variant (`Io`/`YamlParse`/`MissingColumns`/`MaterializationForbidden`/`InvalidNameOverride`) → `MalformedSource`. Range anchored at the file head (offset 0).
  - **Gate (smelt-runtime)**: `gate_diagnostics` now also gates the sources of every project that owns at least one gated file (so `--select` of a model enforces its project's sources, but an unrelated project in a multi-project workspace is not dragged in). Offset-0 ranges resolve to `(1, 1)` unconditionally.
  - **LSP parity (smelt-lsp)**: the editor half of the parity rule needed wiring — `.yml` source files are not tracked `SourceFile` inputs, so they never flowed through the per-file `diagnostics_for` path and a malformed source was build-refused but editor-green (the inverse parity gap §"Diagnostic parity rule" forbids). Added `Backend::publish_source_diagnostics`, published project-scoped to each offending `.yml`'s own URI at `initialized` (**before** the `register_capability` round-trip, which awaits a client reply). Source discovery is restart-scoped, so a single startup publish is its lifecycle.
  - **Fixture cleanup**: the producer surfaced a genuinely-malformed, **unused** file — `examples/meta_config/models/sources/sources.yml` used the obsolete aggregate `sources:`/`tables:`/`schema:` format (no top-level `columns:`) and was referenced by nothing. Removed it (the empty `sources/` dir with it); `meta_config` is clean again on both surfaces.
  - **Tests**: `crates/smelt-cli/tests/source_diagnostics.rs` (analysis-layer query + end-to-end `smelt build` refusal naming the code + a valid-source clean case) and `sources_broken_malformed_surfaces_via_lsp` in `crates/smelt-lsp/tests/example_workspaces.rs` (LSP publishes `malformed-source` on the `.yml` URI). New fixture `examples/sources_broken_malformed/` (a `materialization:`-bearing source + a trivial probe model).
- **Commit**: `feat(db,runtime,lsp): surface malformed per-entity source YAML as MalformedSource diagnostics (closes BUG-032)`

### P2b — Uniform planner rule → diagnostics interface
- **Spec**: `architecture.md` §"Planner scope" (rule → diagnostics interface) + §"Diagnostic parity rule" Scope (both already added) are the oracle.
- **Context**: the cumulative classifier is `smelt_planner::classify_cumulative` (`crates/smelt-planner/src/rules/cumulative.rs:227`), returning `Result<CumulativeClassification, Vec<CumulativeDiagnostic>>`. `smelt-db` already depends on `smelt-planner` (compile-time edge; the reverse is dev-only), so `file_diagnostics` can call it directly — no new dependency. Today the classifier runs only on the build/dispatch path (`smelt-runtime` via `smelt_planner::classify_cumulative`), so a malformed cumulative model is LSP-clean (BUG-011).
- **Tests (red-green)**: (1) a `cumulative_*_broken_*` fixture (e.g. missing GROUP BY / forbidden aggregate) now produces its `CumulativeDiagnostic`-mapped code via `smelt_db::file_diagnostics` — assert in an `smelt-db`/`example_diagnostics`-style test where today the model is diagnostic-clean. (2) The same fixture fails `smelt build` at the P2 gate naming that code (parity end-to-end). (3) A valid cumulative example stays clean in both surfaces.
- **Implementation**: introduce a uniform rule → diagnostics interface in `smelt-planner` (a `detect`-shaped function/trait each built-in rule implements, returning `Vec<Diagnostic>` or rule-native diagnostics mappable to `smelt_db::Diagnostic`). Route the cumulative classifier and the incremental batch-safety/bounds analyzers through it. Have `file_diagnostics` invoke the built-in rules over each model and merge their diagnostics, mapping each to its correct severity (the cumulative classifier's reject codes are `Error`; advisory incremental findings stay `Warning`). Keep the checks in the rule (analysis-pure); `file_diagnostics` only aggregates. The runtime continues to call the same classifier, so the build/dispatch verdict is unchanged — it is now *also* visible to the editor.
- **As-built notes**:
  - New `smelt_planner::rule_diagnostics` module: `PlannerRule` trait + `RuleContext`/`RuleDiagnostic`/`RuleDiagnosticCode`/`RuleSeverity` + `detect_builtin_rules` + the shared `collect_path_refs` (moved out of `smelt-runtime::cumulative` so the runtime dispatch and the analysis gate share one ref scanner). `CumulativeRule` → `Error`; `IncrementalRule` → `Warning` (the build path uses `analyze_batch_safety`, which never hard-refuses, so advisory `Warning` is the parity-correct severity — `incremental::detect` is not a build gate). 9 new `DiagnosticCode` variants + LSP code-string arms.
  - `file_diagnostics` builds the cumulative driving-source `smelt.<path> → timeseries` map by resolving each ref (handles single / multi-section / generator-emitted models, mirroring the runtime's graph-derived map) to avoid a spurious `CumulativeNoDrivingSource`.
  - **Gate-scoping correction (CLI `run.rs`)**: surfacing the cumulative `Error` exposed that the P2 CLI gate ran over the *whole* discovered project, so `--select`-ing a clean model was refused by an unrelated broken model. Per §"Diagnostic parity rule" ("selected models + in-DAG deps"), the gate was moved to after `execution_order` is computed and scoped to that set (+ all function-definition and `*.gen.sql` generator files, always); `graph.validate()` now runs *after* the gate so an analyzer `Error` is still reported by its real code before any downstream dependency error (preserves BUG-015/019). `smelt build` (no `--select`) is unchanged (all models selected). `execute_project` (UI path) already scoped to selected models.
  - Known limitation (deferred): materialization detection keys off frontmatter (`metadata.materialization` / `metadata.incremental`); a `cumulative_aggregate`/`incremental` set only in `smelt.yml` is not yet surfaced in the editor (under-reporting, not a false positive).
- **Commit**: `feat(planner,db): surface built-in planner-rule diagnostics via a uniform rule→diagnostics interface in file_diagnostics (closes BUG-011)`

### P3 — BUG-013 nested `smelt.define` fixpoint
- **Tests (red-green)**: build+execute a nested-composition example (`examples/functions_demo/models/uses_nested_helpers.sql`, or a minimal 3-level chain) and assert correct output, where today DuckDB errors `Catalog "smelt" does not exist`. Remove the relevant entry from `KNOWN_UNBUILDABLE` if `functions_demo`'s only blockers were 013/018.
- **Implementation**: make function-call expansion reach a fixpoint — re-expand nested `SMELT_PATH_CALL` nodes left in a substituted body (thread the expanders through `substitute_params_with_named`, or loop the printer pass until stable). See `crates/smelt-dialect/src/printer.rs:205-214`, `crates/smelt-runtime/src/compile.rs:255-297`.
- **As-built notes**:
  - **Root cause confirmed by parser probe**: the printer's body-reparse (`printer.rs` `SMELT_PATH_CALL` branch) called `smelt_parser::parse(&expanded)` on the substituted body, but the body text spans the function's wrapping parens (`(… + 1)` / `(SELECT …)`) and the parser only emits `SMELT_PATH_CALL` nodes inside a *statement* context — never at the top of a bare/parenthesised fragment. So the re-expansion pass was a silent no-op and the inner `smelt.functions.*` reached DuckDB verbatim.
  - **Fix** (printer-only, keeps the existing text-substitution emission mechanism — the larger "route through the planner's AST-level expansion" option a′ was deliberately not taken): new `reexpand_call_body(expanded, ctx)` helper wraps the body in a synthetic `SELECT ` prefix, reparses (now nested path-calls *are* recognised), prints through `ctx` (the existing `print_node` recursion is the fixpoint, depth-bounded for free by the upstream `FunctionCallCycle` Error gate), and strips the `SELECT ` prefix. Wired into **both** the FROM-position (`(…) AS __smelt_t{offset}`) and scalar `SMELT_PATH_CALL` branches. The synthetic outer `SELECT` never carries `QUALIFY`, so it never routes through `print_select_with_qualify_rewrite`; the prefix is always present (asserted via `.expect`, not a silent fallback).
  - **Tests**: `e2e_nested_define_chain_executes` (3-level scalar chain `outer_call → middle → inner_unary`, asserts materialised arithmetic) and `e2e_nested_table_fn_chain_executes` (FROM-position `wrap_tbl → passthru` identity chain, asserts materialised rows) in `crates/smelt-cli/tests/functions_e2e.rs`; `nested_define_chain_expands_to_fixpoint` printer unit test in `crates/smelt-dialect/src/printer.rs`. Both e2e tests confirmed red on `git stash` of the printer fix (DuckDB `Catalog "smelt" does not exist`).
  - `functions_demo` stays on `KNOWN_UNBUILDABLE` — its remaining blockers are the block-`PASSING` fragment (P4) and the provenance `n:`-family frontmatter, not BUG-013.
- **Commit**: `fix(dialect): expand nested smelt.define calls to a fixpoint (closes BUG-013)`

### P4 — BUG-018 block `PASSING` fragments
- **Tests (red-green)**: build+execute `examples/functions_demo/models/rollup_with_passing.sql` and assert correct output, where today the fragment is emitted as its bare name/default → DuckDB error. Remove `functions_demo` from `KNOWN_UNBUILDABLE`.
- **Implementation**: collect `path_call.passing_clauses()` in the printer, extend the `SmeltPathCallExpander` signature with a PASSING-binding slot, and merge PASSING bindings into substitution alongside named args. See `crates/smelt-runtime/src/compile.rs:134-146`, `crates/smelt-dialect/src/printer.rs:144-175`, `crates/smelt-parser/src/ast.rs:726`.
- **As-built notes**:
  - **Expander signature unchanged** (deviation from the plan's "extend the signature with a PASSING-binding slot"). A `PASSING <name> AS (<body>)` clause is semantically identical to a `<name> => <body>` inline named argument — both bind a parameter by name, and the type-checker (`function_body_check.rs:1213-1232`, `UnknownPassingParameter`) already rejects supplying the same parameter both ways, so there is no collision to disambiguate. Merging PASSING bindings into the existing `named` vec inside the printer's `SMELT_PATH_CALL` branch is the minimal correct fix; the `SmeltPathCallExpander` interface and its three `compile.rs` call sites are untouched, and the existing `substitute_params_with_named` binds the merged set unchanged.
  - **Printer-only change** (`crates/smelt-dialect/src/printer.rs`): after building `named` from `arg_list().named_params()`, iterate `path_call.passing_clauses()` and push `(clause.name(), <printed body>)`. The body is printed through `ctx` (via `clause.body_expr()`, falling back to `clause.body_text()` on error-recovery) so nested smelt constructs inside the fragment expand too — matching how inline named-arg values are printed.
  - **Tests**: `e2e_block_passing_fragment_executes` in `crates/smelt-cli/tests/functions_e2e.rs` (hermetic project: a `SelectItems<Agg>` fragment param supplied via `PASSING metrics AS (COUNT(*))`, build+execute on DuckDB, assert per-category counts; confirmed red before the fix — DuckDB `syntax error at or near ")"` from the unsubstituted `metrics` defaulting to `()`). Printer unit test `block_passing_binds_fragment_into_named_args` asserts a PASSING clause arrives in the expander's `named` set rather than falling back to the default. Note: the PASSING body parser (`smelt_ext.rs:1148-1156`) accepts a single expression — no `AS alias` inside the body — so the fragment is bound unaliased, matching `examples/functions_demo/models/rollup_with_passing.sql`.
  - **`functions_demo` stays on `KNOWN_UNBUILDABLE`** (deviation from the plan's "Remove `functions_demo`"). With PASSING (P4) and nested `smelt.define` (P3) both fixed, the example's remaining blocker is unrelated to diagnostic parity: `uses_generics` lowers to SQL DuckDB rejects with `Parser Error: NameListToString NOT IMPLEMENTED` (a generics/registry codegen gap), plus unknown `provenance`/`deterministic` frontmatter fields surface as warnings. The allow-list reason was rewritten to record the actual current failure; de-listing waits on the separate generics-codegen fix. The hermetic e2e test is the BUG-018 regression target instead of the full example.
- **Commit**: `fix(dialect): bind block PASSING fragment arguments in substitution (closes BUG-018)`

### P5 — BUG-006a list spread executes at build
- **Spec**: `meta_language.md` Surface — list spread is evaluated at compile time and emits plain SQL columns/items.
- **Tests (red-green)**: build+execute `examples/meta_lists` and assert the expanded SELECT output (columns + rows). Negative `meta_lists_broken_*` fixtures continue to produce their diagnostics at the gate. Remove `meta_lists` from `KNOWN_UNBUILDABLE`.
- **Implementation**: a compile-time meta-expansion pass (CST→CST rewrite) run in `compile.rs` before printing, with a `MetaEvalContext` (type context + upstream schemas + config), starting with `LIST_SPREAD` of list literals/variables in SELECT lists. Reuse `smelt-db`'s spread validation (`type_inference/hof.rs:check_select_list_spreads`) for shape/typing; add the *expansion* it lacks.
- **As-built notes**:
  - **Pass location & shape (deviation from "CST→CST rewrite with `MetaEvalContext`").** The build-path expander is a **pure text rewrite** in a new `crates/smelt-runtime/src/meta_eval.rs` (`expand_in_model_meta` → `expand_select_list_spreads`), matching the codebase's established "expand smelt constructs by producing SQL text, then reparse/print" idiom (cf. `printer.rs::reexpand_call_body`, `compile::expand_function_calls`) rather than building a new green tree. For each `SELECT_LIST` containing a `LIST_SPREAD`, the list is rebuilt from its child nodes — items kept verbatim, an inline-list-literal spread `...[e_1,…,e_n]` contributing its element texts, an empty `...[]` contributing nothing (commas elide, rule 7) — and the node's text range is spliced (surrounding whitespace preserved). Idempotent (a second pass finds no spread) and guarded by a cheap `contains("...")` short-circuit so non-meta models pay no reparse. **`MetaEvalContext` is deferred to P6**: list-*literal* spread is self-contained (no type context / schema / config needed to expand), and adding unused context fields now would trip the zero-warning clippy gate. P6 (lambda binding, `config.var`) is where the eval context first earns its keep. **Reuse of `check_select_list_spreads`**: validation already runs in the analysis layer and flows through the P2 gate before compile, so the build pass only *expands* — it does not re-validate (keeping Salsa-pure validation in `smelt-db`, not duplicated in the runtime). Non-literal spread operands (a `List<T>` variable) and nested-spread literals are left verbatim (P6+ scope), as is any SELECT list with non-item/non-spread children (defensive).
  - **Wiring — five compile entry points, not the obvious three.** `SqlCompiler` parses user SQL in five methods; `smelt build`/`run` for a plain `FullRefresh` model goes through `compile_with_ephemerals` (CLI `run.rs:1336`), **not** `compile_with_sql_and_ephemerals` (that is the cumulative / incremental path). The expander is invoked at the top of all five — `compile`, `compile_with_sql`, `compile_with_sql_and_ephemerals`, `compile_with_ephemerals`, and the private `compile_ephemeral` — so every path that prints a model first lowers its spreads. (Initial wiring missed `compile_with_ephemerals`; the e2e caught it — the spread reached DuckDB until that fifth site was wired.)
  - **`meta_lists` stays on `KNOWN_UNBUILDABLE` (deviation from the plan's "Remove `meta_lists`").** With spread expansion landed, `meta_lists` no longer fails on the spread — `smelt build` now emits `SELECT id, 1, 2, 3 FROM raw.users` — but its models read `smelt.sources.raw.users`, an **unseeded source** in the standalone build env (the harness builds a clean copy with no `setup_sources`/datagen), so the build now fails with `Catalog Error: Table "raw.users" does not exist`, the same category as `timeseries`/`web_analytics`/`test_workspace`. The allow-list reason was reclassified to record this; de-listing is a seeding decision for P8, not a codegen gap. **Separately discovered, latent, orthogonal**: the `list_literal` model's bare integer-literal columns (`1, 2, 3`) trip a pre-existing `apply_type_casts` defect — the wrapper names unaliased non-identifier columns `_colN` and references them, but the inner subquery does not expose those names (DuckDB `Binder Error: Column "_col2" … cannot be referenced before it is defined`). This reproduces with **no spread at all** (a plain `SELECT id, 1, 2, 3 FROM t`), so it is tracked apart from diagnostic parity and would surface only once `raw.users` is seeded.
  - **Tests (red-green)**: `meta_eval` unit tests (9) in `crates/smelt-runtime/src/meta_eval.rs` cover the four spread shapes (literal, column-ref, multi, empty-elision), idempotency, non-literal pass-through, the `contains("...")` guard, and nested SELECT lists. Hermetic e2e `crates/smelt-cli/tests/meta_lists_e2e.rs` (2 tests) stages a self-contained project (a `VALUES` base model + column-ref spreads), `smelt build`s it, and asserts the materialised columns (via `information_schema`) and rows on DuckDB. Both e2e tests were confirmed **red** before the fix — `Parser Error: syntax error at or near ".."` from the un-expanded `...` reaching DuckDB. (Execution coverage uses column-ref spreads, which fully execute; literal-spread execution is blocked only by the orthogonal `apply_type_casts` defect above, so the literal case is covered by the unit test's compiled-text assertion rather than e2e.)
  - **Spec increment**: `meta_language.md` §"Lists and spread → Spread operator" gains a timeless **Build-path execution** paragraph (the compile pipeline materialises an accepted spread into plain select items before codegen, per §Semantics rule 10 and `architecture.md` §"Diagnostic parity rule"); a Known Divergence records that build-path lowering currently covers list spread only (HOFs/pipe/lambda/ternary/`config.var`/reflection are analyzer-validated but not yet lowered — P6/P7).
- **Commit**: `feat(runtime): evaluate in-model list spread at compile time (closes BUG-006 lists)`

### P6 — BUG-006b HOFs/pipe/lambda + ternary + config.var
- **Design decision (resolved 2026-06-02, Andrew)** — *bare `List<T>` as a complete Data-World scalar / SELECT-item position*: **forbid it.** A meta `List<T>` value that reaches a Data-World scalar or select-item position **without being consumed** by spread / HOF / reducer / record / map / generator is an `Error`-severity diagnostic (`MetaListInScalarPosition`). This is itself a parity under-report: §Semantics "Lists and spread" rule 10 already requires every list to be consumed, and the explicit `...xs` spread exists precisely so lists never auto-materialise into comma positions; option (B) implicit-spread and (C) defer were rejected. The spec increment must (1) add the rule + diagnostic to `meta_language.md` (§Semantics "Lists and spread" near rule 10, and the diagnostics table) and resolve the corresponding Open Question, and (2) note that the Data-World select-shape check must run even for FROM-less models (today `check_types.rs` early-returns with no FROM, which is why `pipe_rewrite.sql` was clean by accident). Then rewrite `examples/meta_hofs/models/pipe_rewrite.sql` so its list is consumed (e.g. `SELECT reduce([1,2,3] |> filter(fn c => c > 0) |> map(fn c => c * 2), plus_chain)`) and keep it a build+execute fixture; add a `*_broken_*` (or in-fixture) negative case asserting `MetaListInScalarPosition` fires.
- **Spec**: `meta_language.md` Surface — HOFs (`map`/`filter`/`reduce`), pipe, lambdas, ternary, and `smelt.config.var` evaluate at compile time.
- **Tests (red-green)**: build+execute `examples/meta_hofs` and `examples/meta_polish`; assert outputs. Negative `*_broken_*` fixtures still gate. Remove both from `KNOWN_UNBUILDABLE`.
- **Implementation**: extend the meta-eval pass with lambda binding + fold/map/filter evaluation, pipe desugaring, ternary, and compile-time `config.var` resolution from `smelt.yml`.
- **As-built notes**:
  - **Analysis half — `MetaListInScalarPosition` lives in `check_file_diagnostics`, not `check_types.rs`.** The plan said to fix `check_types.rs`'s FROM-less early-return; instead the new check was added to the meta walk in `crates/smelt-db/src/lib.rs::check_file_diagnostics` (the SELECT_ITEM loop), which **already runs regardless of FROM** — so the observable requirement ("the select-shape check runs for FROM-less models") is met without touching the early-return. Detection (`select_item_yields_bare_list`) is structural: a bare array literal, a top-level `map`/`filter` call, or a pipe whose outermost call is `map`/`filter` (these yield `List<U>`); `reduce` collapses to a scalar and is clean. Suppressed when another meta diagnostic already flagged the item (`flagged_meta_ranges` intersect) so a malformed item never double-emits (verified against `meta_lists_broken_empty_unknown`/`heterogeneous` and `meta_hofs_broken_pipe_rhs_not_call`). New `DiagnosticCode::MetaListInScalarPosition` + LSP code-string `meta-list-in-scalar-position`; added to `PHASE_A_CODES`. New fixture `examples/meta_lists_broken_list_in_scalar_position/` (FROM-less `SELECT [1, 2, 3]`).
  - **`meta_workspace` collateral (P7 examples).** The design decision (forbid bare `List<T>` in scalar position) made two previously-"clean-by-accident" reflection models (`audit_sources.sql`, `all_cohorts.sql` — bare `map(smelt.…with_tag(…), fn x => x.name)`) into `MetaListInScalarPosition` errors. Both were rewritten to consume the list with a spread (`SELECT ...map(…)`), keeping `meta_workspace` analysis-clean (it stays on `KNOWN_UNBUILDABLE` for P7 reflection).
  - **Build half — pure text-rewrite evaluator in `crates/smelt-runtime/src/meta_eval.rs`** (matching P5's idiom, not a CST→CST `MetaEvalContext` green-tree pass). Three guarded passes: (1) `expand_config_vars` resolves every `smelt.config.var('x')` globally to its `Text` literal using the analyzer's own `smelt_db::config_vars::{parse_vars_from_yaml, coerce_yaml_scalar_to_text}` (parity); (2) `expand_select_item_meta` evaluates each meta-valued SELECT item (`reduce` fold, ternary, pipe/HOF) to plain SQL; (3) the existing `expand_select_list_spreads`. A recursive `eval_meta` returns `List`/`Scalar`; `apply_hof` handles `map` (lambda substitution via token-level `substitute_ident`), `filter` (predicate const-eval), and `reduce` (`fold_reducer` renders each reducer's left-fold). A small constant evaluator (`eval_const`/`apply_binop`) evaluates filter predicates and ternary conditions. `splice_preserving_ws` preserves the trailing trivia a CST node's `text()` includes (else `true`+`AS` glued to `trueAS`).
  - **`MetaEvalContext { vars }` plumbed via `UpstreamSchemas`, not `Config`.** Adding a `vars` field to `smelt_core::Config` would have broken ~48 struct literals; instead `vars: BTreeMap<String,String>` (coerced) was added to `UpstreamSchemas` (3 construction sites), populated in `from_database` from the project's `smelt.yml`, and reached at all five `SqlCompiler` compile entry points (incl. the static `compile_ephemeral`, whose `EphemeralResolver::new` gained a `vars` param).
  - **`config` excluded from dependency extraction** in both `smelt-core::graph` and `smelt-cli::logical_graph` (alongside `functions`/`sources`/`seeds`): `smelt.config.*` calls are compile-time meta, resolved before codegen, and the analyzer already treats them as non-deps. Without this, dependency validation (which runs before compile) rejected `smelt.config.var` as an undefined model ref.
  - **Aliasing the example select items.** `meta_hofs`/`meta_polish` models' computed select items were aliased (`AS total`, `AS region`, …) to sidestep the pre-existing `apply_type_casts` unaliased-non-identifier-column defect (P5-documented, orthogonal to diagnostic parity); aliasing lets them build + execute so both could be removed from `KNOWN_UNBUILDABLE`.
  - **Reviewer fix.** `and_all`/`or_any` now parenthesise each element (`(e1) AND (e2)`) so a non-atomic boolean element (`a OR b`) cannot re-associate the fold. Two narrower evaluator limitations (bare `reduce(xs, comma_sep)` in a single scalar item; a `filter` predicate the const-evaluator can't reduce → loud build error rather than per-element drop) are recorded in `meta_language.md` §"Known Divergences".
  - **Tests**: 21 `meta_eval` unit tests (fold rendering for every reducer incl. the parenthesisation regression, pipe/filter/map, config.var, ternary, idempotency); 5 `crates/smelt-db/tests/meta_list_scalar.rs` (analysis diagnostic, FROM-less, drop-on-error suppression); `crates/smelt-cli/tests/meta_hofs_e2e.rs` (build + execute on DuckDB, asserting materialised values 6 / false / `'alpha OR beta OR gamma'` / 12 / `'us-west-2'` / `'permissive'`). The new broken fixture's `MetaListInScalarPosition` is confirmed at the `example_builds` gate (analyzer Error → build refused naming the code), proving end-to-end parity.
- **Commit**: `feat(db,runtime): forbid bare List<T> in scalar position + evaluate HOFs/pipe/lambda/ternary/config.var at compile time (closes BUG-006 hofs)`

### P7a — BUG-006c `smelt.columns_of` reflection at build
- **Spec**: `meta_language.md` §"Reflection: `smelt.columns_of` and `ColumnRef`" — Build-path execution (added in this phase) is the oracle: `columns_of(t)` materialises to its concrete `List<ColumnRef>` and the surrounding `map`/`filter`/spread lower to plain select items at compile time.
- **Tests (red-green)**: a **hermetic e2e** (`crates/smelt-cli/tests/meta_columns_e2e.rs`) staging a self-contained project with a seeded base model (a `VALUES` model with a mix of numeric/non-numeric columns, no external source) and a model that consumes `smelt.columns_of(smelt.base)` via `filter(fn c => c.is_numeric)` + `map` + spread (both directly and inside a `smelt.define` body), `smelt build`s it, and asserts the materialised columns/values on DuckDB. Confirmed **red** before the fix (`columns_of` reaches DuckDB → `Catalog "smelt" does not exist` / parse error). `meta_eval` unit tests for the `columns_of` value model. `meta_columns` stays on `KNOWN_UNBUILDABLE` (reads the unseeded `raw.orders` source — fails on the `orders` model before reaching `orders_safe`'s reflection; reason reclassified, e2e is the regression target).
- **Implementation**: extend `crates/smelt-runtime/src/meta_eval.rs` to (1) evaluate `smelt.columns_of(<smelt-path>)` to a `List` of `ColumnRef` record elements using column metadata threaded through `MetaEvalContext` (built from `UpstreamSchemas.models`/sources, reusing the analyzer's numeric classification); (2) resolve `ColumnRef` field projections (`c.name`, `c.is_numeric`) inside `map`/`filter` lambda bodies; (3) lower a spread whose operand is a HOF chain (non-literal list operand) to its evaluated select items. To make a `columns_of` inside a `smelt.define` body visible to the (pre-print) evaluator, the pass first inlines function bodies via the existing `expand_function_calls(sql, fn_bodies)` when a `columns_of` call is present (smelt paths stay symbolic, so the operand is still `smelt.<path>`). Plumb the column metadata + `fn_bodies` into `MetaEvalContext` at all five `SqlCompiler` compile entry points (incl. the static `compile_ephemeral`).
- **Commit**: `feat(runtime): evaluate smelt.columns_of reflection at compile time (closes BUG-006 columns_of)`

### P7b — BUG-006c wide reflection (`smelt.models.*` / `smelt.sources.*`) at build
- **Spec**: `meta_language.md` §"Reflection: `smelt.models`, `smelt.sources`, `ModelRef`, `SourceRef`" — add a Build-path execution note.
- **Tests (red-green)**: hermetic e2e exercising `...map(smelt.sources.with_tag('audit'), fn s => s.name)` and the `smelt.models.*` form. `meta_workspace` stays on `KNOWN_UNBUILDABLE` (unseeded `raw.events`); e2e is the regression target.
- **Implementation**: extend the meta-eval pass to resolve `smelt.models.with_tag`/`.all` and `smelt.sources.with_tag`/`.all` against workspace model/source listings + tags (threaded through `MetaEvalContext`), producing `List<ModelRef>`/`List<SourceRef>` record elements with `path`/`name`/`tags` field projection; reuse the `columns_of` record-element + spread-of-HOF machinery from P7a.
- **As-built notes**:
  - **Listings reuse the analyzer's own queries.** `UpstreamSchemas::from_database` now also builds `model_refs` / `source_refs` (`Vec<EntityRefMeta { path, name, tags }>`) by calling `smelt_db::models_all(db, workspace)` and `smelt_db::sources_all(db, project)` — the *same* Salsa queries the analyzer's `smelt.models.*` / `smelt.sources.*` resolution uses — so the build's tag merge (`Config::get_tags`), `/`-normalised paths, and path-sorted order are byte-identical to the editor's (parity). Models are workspace-wide (`models_all` is a single workspace query); sources are per-project, so they are unioned across `workspace.projects(db)` and **re-sorted by `path`** after the union (each project's `sources_all` is only internally sorted — reviewer finding). Threaded into `MetaEvalContext` as borrowed slices (`&'a [EntityRefMeta]`, matching the borrowed `fn_bodies`) — *not* cloned per `meta_ctx()` call, so the 2000-model `huge` workspace pays no per-compile clone (reviewer finding).
  - **`EntityRefMeta` records render `name` / `path` as `Text` string literals**, not column references — a model/source name is a data value (`SELECT 'cohort_a'`), unlike a `columns_of` `c.name` which is a live column identifier. `entity_ref_record` pre-renders `name` / `path` via `sql_text_literal` (single-quote-doubling escape). `tags` (a `List<Text>`) is **not** projected — `m.tags` has no scalar form, so it is left to the conservative drop-on-error path and recorded as a Known Divergence in `meta_language.md`.
  - **Call form only — bare `smelt.models.all` is deliberately not lowered.** The analyzer recognises wide reflection only at a `SmeltPathCall` (`infer_smelt_path_call_type`); a *bare* `smelt.models.all` path ref (no parens) is collected by `model_path_refs` and rejected as `UndefinedModelRef`. `wide_reflection_parts` therefore matches only the call form (`with_tag('x')`, `all()`), keeping the build-accepted surface equal to the analyzer-accepted surface — a bare `.all` is refused by the P2 gate with the analyzer's own code (reviewer finding; the initial implementation also handled the bare ref, which would have made the build accept a form the editor flags).
  - **`smelt.config.var` tag argument resolves for free.** `with_tag(smelt.config.var('seg'))` (which the analyzer accepts as a compile-time-`Text` argument) is resolved by the pass-1 `expand_config_vars` rewrite *before* wide reflection runs in pass 2/3, so the `with_tag` argument is already a string literal when `extract_string_literal_value` reads it (unit test `with_tag_config_var_argument_resolved`).
  - **Dependency-extraction exclusion (`smelt-core::graph` + `smelt-cli::logical_graph`).** Surfacing wide reflection exposed that the build's ref collection treats `smelt.models.with_tag(...)` / `smelt.models.all` as a model dep (stripping the `models` prefix → a phantom dep on `with_tag` / `all`), failing dependency validation before compile. Both dep builders now exclude `smelt.models.<with_tag|all>` (segment-based), mirroring the already-excluded `smelt.sources.*` and the P6 `smelt.config.*` exclusion. (The build's ref collection differs from the analyzer's `model_path_refs`, which skips refs inside a `SmeltPathCall`; the segment-based exclusion is the graph-builder equivalent.)
  - **`meta_workspace` stays on `KNOWN_UNBUILDABLE`** (deviation from the plan's "Remove"). With wide reflection lowering, the workspace's reflection models build, but the cohort models read the **unseeded** `raw.events` source (no datagen in the standalone build env), so the build fails on the first cohort model — same category as `meta_lists` / `meta_columns` (P5/P7a). The allow-list reason was reclassified to record the actual failure; de-listing is a P8 seeding decision. The hermetic e2e is the regression target.
  - **Tests**: 9 `meta_eval` unit tests (models/sources `with_tag` → name/path literals, `all()` call form, `all()|>filter|>map` composition, config-var tag argument, empty-match-left-verbatim, bare-`.all`-left-verbatim, idempotency). Hermetic e2e `crates/smelt-cli/tests/meta_workspace_e2e.rs` builds + executes on DuckDB, folding the reflected model/source names with `reduce(map(...), concat_with(', '))` into a single aliased scalar (asserting `'cohort_a, cohort_b'` and `'events'`). The bare-spread form `SELECT ...map(..., fn m => m.name)` lowers correctly (unit-tested) but produces unaliased string-literal columns that trip the same pre-existing, orthogonal `apply_type_casts` defect P5 documented for literal spreads, so the e2e uses the fold form for execution. Confirmed **red** before the fix (neutering `eval_wide_reflection` → `smelt.sources.with_tag(...)` reaches DuckDB → parse error).
- **Commit**: `feat(runtime): evaluate wide reflection (smelt.models/sources) at compile time (closes BUG-006 wide reflection)`

### P7c — BUG-006c `smelt.config.load_yaml` / `load_json` loader at build
- **Design decision (resolved 2026-06-02, Andrew)** — *loader value in a bare scalar / SELECT-item position*: **loaders must be consumed** (Option A; consistent with the P6 bare-`List` decision, no new surface). A loader value (`List<record>` / `Map<Text, record>`) reaching a Data-World scalar / SELECT-item position **without** being consumed by spread / HOF / reducer / record / map / generator is the same `MetaListInScalarPosition` (Error) as P6 — this is a current under-report because the P6 detector is structural and does not catch loader calls. So: (1) **extend the P6 detector** to flag bare loader values in scalar position (closing the under-report); (2) build-path lowering handles loader values **only in consuming positions** (spread/HOF/generator), reusing the P7a/P7b record-element machinery; (3) the spec increment records the loader is governed by the existing "lists must be consumed" rule (no new "config table" surface — Option B's `VALUES`-relation materialisation was rejected); (4) **rewrite the `meta_config` example models** to a consuming form (e.g. `SELECT ...load_yaml(...) |> map(fn c => …)` or a generator) and keep them build+execute fixtures; (5) **correct the user docs** (`config-loaders.md`) whose bare `SELECT load_yaml(...)` worked-examples currently imply a non-existent surface; add a negative case asserting `MetaListInScalarPosition` fires on the bare form.
- **Spec**: `meta_language.md` / `meta_config_loading.md` — add a Build-path execution note for the loader family.
- **Tests (red-green)**: build+execute `examples/meta_config` (or a hermetic variant); assert materialised values. Negative fixtures still gate.
- **Implementation**: extend the meta-eval pass to resolve `smelt.config.load_yaml`/`load_json` against the loaded file content (reusing the analyzer's loader resolution), materialising the record/list/map value into the consuming Data-World position. Decide the lowering of a loader value in a bare scalar SELECT position (the `meta_config` `SELECT smelt.config.load_yaml(...)` shape) — this may require a spec clarification on what a loader value renders to in scalar position.
- **Landed (2026-06-02)**: the bare-loader detector (extended P6 detector → `MetaListInScalarPosition` for bare `List`/`Map` loaders) and **List**-loader build-path lowering landed in `58c2fcd4`; the `List` consuming form is proven by `crates/smelt-cli/tests/meta_config_e2e.rs`. The **Map**-loader half (steps 2/4/5 for the `Map` shape) is split to **P7d** — see the "P7c split (2026-06-03)" note above.
- **Commit (landed)**: `feat(runtime): forbid bare List/Map config loaders in scalar position (closes BUG-006 loader consumption)`

### P7d — BUG-006c Map-loader consumption
- **Premise** — the `Map` consumer is the closed Map API already specified in `meta_language.md` §"Maps" (`m.entries()` / `m.keys()` / `m.values()` / `m.get` / `m.has`), with the `MAP_METHOD_CALL` parser node and `type_inference/record.rs` inference already landed. This phase is **implementation of the existing surface, not a new one** (the discarded free-function design is recorded in `docs/research/2026-06-03-map-consumption-surface.md`). The blockers are: (a) the parser emits `MAP_METHOD_CALL` only for a bare-identifier receiver, so `load_yaml(...).entries()` does not parse; (b) the loader family is not lowered at build; (c) `check_file_diagnostics` does not walk `MAP_METHOD_CALL`.
- **Spec**: `meta_language.md` — §"Map API" clarified so the method receiver may be any `Map`-typed expression (a loader call included); §"Known Divergences" notes the loader-call-receiver parsing + loader build-path lowering gap (both tracked here). On land, narrow the loader-not-lowered and `MAP_METHOD_CALL`-not-walked divergence bullets. `meta_config_loading.md` — add a Build-path execution note for the loader family. No new surface.
- **Tests (red-green)**:
  - *Parser* (`crates/smelt-parser/src/parser/tests.rs`): `smelt.config.load_yaml('p', Map<Text, {…}>).entries()` parses to a `MAP_METHOD_CALL` whose receiver is the loader `FUNCTION_CALL`; same for `.keys()`.
  - *Type inference* (`crates/smelt-db/src/type_inference/tests.rs`): a loader-call-receiver `.entries()` infers `List<{key, value}>`, `.keys()` infers `List<Text>`; a non-`Map` receiver still raises the receiver-type diagnostic.
  - *Scalar position* (`crates/smelt-db/tests/meta_list_scalar.rs`): a bare `load_yaml(…, Map<…>).entries()` (a `List` result) still emits `MetaListInScalarPosition`; the consumed `… |> map(fn e => e.key)` form emits zero.
  - *Diagnostics wiring* (`file_diagnostics`): an unknown Map method on a loader receiver surfaces `MapApiUnknown` via `file_diagnostics` (walks `MAP_METHOD_CALL`).
  - *Build + execute* (`crates/smelt-cli/tests/meta_config_e2e.rs`): `tenants.sql` (Map) materialises the expected keys/values from `configs/tenants.yaml`; the whole `examples/meta_config` workspace builds with zero diagnostics.
- **Implementation**:
  1. *Parser* — lift `MAP_METHOD_CALL` into the postfix position (alongside array subscript at `expr.rs:706`) so a Map method call chains on any primary, notably a loader `FUNCTION_CALL` receiver.
  2. *Build path* — extend the meta-eval pass (`crates/smelt-runtime/src/meta_eval.rs`) to resolve the loader value and evaluate the Map method call against it (`entries`/`keys`/`values` → the corresponding `List`, sorted ascending by key per spec), reusing the P7a/P7b record-element machinery, then lower the consuming spread/HOF to plain select items.
  3. *Diagnostics* — walk `MAP_METHOD_CALL` in `check_file_diagnostics`, calling the existing pure inference functions.
  4. *Examples* — rewrite `examples/meta_config/models/tenants.sql` to consume the Map via `.entries()`/`.keys()`, and `examples/meta_config/models/cohorts.sql` to the `reduce(map(load_yaml(...), …), …)` List form, so the workspace is build+execute-clean; remove `meta_config` from `KNOWN_UNBUILDABLE`.
  5. *User docs* — `docs-site/docs/meta-language/{maps,config-loaders}.md`: show the `.entries()`/`.keys()` consuming form on a loader; add the negative case (bare Map loader → `MetaListInScalarPosition`).
- **Commit**: `feat(parser,runtime): consume Map config loaders via the Map API at build (closes BUG-006 Map loader)`

### P8 — Close-out
- **Tests**: `example_builds` `KNOWN_UNBUILDABLE` is empty except intentional `*_broken_*` markers; full suite + all gates green.
- **Docs**: remove the now-resolved drift bullet from `architecture.md` Known Divergences; update `docs/ROADMAP.md`; flip BUG-006/011/013/015/018/019/024/032 to `fixed` in `docs/bug-hunt/2026-05-30-findings.md` with their regression-test names.
- **Commit**: `docs(diag-parity): close out — examples all build, ledger + spec + roadmap updated`

## Verification
- Every table row `done`.
- `cargo fmt --all -- --check`, `cargo clippy --all-targets` (no warnings), `cargo test`, `cargo test -p smelt-cli --test example_diagnostics`, `cargo test -p smelt-cli --test example_builds`, `cargo test -p smelt-lsp --test example_workspaces`, `cargo test -p smelt-runtime` all green.
- Each `Closes` bug has a red-green regression test (a `*_broken_*` build-gate assertion or a build+execute output assertion), confirmed red on `git stash` of the fix.
