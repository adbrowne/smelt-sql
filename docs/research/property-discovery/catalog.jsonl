{"id": "P0-1", "construct": "(infra)", "source_property": "(infra)", "technique": "(infra)", "layer": "harness", "skeleton_cols": null, "status": "done", "hypothesis": "Build the in-process real-planner PBT harness: drive smelt-runtime::execute_project over a temp DuckDB with generator-produced rows + a generated run schedule; read back the table. No hand-injected WHERE. Gating deliverable (design 3a).", "owning_test": "smelt-cli::tests::property_discovery::smoke::execute_project_derives_time_filter_no_hand_injected_where", "appended_from": null}
{"id": "P0-2", "construct": "(infra)", "source_property": "(infra)", "technique": "(infra)", "layer": "harness", "skeleton_cols": null, "status": "done", "hypothesis": "Run-schedule generator: a proptest value = sequence of steps (advance-window+run | append-late-row | in-place-update/delete). Snapshot source contents each step for the step-k oracle baseline.", "owning_test": "smelt-cli::tests::property_discovery::p0_2_run_schedule::step_k_snapshot_differs_from_pre_populated_after_late_append", "appended_from": null}
{"id": "P0-3", "construct": "(infra)", "source_property": "(infra)", "technique": "(infra)", "layer": "harness", "skeleton_cols": null, "status": "done", "hypothesis": "Oracle: EXCEPT ALL (multiset) over ALL columns minus provably-payload-and-declared-nondeterministic; full-refresh baseline = source snapshot at step k; per-cell comparison mode. Prove a duplicated identical row makes EXCEPT ALL non-empty (vs plain EXCEPT).", "owning_test": "smelt-cli::tests::property_discovery::oracle::duplicated_identical_row_is_visible_to_except_all_but_not_except", "appended_from": null}
{"id": "P0-4", "construct": "(infra)", "source_property": "(infra)", "technique": "(infra)", "layer": "harness", "skeleton_cols": null, "status": "done", "hypothesis": "Generator MutationProfile self-check: assert data emitted for a declared profile (append_only/mutable/change_feed) actually has that shape \u2014 an unverified label silently poisons a verdict (F7).", "owning_test": "smelt-cli::tests::property_discovery::p0_4_mutation_profile_selfcheck::arb_mutable_schedule_output_matches_profile_and_actually_mutates", "appended_from": null}
{"id": "P0-5", "construct": "(infra)", "source_property": "(infra)", "technique": "(infra)", "layer": "linkA", "skeleton_cols": null, "status": "done", "hypothesis": "Link-A abstract contract-safety proptest scaffold: model a combiner + generate the 5 adversarial schedule kinds (partition/reorder/re-deliver/backfill/late in-or-out-of-horizon). Refutation-only; emits witness schedule kinds for Link C.", "owning_test": "smelt-db::tests::proptests::maintenance_link_a::idempotent_monoid_fold_delta_matches_batch_over_reorder_redeliver_backfill", "appended_from": null}
{"id": "P0-6", "construct": "(infra)", "source_property": "(infra)", "technique": "(infra)", "layer": "linkB", "skeleton_cols": null, "status": "done", "hypothesis": "Link-B classification-diagnostic scaffold: report smelt analyzer facts (derive_model_bounds reach, discriminants combiner, window_independence, input_delta kind, join_shape) for a model and compare to DuckDB ground-truth probes; emit the skeleton-column floor.", "owning_test": "smelt-db::tests::proptests::maintenance_link_b::clamp_probe_at_derived_margin_matches_true_output", "appended_from": null}
{"id": "SC-1", "construct": "correlated EXISTS (7-day attribution)", "source_property": "append-only (late conversion appended between runs)", "technique": "window-forward / recompute-region", "layer": "linkC", "skeleton_cols": null, "status": "done", "hypothesis": "source_bounds 'no-bound-found' fallback yields optimistic (0,0) for the correlated EXISTS, so smelt's derived filter clamps away a conversion arriving within 7 days AFTER the event \u2192 the maintained `converted` stays false where full-refresh says true. Predicted REFUTED = confirmed unsound acceptance. REACHABILITY: the late conversion MUST be appended between runs (not pre-populated), else both paths see it and the bug is masked.", "owning_test": "smelt-cli::tests::property_discovery::sc_1_correlated_exists::late_conversion_appended_between_runs_within_7_day_window", "appended_from": null}
{"id": "SC-2", "construct": "pass-through + additive agg", "source_property": "clocked mutable (in-place update of a processed partition between runs)", "technique": "window-forward", "layer": "linkC", "skeleton_cols": null, "status": "done", "hypothesis": "input_delta.rs:89-91 classifies a clocked Mutable source as WindowForward (reads only new partitions). An in-place UPDATE of an already-processed partition is never revisited \u2192 maintained table diverges from a full re-scan. Predicted REFUTED = confirmed unsound acceptance.", "owning_test": "smelt-cli::tests::property_discovery::sc_2_clocked_mutable_window_forward::in_place_update_of_already_processed_partition_is_missed_on_forward_only_advance", "appended_from": null}
{"id": "G-01", "construct": "additive agg (SUM/COUNT) group-by", "source_property": "append-only", "technique": "fold-delta", "layer": "linkC", "skeleton_cols": null, "status": "done", "hypothesis": "Additive monoid fold over disjoint append-only deltas equals full-refresh over all schedules \u2192 HOLDS. Control cell establishing the happy path.", "owning_test": "smelt-cli::tests::property_discovery::g_01_additive_agg_append_only::additive_sum_fold_over_disjoint_append_only_windows_matches_full_refresh", "appended_from": null}
{"id": "G-02", "construct": "additive agg (SUM/COUNT) group-by", "source_property": "append-only (delta RE-DELIVERED)", "technique": "fold-delta", "layer": "linkC", "skeleton_cols": "{d}", "status": "done", "hypothesis": "Re-delivering a delta with no dedup ledger double-counts SUM \u2192 EXCEPT ALL non-empty. Tests whether smelt's ledger obligation 'never fold a delta twice' holds under re-run; predicted HOLDS if smelt is idempotent, REFUTED if it double-counts.", "owning_test": "smelt-cli::tests::property_discovery::g_02_additive_agg_redelivery::redelivering_the_same_window_does_not_double_count_the_fold", "appended_from": null}
{"id": "G-03", "construct": "idempotent agg (MAX/BOOL_OR) group-by", "source_property": "append-only", "technique": "fold-delta", "layer": "linkC", "skeleton_cols": "{d}", "status": "done", "hypothesis": "Idempotent monoid fold over append-only equals full-refresh under all schedules incl. re-delivery \u2192 HOLDS.", "owning_test": "smelt-cli::tests::property_discovery::g_03_idempotent_agg_append_only::idempotent_max_fold_over_disjoint_append_only_windows_with_redelivery_matches_full_refresh", "appended_from": null}
{"id": "FIX-1", "construct": "correlated EXISTS / Form-B reach derivation", "source_property": "multi-source (events + conversions)", "technique": "column-aware bound attribution", "layer": "fix", "skeleton_cols": null, "status": "done", "hypothesis": "PRODUCTION FIX (from SC-1). extract_form_b_bounds (crates/smelt-logical/src/analysis/source_bounds.rs:589) takes _partition_col_upper but never uses it \u2014 it attributes a BETWEEN+INTERVAL bound to a source by whole-text scan, column-blind, so the 7-day margin is spuriously ALSO attributed to events (confirmed over-read in SC-1: events read widened to < 2024-01-09). Make Form-B attribution column-aware: attribute the bound only to a source whose partition/event-time column actually appears in the matched BETWEEN operands. Red->green: a test asserting the compiled events read is NOT widened by the conversions 7-day margin (SC-1 spurious over-read gone) while SC-1 conversions bound Bounded(0,7d) is preserved. Gate: cargo test -p smelt-logical (+ any other touched crate) green, fmt+clippy clean, recorded in ledger.", "owning_test": "smelt-logical::analysis::source_bounds::tests::test_form_b_does_not_leak_bound_to_unrelated_source", "appended_from": "SC-1"}
{"id": "FIX-2", "construct": "input_delta_discovery dormant classifier", "source_property": "clocked mutable", "technique": "wire-or-fence", "layer": "fix", "skeleton_cols": null, "status": "blocked", "hypothesis": "FROM SC-2. input_delta_discovery classifies clocked Mutable as WindowForward but has ZERO production call sites (dead code). Wiring it to a consuming maintenance mode is behaviour-DEFINING design (new semantics for mutable sources) -> BLOCK for human review per policy 8(4). In-scope mechanical option ONLY: add a tripwire test that fails if a production path starts consuming InputDeltaKind::WindowForward for a Mutable source without a mutation-profile branch, so the latent trap cannot be wired silently. If even the tripwire needs a semantic choice, record the finding and BLOCK.", "owning_test": "smelt-logical::tests::input_delta_discovery_dead_code_tripwire::input_delta_discovery_has_no_production_call_sites", "appended_from": "SC-2"}
{"id": "G-04", "construct": "idempotent agg (MIN) group-by", "source_property": "mutable-snapshot (in-place update lowering then raising the min)", "technique": "fold-delta", "layer": "linkC", "skeleton_cols": null, "status": "done", "hypothesis": "MIN folded over a mutable source is non-invertible: fold = min-ever-seen, full-refresh = min-in-current-snapshot. Predicted REFUTED (observer semantics; paper 4). Whether smelt REFUSES this cell up front (over-conservative-good) or maintains it (unsound) is the finding.", "owning_test": "smelt-cli::tests::property_discovery::g_04_idempotent_min_mutable_snapshot::in_place_update_lowering_then_raising_the_min_is_recovered_on_backfill", "appended_from": null}
{"id": "G-05", "construct": "inner-join enrichment (fact x dim)", "source_property": "mutable dimension (in-place update between runs)", "technique": "column-scoped re-derivation", "layer": "linkC", "skeleton_cols": "{d,user_id}", "status": "done", "hypothesis": "A dimension update broadcasts to every fact row referencing it (breaks A, keeps B; paper 10). Does smelt re-derive the enriched column over the horizon, or miss the dimension change? Predicted REFUTED-or-CONDITIONAL depending on horizon derivation.", "owning_test": "smelt-cli::tests::property_discovery::g_05_join_enrichment_mutable_dimension::dimension_update_between_runs_is_recovered_on_backfill_but_not_forward_advance", "appended_from": null}
{"id": "G-06", "construct": "left-join (null-preservation)", "source_property": "append-only fact + late-arriving right side", "technique": "recompute-region", "layer": "linkC", "skeleton_cols": "{d,user_id}", "status": "done", "hypothesis": "A left-join row is emitted with NULLs then a matching right row arrives late. Recompute-region over the step-k snapshot should equal full-refresh \u2192 HOLDS; fold/window-forward may strand the NULL row \u2192 REFUTED. Skeleton includes the join key.", "owning_test": "smelt-cli::tests::property_discovery::g_06_left_join_null_preservation::late_refund_appended_between_runs_is_recovered_on_backfill_but_not_forward_advance", "appended_from": null}
{"id": "G-07", "construct": "holistic agg (MEDIAN / COUNT DISTINCT)", "source_property": "append-only", "technique": "recompute-region", "layer": "linkC", "skeleton_cols": "{d}", "status": "done", "hypothesis": "No bounded combiner state (non-monoid; Link 0). Fold-delta is not available; smelt should REFUSE (NotDerivable). Predicted CONDITIONAL/REFUTED \u2014 recompute-region is the only valid technique. REVISED (Link-B research): smelt's `refresh: batched` technique is ALWAYS recompute-region (DELETE+INSERT full partition recompute), never fold-delta \u2014 `combiner_discriminants`/`Discriminants` is consulted only by the cumulative/running-total and join-fan-out features, never by `rules/incremental.rs`'s batched GROUP BY path. So holistic combiners are safe by construction (no partial state to fold). Predicted (revised): HOLDS unconditionally, including under re-delivery.", "owning_test": "smelt-cli::tests::property_discovery::g_07_holistic_agg_append_only::holistic_median_and_count_distinct_fold_over_disjoint_append_only_windows_with_redelivery_matches_full_refresh", "appended_from": null}
{"id": "G-08", "construct": "windowed running total (ROWS UNBOUNDED PRECEDING)", "source_property": "append-only (late row appended between runs)", "technique": "fold-delta (end-state) / stored trajectory", "layer": "linkC", "skeleton_cols": null, "status": "done", "hypothesis": "A late row has unbounded-forward footprint over the stored trajectory (breaks A; paper 7/10). End-state projection folds fine; stored trajectory needs every later partition rewritten. Predicted CONDITIONAL (as-of-run) for the trajectory grain.", "owning_test": "smelt-cli::tests::property_discovery::g_08_running_total_self_ref::late_transaction_into_an_already_processed_partition_requires_a_downstream_cascade", "appended_from": null}
{"id": "G-09", "construct": "UNION ALL of two sources", "source_property": "append-only both arms", "technique": "recompute-region", "layer": "linkC", "skeleton_cols": null, "status": "done", "hypothesis": "Multiset union composes: recompute-region over the step-k snapshot equals full-refresh \u2192 HOLDS. Tests that smelt's bound derivation composes across UNION arms (each arm's reach unioned).", "owning_test": "smelt-cli::tests::property_discovery::g_09_union_all_append_only::late_rows_appended_into_both_union_arms_between_runs_are_recovered_on_backfill_but_not_forward_advance", "appended_from": null}
{"id": "G-10", "construct": "join fan-out on COMPOSITE unique key", "source_property": "append-only", "technique": "column-scoped re-derivation", "layer": "linkB", "skeleton_cols": null, "status": "done", "hypothesis": "join_shape proves OneToOne on single-column keys only; a composite-key fact-dim join classifies OneToMany, so any fan-out-bounded claim on a composite key is mislabeled. Predicted over-conservative or unsound in join_shape \u2014 a Link-B classification finding.", "owning_test": "smelt-db::tests::proptests::maintenance_link_b_composite_key_fan_out::fan_out_cannot_express_composite_unique_key_and_conservatively_classifies_one_to_many", "appended_from": null}
{"id": "SC-1b", "construct": "correlated EXISTS with an unrelated BETWEEN in the same model (column-blind Form-B attribution)", "source_property": "append-only", "technique": "recompute-region", "layer": "linkC", "skeleton_cols": null, "status": "pending", "hypothesis": "SC-1's Link-B finding: `source_bounds::extract_form_b_bounds` scans the WHOLE model SQL for any `col BETWEEN expr AND expr +/- INTERVAL` pattern and attributes whatever it finds to EVERY source in the BoundContext, ignoring which column/source the match actually belongs to (`_partition_col_upper` param unused). SC-1's own construct happened to have only one such pattern, so the accidental attribution was correct. This cell adds a SECOND unrelated BETWEEN+INTERVAL clause referencing a different source/column (e.g. a WHERE filter on an unrelated payload column) ahead of the real correlated-EXISTS clause in text order, or reorders the true pattern to use `expr - INTERVAL ... AND expr` (lookback form) instead of the lookahead form SC-1 used. Predicted REFUTED = the wrong bound (from the unrelated clause) gets attributed to `conversions`, clamping away the late conversion the same way the original SC-1 hypothesis predicted.", "owning_test": null, "appended_from": "SC-1"}
{"id": "G-11", "construct": "self-referential batched model (running-total self-join)", "source_property": "n/a (execution-layer, not a source-property cell)", "technique": "outer time-filter clamp qualification", "layer": "linkC", "skeleton_cols": null, "status": "pending", "hypothesis": "FROM G-08. The exact self-referential-batched-model shape `docs/specs/batched_models.md` documents and `window_independence`'s own unit tests use (a DIRECT join of the driving source to `smelt.<self>`, both exposing the partition column under its OWN name, e.g. `t.d`/`bal.d`) fails at EXECUTION time, not just in maintenance correctness: `crates/smelt-runtime/src/transformer.rs::inject_time_filter` injects the outer output-clamp as a BARE, unqualified `event_time_column` (`format!(\"{col} >= ... AND {col} < ...\")`, no table/alias qualification) whenever the model has more than one bounded source (`is_transparent_single_source` is false for any self-referential model with a nonzero self-margin, since the self-edge is itself a second bounded source). A self-referential model's own driving source and its self-reference both naturally expose a column literally named the partition column (the self table's own materialized schema IS the model's own declared `partition_column`) -- so the direct-join form hits DuckDB `Binder Error: Ambiguous reference to column name` on every run, not merely a wrong-answer divergence. G-08's own test could only proceed by wrapping the self-join in a subquery (`SELECT d, balance FROM (... direct self-join ...) inner_balance`) so the outer clamp's FROM scope exposes only one `d`-named column -- a workaround the spec's own documented pattern does not describe or require. Candidate fix: qualify the injected filter to the specific FROM-item that supplies the model's own output `event_time_column` (the driving/self source, resolved the same way `cumulative.rs`'s `resolve_single_anchor` already picks a driving fact) rather than an unqualified bare column -- OR require/auto-apply the subquery-wrapping this cell discovered is necessary. Recording as its own cell rather than folding into G-08's fix-or-block decision because determining the CORRECT qualification target (which FROM item legitimately owns the output column when several expose the same name) is itself judgment-bearing and deserves its own red-green cell, not a same-iteration side effect.", "owning_test": null, "appended_from": "G-08"}
