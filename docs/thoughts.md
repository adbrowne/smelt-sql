could create a clearer logical vs physical transformation layers (there in part already). sql mesh like virtual support could be build on the physical layer

types fully resolved at the logical layer?

how do we integrate pyspark/external transformations? we may just need to represent them as models and specify what inputs they require and is the shape of the model they produce.

we need to think more deeply about how smelt can be a library. that then enables you to do things on top. a types sql toolkit.
