name: functions_demo
version: 1

paths:
  - models

targets:
  dev:
    type: duckdb
    database: target/dev.duckdb
    schema: main

default_materialization: view

# Enable unstable schema features (e.g., provenance: frontmatter key).
unstable_schema: true
