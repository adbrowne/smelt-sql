name: meta_columns_broken_columns_of_unresolvable_schema
version: 1

paths:
  - models

targets:
  dev:
    type: duckdb
    database: target/dev.duckdb
    schema: main

default_materialization: view
