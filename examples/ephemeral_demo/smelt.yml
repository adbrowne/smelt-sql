name: ephemeral_demo
version: 1

paths:
  - models
  - checks

targets:
  dev:
    type: duckdb
    database: target/dev.duckdb
    schema: main

default_materialization: view

models:
  daily_revenue:
    materialization: table
  user_totals:
    materialization: table
