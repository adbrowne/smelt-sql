name: ephemeral_demo
version: 1

model_paths:
  - models

targets:
  dev:
    type: duckdb
    database: target/dev.duckdb
    schema: main

default_materialization: view

models:
  daily_revenue:
    materialization: table
  user_totals:
    materialization: table
