# Active multi-session plan pointer (branch worktree-roadmap_todo)
#
# Read by the autonomy loop's iteration prompt (.claude/scripts/autonomy-loop.sh)
# and by a fresh-context Claude that typed `continue` after `/clear`.
#
# STRUCTURE: master_plan is the registry-driven backlog; the loop executes the
# first READY sub-plan in its "## Spawned sub-plans" table (Status not `done`
# AND a `pending` phase exists), top-to-bottom. active_subplan is vestigial
# under unified mode (the registry table wins) — kept for fresh-context flows.
#
# ─────────────────────────────────────────────────────────────────────────────
# THIS BRANCH runs the QUALITY-GRIND programme ONLY (adopted 2026-07-18).
# It deliberately diverges from main's active-plan (which drives the
# maintenance-conformance programme in a DIFFERENT worktree). Two loops run
# concurrently; this one must never pick up the other's master. On merge
# conflicts with origin/main over this file, keep THIS branch's version
# (the quality-grind pointer).
#
# ACTIVE (2026-07-18): the quality-grind programme — small root-caused fixes
# (parser ledger categories, TABLESAMPLE ordering, VALUES arity, UTF-8
# positions, registry gaps, doc gaps) then generator coverage + planner/logical
# consolidation + bench-regression profiling. Registry-driven, no probe rows.
#   Master:   docs/plans/20260718-quality-grind.md
#   Sub-plans: docs/plans/20260718-quality-grind-t1.md (12 phases)
#              docs/plans/20260718-quality-grind-t2.md (9 phases)
# Tier 3 decision items live in the master's "## Tier 3 — decision queue" —
# the loop NEVER picks those up.
#
# PREREQUISITE: DUCKDB_LIB_DIR + LD_LIBRARY_PATH exported into the loop env
# (unset → DuckDB legs skip green, which is a silent hole — the parser
# differential and property-oracle gates are DuckDB-backed).
# ─────────────────────────────────────────────────────────────────────────────
master_plan: docs/plans/20260718-quality-grind.md
active_subplan: docs/plans/20260718-quality-grind-t1.md

# Back-compat aliases (older fresh-context flows read these names).
in_repo_plan: docs/plans/20260718-quality-grind.md
meta_plan: docs/plans/20260718-quality-grind.md
