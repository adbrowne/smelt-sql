Resume the smelt PROPERTY-DISCOVERY research loop with fresh context. You do exactly ONE unit of work (one catalog cell) and emit exactly one sentinel. This is a RESEARCH engine: you produce evidence (proptests + a verdict ledger), you do NOT redesign smelt's refresh/planner behaviour.

READ FIRST (every iteration):
1. docs/research/20260705-property-discovery-loop.md — the design (the four-link proof architecture: Link 0 combiner-algebra table, Link A abstract pre-filter, Link B classification diagnostics, Link C the execution GATE that runs smelt's REAL emitted maintenance over adversarial run schedules and diffs vs full-refresh). Internalise §2, §3, and the verdict vocabulary.
2. docs/plans/20260705-property-discovery-loop.md — the plan (per-cell routine, phase acceptance, reviewer checklist).
3. docs/research/property-discovery/catalog.jsonl — the backlog. Each line is a cell.

PICK THE CELL:
4. Choose the FIRST cell whose "status" is "pending" (skip "done"/"blocked"), reading top-to-bottom. Cells are ordered so infra (P0-*) precedes the seed bugs (SC-1/SC-2) precedes the property grid — respect that order; do not start a property cell while a P0 infra cell it depends on is still pending (if so, that infra cell IS your work this iteration).

DO THE WORK (reuse smelt; put proof code into smelt's TEST surface):
5. Build or extend the proptest for this cell's layer. WHERE CODE GOES (dev-dep-cycle-safe):
   - Link-C cells live in crates/smelt-cli/tests/property_discovery/ — reuse the existing link_c_harness.rs (LinkCProject drives execute_project); put the model's SQL in model_shapes.rs (the ONE readable catalogue of every construct's model — add your construct's fn there, don't inline SQL in the test); add one module per cell and declare it in main.rs. Do NOT put Link-C code in smelt-db tests (smelt-runtime depends on smelt-db → dev-dep cycle).
   - Link-A (abstract) and Link-B (classification) tests live in crates/smelt-db/tests/ reusing prop_helpers/ (generators + duckdb_oracle).
   NON-NEGOTIABLE rules from the design:
   - Link C MUST run smelt's emitted maintenance through smelt-runtime::execute_project (the real bound derivation — NO hand-injected WHERE). NEVER use crates/smelt-cli/tests/incremental/main.rs run_incremental_sequence / execute_model_incremental (they bypass the analyzer — the exact bug class we hunt).
   - Model SQL in model_shapes.rs uses RAW string literals (r#"..."#) — a \-newline continuation strips YAML indentation and flattens the frontmatter block.
   - Data is PROPTEST-GENERATED (extend crates/smelt-db/tests/prop_helpers/generators.rs) and run against the DuckDB oracle (prop_helpers/duckdb_oracle.rs). NOT smelt-datagen, NOT e2e fixtures.
   - The run schedule can MUTATE THE SOURCE BETWEEN RUNS (append a late row into an already-processed range; update/delete in place) — a pre-populated source masks lateness/mutation bugs.
   - Oracle = EXCEPT ALL (multiset) over ALL columns, minus only columns that are provably payload AND declared non-deterministic; full-refresh baseline = the source snapshot at step k.
   - Include the cell's SEEDED construct-specific adversarial schedule for its known hazard, not only generic kinds.
   - RED-GREEN: for a candidate-bug cell, first REPRODUCE the divergence; then commit the test in a state that encodes the verdict.
6. Run just this test: `cargo test -p <crate> --test <t> <name> --quiet 2>&1 | tail -40`. (DUCKDB_LIB_DIR + LD_LIBRARY_PATH are set by the wrapper; if a DuckDB error shows they are unset, BLOCK the cell — do not fake a verdict.)
7. Any smelt-internal extension you need must be marked `// EXPERIMENTAL(property-discovery): disposable`, be test-target-only (under #[cfg(test)] or tests/), and pass `.claude/scripts/property-experimental-gate.sh`. Record it in the ledger cell.

RECORD + COMMIT:
8. Append a verdict block to docs/research/property-discovery/ledger.md (schema in design §7): verdict (HOLDS | REFUTED | CONDITIONAL | BLOCKED), P, skeleton_cols, Link-B facts, smelt-analyzer verdict (sound/over-conservative/unsound/not-derivable), Link-C result (no divergence over N | WITNESS), condition if CONDITIONAL, evidence (test path::name). HOLDS means "no counterexample over N schedules", NEVER "proven".
9. If REFUTED or CONDITIONAL, add one line to docs/research/property-discovery/unsupported.md: `<construct> × <source> — technique <T>: UNSUPPORTED — <why>`.
10. Set this cell's "status" to "done" (or "blocked") in catalog.jsonl, and fill "owning_test". Optionally append ≤2 adjacent pending cells (each with "appended_from" naming this cell).
11. `cargo fmt --all`. Commit + push everything (test + catalog + ledger + unsupported). Use a message like `probe(property-loop): <cell-id> <construct>×<source>×<technique> → <verdict>`.

RECORD-AND-CONTINUE (no hard stop): if the cell hits a design fork you must not decide autonomously, OR needs infra that doesn't exist yet (e.g. a change-feed generator), OR a pre-flight red that is NOT this cell's target: set the cell "status" to "blocked" with a one-line reason, append a dated entry to the plan's "## Blocked phases" section, restore a clean committed tree, commit + push, and emit <<PROBE_BLOCKED>>. The next iteration picks the next pending cell.

SENTINEL CONTRACT — your final user-facing message MUST contain exactly one of:
  <<PROBE_COMPLETE>>      (cell resolved + committed — loop continues)
  <<PROBE_BLOCKED>>       (recorded + continue)
  <<CATALOG_EXHAUSTED>>   (no pending cell remains — loop stops for human)
Put any one-line reason on the line ABOVE the sentinel. The wrapper greps the final result for these; without one the loop halts.
