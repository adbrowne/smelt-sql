[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "smelt-sdk"
version = "0.1.0"
description = "SDK for writing smelt planner rules in Python"
license = "MIT"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
