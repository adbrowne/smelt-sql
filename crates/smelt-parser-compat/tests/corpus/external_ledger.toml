# External-corpus failure ledger — architecture spec §Constraints #13
# "corpus grounding". Keyed by a stable hash of the normalized statement
# (see `stmt_hash` in tests/external_corpus.rs — FNV-1a 64-bit over the
# exact corpus line's UTF-8 bytes, a frozen algorithm stable across runs,
# machines, and Rust toolchain releases). Each entry
# records a `category` (reusing crates/smelt-parser-compat/src/gaps.rs
# vocabulary where a gap corresponds to an existing registry entry) and a
# free-text `note`.
#
# Provenance: this ledger was populated by running
# `cargo test -p smelt-parser-compat --test external_corpus` against the
# corpus vendored under tests/corpus/external/ and bucketing every failure
# by pattern-matching its SQL text against a small set of known DuckDB/
# PostgreSQL grammar-gap signatures (dollar-quoted strings, MAP literals,
# full-text search operators, window-frame EXCLUDE clauses, and so on —
# many of these signatures already exist as smelt-fails gaps.rs entries).
# A large residual of failures did not match a known signature; those are
# recorded under `category = "smelt_fails_unclassified"` with the actual
# first smelt parser error message as the note — an honest, mechanically
# derived diagnostic rather than a guessed root cause, given the sheer
# grammar-surface diversity these upstream test suites intentionally probe.
# NONE of these gaps are fixed by this phase; fixing is follow-on work
# sized by these ledger counts (see architecture.md Known Divergences and
# the plan's "Explicitly deferred" section).
#
# Shrink-only pressure: `ledger_has_no_stale_entries` in
# tests/external_corpus.rs fails the build if a listed statement now passes
# or no longer matches any corpus statement — remove the entry then.

["b86e3620c3472ed0"]
category = "aggregate_call_order_by_clause"
note = "An ORDER BY clause inside an aggregate/window function call's argument list (ordered-set aggregates like WITHIN GROUP, or DuckDB's agg(expr ORDER BY ...) / array_agg(DISTINCT ... ORDER BY ...) direct form) is not parsed"

["60f4625d09268f8f"]
category = "aggregate_call_order_by_clause"
note = "An ORDER BY clause inside an aggregate/window function call's argument list (ordered-set aggregates like WITHIN GROUP, or DuckDB's agg(expr ORDER BY ...) / array_agg(DISTINCT ... ORDER BY ...) direct form) is not parsed"

["430fadb76ce45e99"]
category = "aggregate_call_order_by_clause"
note = "An ORDER BY clause inside an aggregate/window function call's argument list (ordered-set aggregates like WITHIN GROUP, or DuckDB's agg(expr ORDER BY ...) / array_agg(DISTINCT ... ORDER BY ...) direct form) is not parsed"

["d16194276459eb33"]
category = "group_by_tuple_expression"
note = "GROUP BY (col1, col2, ...) / ROLLUP((expr), (expr)) grouping-tuple-of-expressions syntax is not parsed"

["a0c862cd03f7795f"]
category = "qualify_with_window_alias"
note = "QUALIFY referencing a named WINDOW clause alias (WINDOW w AS (...) QUALIFY ... OVER w) is not parsed"

["844a4cf2785f72ce"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["dcfad1c21ca2871b"]
category = "star_exclude_clause"
note = "SELECT * EXCLUDE (col_list) is not parsed at all (recategorized: the chained-dot column reference inside the EXCLUDE list now parses fine on its own; the blocker is the missing star-exclude clause grammar, which is out of scope for this pass since it also needs wildcard-expansion semantics in smelt-db)"

["d4664bad27b9ae0f"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["68209df8cf51bf2f"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["f6ebc1f04febf925"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["4ad43ad287680e7f"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["f81816ae565ef848"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["8cf0888b943601b7"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["9524a6c3963599d3"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["7a69089248afe15c"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["8435920558dfc484"]
category = "struct_type_name_cast"
note = "STRUCT(field TYPE, ...) used as a cast/type-literal target (e.g. col::STRUCT(a INT, b VARCHAR)) is not parsed as a type name"

["f099a4257eff7688"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["a47297fba94ff4c4"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["088a53f703f9a29b"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["8086f41036421b74"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["c4b11046e37641d3"]
category = "struct_type_name_cast"
note = "STRUCT(field TYPE, ...) used as a cast/type-literal target (e.g. col::STRUCT(a INT, b VARCHAR)) is not parsed as a type name"

["0fc51a5a43ff6ee9"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["dc250f60cb2ba1b9"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["473d1d251f89b01d"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["9e47230ac2592631"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["7af72667f9982b6a"]
category = "map_type_or_literal"
note = "MAP {k: v} literal or MAP(key_type, value_type) type is not parsed (tracked gaps.rs::duckdb_map_literal)"

["fe081f2437428f8a"]
category = "array_subscript_negative_or_slice"
note = "Array negative-index or slice subscript syntax is not parsed"

["4563e9312dc33851"]
category = "star_exclude_or_rename_clause"
note = "SELECT * modifier clauses (EXCLUDE(...), RENAME(...)) are not parsed in all positions (nested in list comprehensions, or with a trailing comma in the column list)"

["72346eb01e27547a"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["315571c158974ea3"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["abc0037ef1a7ff76"]
category = "postgres_hash_prefix_operator"
note = "A bare `#` prefix operator/path-reference token (e.g. #1, #thepath) is not parsed as an expression"

["90757f69e6f1528c"]
category = "file_glob_or_path_literal_from"
note = "A glob/path string literal used directly as a FROM target is not parsed"

["4eb105e1f83bc57b"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["bb06d51958809d45"]
category = "file_glob_or_path_literal_from"
note = "A glob/path string literal used directly as a FROM target is not parsed"

["f21f0cdd5e4a6bdc"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["be1b96a2e833999b"]
category = "window_frame_non_integer_bound"
note = "Window RANGE BETWEEN frame with a non-plain-integer bound (interval/float/NaN/EXCLUDE) is not parsed"

["6888fd274860944f"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["110435db7d28187b"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["e40913acdce167cb"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["4b5f29a8027c361a"]
category = "file_glob_or_path_literal_from"
note = "A single-quoted file-path string literal used directly as a FROM target is not parsed (the statement's original RANGE-keyword failure was fixed by the quality-grind range_keyword_as_identifier_or_function phase; this is the distinct root cause left once that one is gone)"

["3156d5843df8e3df"]
category = "file_glob_or_path_literal_from"
note = "A glob/path string literal used directly as a FROM target is not parsed (right-hand side of an EXCEPT; unrelated to set-operation nesting itself)"

["31e689f81aaddaf6"]
category = "file_glob_or_path_literal_from"
note = "A glob/path string literal used directly as a FROM target is not parsed"

["309df939f0e0e423"]
category = "filter_clause_without_where"
note = "FILTER(condition) (DuckDB's bare-condition FILTER shorthand, without a WHERE keyword) is not parsed"

["403890fe1405f8ed"]
category = "file_glob_or_path_literal_from"
note = "A glob/path string literal used directly as a FROM target is not parsed"

["7b71a9f3e3052cea"]
category = "file_glob_or_path_literal_from"
note = "A glob/path string literal used directly as a FROM target is not parsed"

["909599832c250a5a"]
category = "file_glob_or_path_literal_from"
note = "A glob/path string literal used directly as a FROM target is not parsed"

["d8d88b608d1f5372"]
category = "recursive_cte_cycle_using_key"
note = "WITH RECURSIVE ... USING KEY cycle-detection clause is not parsed"

["0287efd3e8516d74"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["8bd4b8deb3f3dbf4"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["555d4c72d46225cc"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["9821e4e6dd8018e7"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["232928ca73dc0ee1"]
category = "if_function_vs_ternary_ambiguity"
note = "SQL IF(cond, a, b) 3-argument function call is not parsed; IF is reserved for the meta-language ternary and is not in at_keyword_as_function_name's list"

["11364d332cae94d5"]
category = "interval_string_literal_unit_in_arg_list"
note = "A DuckDB-style `interval '1' day` typed string literal with a trailing unit keyword is only consumed correctly at SELECT-item top level (where the unit is accidentally swallowed as an implicit column alias); inside a function argument list the unit keyword is left dangling and fails with 'Expected RPAREN'. The statement's original RANGE-keyword failure was fixed by the quality-grind range_keyword_as_identifier_or_function phase; this is the distinct root cause left once that one is gone."

["bb786c3a21b69bb3"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["7d99682d2d443ec3"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["fdddb73327f92808"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["8e5f4a144bd755b4"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["3ed51e20c510117f"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["99ff81ffd052b9ce"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["d496c79b8fc64a9e"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["6165e2fa0040a18e"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["e60a4f9c4a5b6421"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["f3d40f22a0dcb274"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["c32e459dcc171212"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["3d2f3a348900dcb0"]
category = "list_type_bracket_subscript_in_call"
note = "A bracket-typed array literal used as a nested function-call argument (e.g. list_contains(list_value([1,2]::INTEGER[2], ...), ...)) is not parsed"

["d6b2b42e7fbd5615"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["42fc08aa5a971e67"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["eaa0a8e22ad859bb"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["dd08f6ff9fa4843a"]
category = "backslash_escape_in_plain_string_literal"
note = "A trailing backslash immediately before a string literal's closing quote (e.g. parse_dirname('\', ...)) is consumed as an escape by smelt's lexer, which treats backslash as an always-active string escape; this shifts where the literal is judged to close"

["846e3d125c8e02a9"]
category = "caret_at_string_starts_with_operator"
note = "DuckDB's ^@ string starts-with operator is not parsed"

["31e5bad5703082a4"]
category = "caret_at_string_starts_with_operator"
note = "DuckDB's ^@ string starts-with operator is not parsed"

["62a960aea7213de2"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["eb04a0882f486e64"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["165601706514a57b"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["bf4f9acc846503da"]
category = "asof_join_syntax"
note = "DuckDB ASOF JOIN is not parsed"

["fdf234c9e21cdf34"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["3fc21ada2d17e40e"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["24b75a39c9c0bf23"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["f4619b3187dea570"]
category = "join_condition_missing_after_join"
note = "A JOIN whose ON/USING condition itself contains a construct smelt can't parse leaves the table-ref parser positioned such that the JOIN's required table reference is reported missing"

["32b190f08513f4c0"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["f1c243266bfc7ecd"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["c566463312d1e03f"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["bcba706ae690b376"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["65b42acc411df115"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["4370f8971e388a7d"]
category = "sql_json_constructor_functions"
note = "SQL/JSON constructor functions (JSON_OBJECT, JSON_ARRAY) with the standard FORMAT/ON NULL/RETURNING/ENCODING clauses, or a nested SELECT argument, are not parsed"

["da676b8ed997d2e7"]
category = "file_glob_or_path_literal_from"
note = "quoted file-path/glob string literal used directly as a FROM table source (DuckDB file-scan shorthand) is not parsed"

["7dca435889a53d09"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["32f26decb2020f32"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["34c0d550b05d75c8"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["ed076a4bed786a82"]
category = "with_ordinality"
note = "WITH ORDINALITY table-function modifier is not parsed"

["340d518ead2e82d9"]
category = "star_exclude_or_rename_clause"
note = "SELECT * modifier clauses (EXCLUDE(...), RENAME(...)) are not parsed in all positions (nested in list comprehensions, or with a trailing comma in the column list)"

["59b6e8884651ae90"]
category = "grouping_sets_trailing_comma"
note = "A trailing comma inside a parenthesized GROUPING SETS element, e.g. GROUPING SETS (a, (b, )), is not parsed. DuckDB itself rejects this too (verified via the DuckDB oracle) — it is a PostgreSQL-only extension, not required for DuckDB conformance. No smelt gaps.rs entry (that registry tracks DuckDB-conformance gaps only)."

["d93932f2d774acbc"]
category = "pivot_unpivot"
note = "PIVOT/UNPIVOT clauses are not parsed (tracked gaps.rs::pivot_unpivot)"

["327d2fe1686ecd7c"]
category = "star_exclude_or_rename_clause"
note = "SELECT * modifier clauses (EXCLUDE(...), RENAME(...)) are not parsed in all positions (nested in list comprehensions, or with a trailing comma in the column list)"

["85572a0abe900ae0"]
category = "star_exclude_or_rename_clause"
note = "SELECT * modifier clauses (EXCLUDE(...), RENAME(...)) are not parsed in all positions (nested in list comprehensions, or with a trailing comma in the column list)"

["fbe13b47bb1dacaf"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["dca844a46bc203da"]
category = "describe_as_table_source"
note = "DESCRIBE ... used as a nested table source (SELECT ... FROM (DESCRIBE SELECT ...))  is not parsed"

["ea88b2757d9c71e2"]
category = "is_boolean_literal_predicate"
note = "`IS <boolean literal>` (e.g. WHERE a IS false) is not parsed"

["159b1c43ab513ae5"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["cfa83f6ab8d0b3f2"]
category = "row_value_comparison"
note = "A row-value constructor compared with a binary operator, ANY/ALL subquery, IN subquery, or OVERLAPS (e.g. (a,b) < (c,d), (a,b) NOT IN (SELECT ...)) is not parsed"

["7c63c0ec32a851c2"]
category = "row_value_comparison"
note = "A row-value constructor compared with a binary operator, ANY/ALL subquery, IN subquery, or OVERLAPS (e.g. (a,b) < (c,d), (a,b) NOT IN (SELECT ...)) is not parsed"

["8ffa23b311216bca"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["a2a0218136085c71"]
category = "exists_with_cte_subquery"
note = "EXISTS(...) whose body is a CTE-scoped subquery (WITH ... SELECT) is not parsed; parse_exists_expr expects a bare SELECT"

["909102b28165f9f6"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["4e81a9226eafcc77"]
category = "information_schema_roundtrip"
note = "smelt prints a qualified information_schema reference without its schema prefix (silent mis-parse)"

["1fdfbfc3284379d1"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["98685dc6055e7793"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["4444edd7851561d6"]
category = "duckdb_typed_literal_prefix"
note = "A DuckDB typed-string-literal prefix outside the recognized keyword set (e.g. blob '...', BITSTRING'...') is not parsed"

["8150a207f52fa0e4"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["adbd9123def3a1ae"]
category = "postfix_dot_field_access_on_parenthesized_expr"
note = "Postfix `.field` or `.*` access on a parenthesized expression (e.g. (test_conv(...)).*, (NULL::compositetable).oid) is not parsed"

["86d5f8c89a0e4fe2"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["9aec02ab7e317a8e"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["d01c409e1c257644"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["d106520b2b903a2a"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["0f470924dc083f2e"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["0a0802d2ce42c4bb"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["d019f39e7d6696a6"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["f75f39efc83ae601"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["64db298c2123f2cc"]
category = "map_type_or_literal"
note = "MAP {k: v} literal or MAP(key_type, value_type) type is not parsed (tracked gaps.rs::duckdb_map_literal)"

["b53b84a4bb09b36a"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["01c0f2fdd4823d7d"]
category = "array_subscript_negative_or_slice"
note = "Array negative-index or slice subscript syntax is not parsed"

["364300d0dca2b341"]
category = "array_subscript_slice_with_negative_step"
note = "Array/list slice subscript with a negative or expression step (e.g. arr[1:-:2], a[start:stop:step]) is not parsed"

["47d1d0e6b0779852"]
category = "array_subscript_slice_with_negative_step"
note = "Array/list slice subscript with a negative or expression step (e.g. arr[1:-:2], a[start:stop:step]) is not parsed"

["36d81cb73e5419a1"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["08aa64e509d114a3"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["31cbbfb11333cb31"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["d80af49b27e90d8d"]
category = "sqllogictest_template_placeholder"
note = "Recategorized: the chained-dot column references (s.name.v, s.nested_struct.b) now parse fine; the actual blocker is the DuckDB sqllogictest ${source} substitution placeholder in FROM position (not real SQL, not a smelt gap) causing 'Unexpected character: $'"

["76e9af57d672e11b"]
category = "sqllogictest_template_placeholder"
note = "sqllogictest ${...} template placeholder substituted at test-run time by the upstream harness, never valid standalone SQL"

["7a689f12e5ce1105"]
category = "array_slice_bracket_type_literal"
note = "typeof([...]) with a typed-literal array element list is not parsed"

["6419ed0b514e1675"]
category = "table_valued_values_no_parens"
note = "A bare `VALUES (...), (...)` (no enclosing parens) used directly as a FROM table source is not parsed; only `(VALUES ...)` parenthesized form is"

["8d25f22ec749e9ca"]
category = "union_type_name_cast"
note = "DuckDB UNION(tag TYPE, ...) used as a cast/type-literal target is not parsed as a type name"

["10473e2a67b8dceb"]
category = "union_type_name_cast"
note = "DuckDB UNION(tag TYPE, ...) used as a cast/type-literal target is not parsed as a type name"

["122931512d64deb7"]
category = "aggregate_call_order_by_clause"
note = "An ORDER BY clause inside an aggregate/window function call's argument list (ordered-set aggregates like WITHIN GROUP, or DuckDB's agg(expr ORDER BY ...) / array_agg(DISTINCT ... ORDER BY ...) direct form) is not parsed"

["a98f53701e8018fd"]
category = "duckdb_typed_literal_prefix"
note = "A DuckDB typed-string-literal prefix outside the recognized keyword set (e.g. blob '...', BITSTRING'...') is not parsed"

["9c8c8884d1846598"]
category = "window_frame_expression_bound"
note = "A window-frame bound that is a column/expression rather than a literal (e.g. ROWS BETWEEN p PRECEDING AND f FOLLOWING) is not parsed"

["2eeeab6e1921780c"]
category = "window_frame_non_integer_bound"
note = "Window RANGE BETWEEN frame with a non-plain-integer bound (interval/float/NaN/EXCLUDE) is not parsed"

["f7fd82844d7f1224"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["b07a853630e2551e"]
category = "aggregate_call_order_by_clause"
note = "An ORDER BY clause inside an aggregate/window function call's argument list (ordered-set aggregates like WITHIN GROUP, or DuckDB's agg(expr ORDER BY ...) / array_agg(DISTINCT ... ORDER BY ...) direct form) is not parsed"

["4e2938497df48afe"]
category = "aggregate_call_order_by_clause"
note = "An ORDER BY clause inside an aggregate/window function call's argument list (ordered-set aggregates like WITHIN GROUP, or DuckDB's agg(expr ORDER BY ...) / array_agg(DISTINCT ... ORDER BY ...) direct form) is not parsed"

["5a40822826a71b22"]
category = "reserved_word_as_bare_alias"
note = "A reserved keyword used as an explicit `AS alias` (e.g. AS desc) is not parsed; only IDENT (and, since this pass, a double-quoted STRING) is accepted in alias position, not contextual/reserved keywords"

["2a12f587a5d6ca35"]
category = "reserved_word_as_bare_alias"
note = "A reserved keyword used as an explicit `AS alias` (e.g. AS desc) is not parsed; only IDENT (and, since this pass, a double-quoted STRING) is accepted in alias position, not contextual/reserved keywords"

["4fe2ee6cdd0704fd"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["425e2fadb5ea009c"]
category = "any_all_quantified_comparison"
note = "The `expr op ANY (...)`/`expr op ALL (...)` quantified-comparison form outside a WHERE/subquery context (e.g. 33 * any (44), 'foo' like all (array[...])) is not parsed"

["351184b9bb661834"]
category = "any_all_quantified_comparison"
note = "The `expr op ANY (...)`/`expr op ALL (...)` quantified-comparison form outside a WHERE/subquery context (e.g. 33 * any (44), 'foo' like all (array[...])) is not parsed"

["4b1da263d8fc85c9"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["5cd15d2e36445273"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["a3671965a256b79f"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["fd2fd77ba205e851"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["ea7e4d37079be9fb"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["948f61eca609d6f7"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["368958ab62644062"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["4e8b97600a98b622"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["b8783ca07ad8a5f7"]
category = "quoted_type_name_cast"
note = "PostgreSQL double-quoted type name in a cast (e.g. ::'char' (double-quoted in the source)) is not parsed as a type name"

["fad296cb1984b3c2"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["a09178ce769ac702"]
category = "aggregate_call_order_by_clause"
note = "An ORDER BY clause inside an aggregate/window function call's argument list (ordered-set aggregates like WITHIN GROUP, or DuckDB's agg(expr ORDER BY ...) / array_agg(DISTINCT ... ORDER BY ...) direct form) is not parsed"

["778a84897612d89a"]
category = "row_value_comparison"
note = "A row-value constructor compared with a binary operator, ANY/ALL subquery, IN subquery, or OVERLAPS (e.g. (a,b) < (c,d), (a,b) NOT IN (SELECT ...)) is not parsed"

["3dac5fcbdc081e56"]
category = "collation_for_expression"
note = "PostgreSQL's COLLATION FOR (expr) introspection function-like syntax is not parsed"

["edf5f5611a904f27"]
category = "postfix_dot_field_access_on_parenthesized_expr"
note = "Postfix `.field` or `.*` access on a parenthesized expression (e.g. (test_conv(...)).*, (NULL::compositetable).oid) is not parsed"

["a8fd9c1fc1099b82"]
category = "postfix_dot_field_access_on_parenthesized_expr"
note = "Postfix `.field` or `.*` access on a parenthesized expression (e.g. (test_conv(...)).*, (NULL::compositetable).oid) is not parsed"

["1ea4e85c5ee0ca1d"]
category = "postfix_dot_field_access_on_parenthesized_expr"
note = "Postfix `.field` or `.*` access on a parenthesized expression (e.g. (test_conv(...)).*, (NULL::compositetable).oid) is not parsed"

["dc2d3f2f09749641"]
category = "postfix_dot_field_access_on_parenthesized_expr"
note = "Postfix `.field` or `.*` access on a parenthesized expression (e.g. (test_conv(...)).*, (NULL::compositetable).oid) is not parsed"

["eb37ac7ea2d5b1a5"]
category = "regtype_regclass_cast"
note = "PostgreSQL OID alias types (regtype/regclass/regproc/regoper) cast syntax is not parsed/bound the same way"

["9ba1807ec4998afc"]
category = "postgres_table_inheritance_wildcard"
note = "PostgreSQL table-inheritance wildcard suffix (FROM table*, meaning include child tables) is not parsed"

["6af276b398df67ff"]
category = "postgres_table_inheritance_wildcard"
note = "PostgreSQL table-inheritance wildcard suffix (FROM table*, meaning include child tables) is not parsed"

["1bf158008cd20368"]
category = "postgres_table_inheritance_wildcard"
note = "PostgreSQL table-inheritance wildcard suffix (FROM table*, meaning include child tables) is not parsed"

["b99ec95b79923245"]
category = "select_into_clause"
note = "SELECT ... INTO [TABLE] target (PostgreSQL's SELECT-INTO table-creation form) is not parsed"

["bd85096b1212237c"]
category = "regtype_regclass_cast"
note = "PostgreSQL OID alias types (regtype/regclass/regproc/regoper) cast syntax is not parsed/bound the same way"

["b2ac103355cb7e31"]
category = "information_schema_roundtrip"
note = "smelt prints a qualified information_schema reference without its schema prefix (silent mis-parse)"

["129988e382e13e37"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["7cb60da0790f2809"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["54ea05c755c84417"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["21509863ffaf373a"]
category = "group_by_tuple_expression"
note = "GROUP BY (col1, col2, ...) / ROLLUP((expr), (expr)) grouping-tuple-of-expressions syntax is not parsed"

["49912a8d8c54c4f9"]
category = "group_by_tuple_expression"
note = "GROUP BY (col1, col2, ...) / ROLLUP((expr), (expr)) grouping-tuple-of-expressions syntax is not parsed"

["4ae2792a411e7deb"]
category = "quoted_type_name_cast"
note = "PostgreSQL double-quoted type name in a cast (e.g. ::'char' (double-quoted in the source)) is not parsed as a type name"

["d9f48bb80aed9a47"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["eca4611dbb482693"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["c4ad9c9be37cc4cb"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["f9aad433dec6f2b9"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["55a082db860d4f88"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["bffe4815fa87b76a"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["3dafe942693dbbe0"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["2e36739aa763413e"]
category = "row_value_comparison"
note = "A row-value constructor compared with a binary operator, ANY/ALL subquery, IN subquery, or OVERLAPS (e.g. (a,b) < (c,d), (a,b) NOT IN (SELECT ...)) is not parsed"

["748cb87386c1f0f4"]
category = "malformed_corpus_statement"
note = "This corpus line is a truncated/malformed extraction artifact (unbalanced quotes or parens from the upstream test file), not valid SQL in any dialect"

["02bbae9411867710"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["e4659dff58a92e06"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["dc057a3eb1dc18cc"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["488d9923b19c0012"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["3bbd15fcaaa9f9e6"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["9446ad94c98a3bf4"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["59d747665c85c525"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["1cf441456a47a4e5"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["58dafb55824fc674"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["d184d544aec4b1b5"]
category = "postgres_interval_range_qualifier"
note = "PostgreSQL's INTERVAL range-qualifier suffix (e.g. interval '1 2:03' day to minute, ... hour to second(2)) is not parsed"

["8cebc9fc4b523c10"]
category = "postgres_interval_range_qualifier"
note = "PostgreSQL's INTERVAL range-qualifier suffix (e.g. interval '1 2:03' day to minute, ... hour to second(2)) is not parsed"

["03290b92394cab6d"]
category = "postgres_interval_range_qualifier"
note = "PostgreSQL's INTERVAL range-qualifier suffix (e.g. interval '1 2:03' day to minute, ... hour to second(2)) is not parsed"

["24591a4aec125466"]
category = "postgres_interval_range_qualifier"
note = "PostgreSQL's INTERVAL range-qualifier suffix (e.g. interval '1 2:03' day to minute, ... hour to second(2)) is not parsed"

["ca48cd01a3411558"]
category = "postgres_interval_range_qualifier"
note = "PostgreSQL's INTERVAL range-qualifier suffix (e.g. interval '1 2:03' day to minute, ... hour to second(2)) is not parsed"

["0b571b09988e1c59"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["d7de0c9a7a556220"]
category = "row_constructor_field_star_projection"
note = "`table.*` used inside a function-call argument or after a nested-record projection (count(t2.*), tststats.priv_test_tbl.* > (1,1)) is not parsed"

["60a4afc966b46247"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["d85681c984b12038"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["4c93773454a47930"]
category = "regtype_regclass_cast"
note = "PostgreSQL OID alias types (regtype/regclass/regproc/regoper) cast syntax is not parsed/bound the same way"

["b10c367899b34065"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["554ceb62765ff459"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["9d9f1aa06f23fe50"]
category = "postgres_table_function_column_type_alias"
note = "A table function's alias with explicit per-column type list (e.g. func(...) q (a text, b text)) is not parsed"

["7ea589a192639dc4"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["048d69fbfe7bcafe"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["0b81fafd4d61f092"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["46776f190688a163"]
category = "json_to_record"
note = "json_to_record/jsonb_to_record table-valued function is not parsed"

["468b19861afcbfa5"]
category = "json_to_record"
note = "json_to_record/jsonb_to_record table-valued function is not parsed"

["54048be0439b653c"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["476ed4749dd5bfdc"]
category = "dollar_quoted_or_backslash_literal"
note = "Dollar-quoted string or backslash-escape string literal form is not parsed (tracked gaps.rs::duckdb_dollar_quoted_string)"

["856ef694096f70cf"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["fd2e29813b4bad83"]
category = "json_to_record"
note = "json_to_record/jsonb_to_record table-valued function is not parsed"

["76e8507c1ad6c6f6"]
category = "json_to_record"
note = "json_to_record/jsonb_to_record table-valued function is not parsed"

["51f1f7d1e2935e55"]
category = "json_to_record"
note = "json_to_record/jsonb_to_record table-valued function is not parsed"

["be9112ed269e53ae"]
category = "jsonb_operators"
note = "PostgreSQL jsonb/json operators (?, @?, ->, containment path-query strings) are not parsed"

["5a792e901bc6a54c"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["6566fd6398c7faff"]
category = "jsonb_operators"
note = "PostgreSQL jsonb/json operators (?, @?, ->, containment path-query strings) are not parsed"

["e01262da97c32168"]
category = "jsonb_operators"
note = "PostgreSQL jsonb operators (#-, #>, #>>, ?, ?|, ?&) are not parsed"

["e6a25377d78d0163"]
category = "jsonb_operators"
note = "PostgreSQL jsonb/json operators (?, @?, ->, containment path-query strings) are not parsed"

["572b76312eebf0f7"]
category = "jsonb_operators"
note = "PostgreSQL jsonb/json operators (?, @?, ->, containment path-query strings) are not parsed"

["91c452ca932b2952"]
category = "jsonb_operators"
note = "PostgreSQL jsonb/json operators (?, @?, ->, containment path-query strings) are not parsed"

["d0830a7407ca91a3"]
category = "jsonb_operators"
note = "PostgreSQL jsonb/json operators (?, @?, ->, containment path-query strings) are not parsed"

["68643dd2001a3530"]
category = "select_into_clause"
note = "SELECT ... INTO [TABLE] target (PostgreSQL's SELECT-INTO table-creation form) is not parsed"

["a04a59359daf5014"]
category = "from_only_clause"
note = "PostgreSQL's FROM ONLY table (exclude child tables) is not parsed"

["31bde0cab7cc702d"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["351fc33e4a180030"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["dac2a7e17cd3ec09"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["d559586053862371"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["49aadfcd5c8ca312"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["cd0aac3ae6402700"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["0ba012a17888bed1"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["dd16678db72225fe"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["e4ae9f2ab72a7f71"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["77b2dca8f6271dd2"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["7f7ff24642627735"]
category = "postgres_range_types"
note = "PostgreSQL range-type constructors/operators (numrange, nummultirange, ...) are not parsed"

["28f0f438b3d446e0"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["83272f10c183f2ac"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["ee6959fa1df17a5f"]
category = "postgres_typed_literal_prefix"
note = "A non-keyword PostgreSQL type name used as a typed-string-literal prefix (e.g. int2 '0', numeric 'NaN', json '{...}') is not parsed; only a fixed keyword set (DATE, TIME, TIMESTAMP, INTERVAL, ...) is recognized in this position"

["a1dabbd802f301f0"]
category = "postfix_dot_field_access_on_parenthesized_expr"
note = "Postfix `.field` or `.*` access on a parenthesized expression (e.g. (test_conv(...)).*, (NULL::compositetable).oid) is not parsed"

["f8a2c50d020865d6"]
category = "numeric_literal_followed_by_ident_no_space"
note = "A numeric literal immediately followed by an identifier-continuation character with no separating whitespace (e.g. 0.0a, 0b, 1x) is intentionally lexed as a single fail-loud ERROR token rather than silently split (see lexer.rs consume_number's documented rationale, verified against DuckDB); this PostgreSQL corpus sample exercises the same ambiguous shape"

["72d4d2e7c7830425"]
category = "numeric_literal_followed_by_ident_no_space"
note = "A numeric literal immediately followed by an identifier-continuation character with no separating whitespace (e.g. 0.0a, 0b, 1x) is intentionally lexed as a single fail-loud ERROR token rather than silently split (see lexer.rs consume_number's documented rationale, verified against DuckDB); this PostgreSQL corpus sample exercises the same ambiguous shape"

["72d0e2e7c77f367e"]
category = "numeric_literal_followed_by_ident_no_space"
note = "A numeric literal immediately followed by an identifier-continuation character with no separating whitespace (e.g. 0.0a, 0b, 1x) is intentionally lexed as a single fail-loud ERROR token rather than silently split (see lexer.rs consume_number's documented rationale, verified against DuckDB); this PostgreSQL corpus sample exercises the same ambiguous shape"

["9c8fe5f0b0ea90e8"]
category = "regtype_regclass_cast"
note = "PostgreSQL OID alias types (regtype/regclass/regproc/regoper) cast syntax is not parsed/bound the same way"

["4fed97a9b0057d83"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["e55b6152c35d6208"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["cdfce3fe07642069"]
category = "regtype_regclass_cast"
note = "PostgreSQL OID alias types (regtype/regclass/regproc/regoper) cast syntax is not parsed/bound the same way"

["0f783c77304dc0a5"]
category = "malformed_corpus_statement"
note = "This corpus line is a truncated/malformed extraction artifact (unbalanced quotes or parens from the upstream test file), not valid SQL in any dialect"

["51f76c1db9da623c"]
category = "postgres_variadic_array_argument"
note = "PostgreSQL VARIADIC argument modifier in a function call (f(VARIADIC array[...])) is not parsed"

["a009b888921792d4"]
category = "multidim_array_type_literal"
note = "Multi-dimensional or bracket array type literal/cast (e.g. INT[2][2], VARCHAR[]) is not parsed"

["61000844d1e6291c"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["c38be65d41d4fd27"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["761352dd4a45659d"]
category = "with_ordinality"
note = "WITH ORDINALITY table-function modifier is not parsed"

["05f040bdd7e71599"]
category = "with_ordinality"
note = "WITH ORDINALITY table-function modifier is not parsed"

["4d0c4fa00c707000"]
category = "with_ordinality"
note = "WITH ORDINALITY table-function modifier is not parsed"

["a1e4b25c020f937c"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["9480ec7e9fa51e51"]
category = "with_ordinality"
note = "WITH ORDINALITY table-function modifier is not parsed"

["1f8daeacf6b69560"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["3964b41895688849"]
category = "row_value_comparison"
note = "A row-value constructor compared with a binary operator, ANY/ALL subquery, IN subquery, or OVERLAPS (e.g. (a,b) < (c,d), (a,b) NOT IN (SELECT ...)) is not parsed"

["e6094c0f4f9355a3"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["c5f6a2e0c67ff76d"]
category = "postgres_geometric_operators"
note = "PostgreSQL geometric-type or custom infix operators (&&, &<, &<|, <<|, <<, >>, ##, ~<=~, *<=, *>=, ...) are not parsed as expression operators"

["534d7081a626d936"]
category = "postfix_dot_field_access_on_parenthesized_expr"
note = "Postfix `.field` or `.*` access on a parenthesized expression (e.g. (test_conv(...)).*, (NULL::compositetable).oid) is not parsed"

["e89f5cc9ca4f64d6"]
category = "order_by_using_operator_class"
note = "ORDER BY col USING <operator> (custom operator-class ordering direction) is not parsed"

["adb984cf2cd20351"]
category = "postgres_table_inheritance_wildcard"
note = "PostgreSQL table-inheritance wildcard suffix (FROM table*, meaning include child tables) is not parsed"

["6a9839262562ec1a"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["2d9242102c8a8704"]
category = "select_into_clause"
note = "SELECT ... INTO [TABLE] target (PostgreSQL's SELECT-INTO table-creation form) is not parsed"

["e6800814a90ebf63"]
category = "postgres_hash_prefix_operator"
note = "A bare `#` prefix operator/path-reference token (e.g. #1, #thepath) is not parsed as an expression"

["b4278cee39d455a2"]
category = "sql_json_constructor_functions"
note = "SQL/JSON constructor functions (JSON_OBJECT, JSON_ARRAY) with the standard FORMAT/ON NULL/RETURNING/ENCODING clauses, or a nested SELECT argument, are not parsed"

["03040bcc83e9d236"]
category = "sql_json_constructor_functions"
note = "SQL/JSON constructor functions (JSON_OBJECT, JSON_ARRAY) with the standard FORMAT/ON NULL/RETURNING/ENCODING clauses, or a nested SELECT argument, are not parsed"

["3ec5993760c7719d"]
category = "sql_json_constructor_functions"
note = "SQL/JSON constructor functions (JSON_OBJECT, JSON_ARRAY) with the standard FORMAT/ON NULL/RETURNING/ENCODING clauses, or a nested SELECT argument, are not parsed"

["8c15ff6acd16d5be"]
category = "sql_json_constructor_functions"
note = "SQL/JSON constructor functions (JSON_OBJECT, JSON_ARRAY) with the standard FORMAT/ON NULL/RETURNING/ENCODING clauses, or a nested SELECT argument, are not parsed"

["2effb464d1a6c8ef"]
category = "sql_json_constructor_functions"
note = "SQL/JSON constructor functions (JSON_OBJECT, JSON_ARRAY) with the standard FORMAT/ON NULL/RETURNING/ENCODING clauses, or a nested SELECT argument, are not parsed"

["211b433174d29594"]
category = "psql_meta_command"
note = "A psql meta-command (\gset, ...) embedded in the corpus statement is not SQL and never parses; upstream-test-harness artifact"

["de88738ea7aa7c95"]
category = "postgres_psql_variable_colon_syntax"
note = "psql's `:'variable'` interpolation-variable colon syntax is not parsed"

["b167115761f11f93"]
category = "psql_meta_command"
note = "A psql meta-command (\gset, ...) embedded in the corpus statement is not SQL and never parses; upstream-test-harness artifact"

["49b2259ed1162c9d"]
category = "row_constructor_field_star_projection"
note = "`table.*` used inside a function-call argument or after a nested-record projection (count(t2.*), tststats.priv_test_tbl.* > (1,1)) is not parsed"

["9eab7a815794a2e9"]
category = "postgres_unicode_escape_string_literal"
note = "PostgreSQL Unicode-escape string literal (U&'...') is not parsed"

["161238bb43fb14f3"]
category = "like_escape_clause"
note = "LIKE ... ESCAPE 'char' custom escape-character clause is not parsed"

["cf201f564ec6d1e3"]
category = "like_escape_clause"
note = "LIKE ... ESCAPE 'char' custom escape-character clause is not parsed"

["3419183ec6d2f76e"]
category = "set_operation_nesting"
note = "A parenthesized query used as a scalar-subquery expression whose *own* first (unparenthesized) operand is itself parenthesized — `((SELECT …) UNION SELECT …)` — is not parsed; the parenthesized right-hand operand of an explicit UNION/EXCEPT/INTERSECT (`A UNION (B)`) and nested `(((SELECT …)))` forms now parse"

["96df5cd72067098a"]
category = "lambda_or_lateral_syntax"
note = "Lambda-arrow/`lambda x, y: ...`/LATERAL join syntax used in a form not yet parsed"

["79cdb2200dc2cadc"]
category = "tablesample_custom_method_name"
note = "TABLESAMPLE with a non-keyword (custom/extension) sampling-method name is not parsed; only the recognized keyword set (BERNOULLI, SYSTEM, ...) is"

["d63aa08ca702f3ab"]
category = "implicit_cross_join_comma_syntax"
note = "FROM t1, t2 (ANSI-89 implicit comma-separated cross join) is not parsed; parse_from_clause's join loop only recognizes explicit JOIN/CROSS JOIN keywords"

["348bff3f69bc56c0"]
category = "postgres_variadic_array_argument"
note = "PostgreSQL VARIADIC argument modifier in a function call (f(VARIADIC array[...])) is not parsed"

["fda6f7054f01a5b7"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["b8d2d9b9020d12ee"]
category = "malformed_corpus_statement"
note = "This corpus line is a truncated/malformed extraction artifact (unbalanced quotes or parens from the upstream test file), not valid SQL in any dialect"

["012bffb042b14579"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["d5b343bc44fadced"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["5033d5c660b5c00d"]
category = "at_time_zone_or_time_tz_type"
note = "AT TIME ZONE operator / TIME WITH[OUT] TIME ZONE type literal is not parsed (tracked gaps.rs::at_time_zone)"

["66b4d5ff8c0ec6bf"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["1031de1ddf96bf80"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["a601f8785bb9631e"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["acebeb246c8295dc"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["9ddc979731608072"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["d5f2010e01e92764"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["6848eae2af6dc971"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["144d89d1ae21fbd9"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["b114e508a907ae77"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["7ed8a1b8e866de24"]
category = "fulltext_search_operators"
note = "PostgreSQL full-text search operators/types (tsvector/tsquery, @@, <->) are not parsed"

["a2336f1f88e308b0"]
category = "regtype_regclass_cast"
note = "PostgreSQL OID alias types (regtype/regclass/regproc/regoper) cast syntax is not parsed/bound the same way"

["191708174fb6c5c1"]
category = "select_with_no_target_list"
note = "PostgreSQL's `SELECT FROM ...` (empty target list before FROM) is not parsed"

["bd5f317418980fde"]
category = "information_schema_roundtrip"
note = "smelt prints a qualified information_schema reference without its schema prefix (silent mis-parse)"

["0baef910b27c1842"]
category = "window_frame_non_integer_bound"
note = "Window RANGE BETWEEN frame with a non-plain-integer bound (interval/float/NaN/EXCLUDE) is not parsed"

["d6216a6d442919fe"]
category = "window_frame_non_integer_bound"
note = "Window RANGE BETWEEN frame with a non-plain-integer bound (interval/float/NaN/EXCLUDE) is not parsed"

["a1818a8e35c2a665"]
category = "window_frame_non_integer_bound"
note = "Window RANGE BETWEEN frame with a non-plain-integer bound (interval/float/NaN/EXCLUDE) is not parsed"

["29049631557d1e26"]
category = "window_frame_non_integer_bound"
note = "Window RANGE BETWEEN frame with a non-plain-integer bound (interval/float/NaN/EXCLUDE) is not parsed"

["0513478606d4f13d"]
category = "xml_functions"
note = "SQL/XML functions (xmlelement, xmlparse, xmlpi, xmltable, ...) are not parsed"

["0b78dd203657b8aa"]
category = "xml_functions"
note = "SQL/XML functions (xmlelement, xmlparse, xmlpi, xmltable, ...) are not parsed"

["5a2ecce08130bc05"]
category = "xml_functions"
note = "SQL/XML functions (xmlelement, xmlparse, xmlpi, xmltable, ...) are not parsed"

["93c5d1c2329ea02b"]
category = "xml_functions"
note = "SQL/XML functions (xmlelement, xmlparse, xmlpi, xmltable, ...) are not parsed"

["3200f90f3e3804af"]
category = "xml_functions"
note = "SQL/XML functions (xmlelement, xmlparse, xmlpi, xmltable, ...) are not parsed"

["3b6d9e2cbbc5e431"]
category = "xml_functions"
note = "SQL/XML functions (xmlelement, xmlparse, xmlpi, xmltable, ...) are not parsed"

["1edba1798c2f03f2"]
category = "xml_functions"
note = "SQL/XML functions (xmlelement, xmlparse, xmlpi, xmltable, ...) are not parsed"

["2729feeaafc6b76b"]
category = "roundtrip_mismatch"
note = "smelt parses cleanly but printed SQL is rejected by DuckDB (silent mis-parse); CTE printer drops a VALUES-clause CTE body (Display for Subquery, reached via Display for Cte's query() call, only prints select_stmt(), not values_clause()), printing 'AS  SELECT ...' with the VALUES rows dropped entirely"

